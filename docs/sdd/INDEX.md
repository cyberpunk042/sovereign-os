# SDD index

Reserved slots for the foundation phase (PRs 1–10) per the Plan-agent
macro-arc (info-hub `raw/dumps/2026-05-16-sovereign-os-macro-arc-plan.md`).
Numbering is three-digit zero-padded, never recycled.

| # | Title | Status | PR | Notes |
|---|---|---|---|---|
| 000 | Project charter | accepted | PR 1 | This repo's foundational charter — mission, SDD+TDD, SFIF, IaC bar, Debian-as-Ark, non-goals. |
| 001 | Cross-repo boundaries | accepted | PR 2 | Cross-repo reference contract (sovereign-os ↔ selfdef ↔ info-hub). Q-011 partial resolution; final closure at CI-guard PR. |
| 002 | Documentation pipeline | accepted | PR 3 | mdbook + MCP config template + CI publishing. Q-A..Q-D open (deploy cadence; missing-page handling; Pages provider; sovereign-os MCP stub). |
| 003 | Substrate survey | review | PR 4 | Resolves Q-001 + Q-016 at Gate 2. Primary recommendation: mkosi-on-Debian-13. Alternatives A (live-build), B (rpm-ostree/Silverblue), C (NixOS). Q4-A..Q4-E open for Gate 2 closure. |
| 004 | Profile schema | review | PR 5 | Resolves Q-002 (in part) at Gate 3. Single-parent inheritance + composition via mixins; YAML; substrate-agnostic. Q5-A..Q5-E open. |
| 005 | Initial profile stubs (sain-01 + old-workstation) | review | PR 6 | Validates schema against real profiles. 2 stubs + 3 mixins + INDEX + validation harness placeholder. Q6-A..Q6-D open. |
| 006 | Debian (or successor) surface audit | review | PR 7 | Whitelabel target inventory. ~50 surfaces / 10 sections / legal floor explicit. Q7-A..Q7-E open. |
| 007 | Whitelabel mechanism | review | PR 8 | Resolves Q-004 (legal scope) at Gate 4; Q-003 may stay open. 7-strategy taxonomy; schema + default whitelabel placeholder. Q8-A..Q8-E open. |
| 008 | TDD harness specification | review | PR 9 | 5-layer pyramid; CI for Layer 1; substantive Layer 3/4/5 added alongside script bodies at Stage 2+. Q9-A..Q9-E open. |
| 009 | TDD harness bootstrap | accepted | PR 10 | Layer 1 schema + lint pytest + shellcheck CI shipped; chroot/nspawn/qemu scaffolds present; substantive Layer 3+ at Stage 2+. |
| 010 | Stage-2 stub | scoping | post-Gate-5 | Build pipeline + hooks + render engine + sovereign-osctl already on main. Subsequent Stage-2 rounds deferred items documented. |
| 011 | Inference backend stack | review | post-Gate-5 | Q-017 resolution path: direct-stack architecture (vLLM + bitnet.cpp + llama.cpp, no unifying abstraction); scripts/inference/ scaffold shipped (router + 3 backends + 3 start scripts). Q11-A..Q11-E open. |
| 012 | Brand identity placeholder strategy | review | post-Gate-5 | Q-003 deferred-with-criteria: placeholder contract specified, legal-floor contract restated, promotion criteria + mechanism for when a real brand lands. CI gates placeholder-leak detection. |
| 013 | Installer experience | review | post-Gate-5 | Q-008 resolution: image-only (mkosi-built bootable image) + cloud-init/preseed pre-supplied answers. No d-i / Calamares / custom TUI. CI gate `test_install_configs.sh` (24 assertions). |
| 014 | Decommission testing scope | review | post-Gate-5 | Q-014 resolution: test gates (require_root + SOVEREIGN_OS_CONFIRM_DESTROY env + interactive confirm + idempotency) not destruction. CI gate `test_decommission_gates.sh` (12 assertions). |
| 015 | Secure-boot posture | review | post-Gate-5 | Q-006 resolution: 3-level posture (none/shim/signed) declared per profile; operator-supplied keys (never in-repo); preflight-tpm + 08-image-sign as the only gates. TPM2 PCR binding partial (PCR-7 + PCR-11 prescribed; disk-encryption binding deferred). Q15-A..Q15-C open. |
| 016 | Observability bindings | review | post-Gate-5 | Q-013 resolution: 3-layer stack (A: JSONL logs, B: Prometheus textfile collector contract, C: sovereign-osctl + Grafana JSON templates). Layer A shipped + gated; Layer B contract locked + emission deferred; Layer C CLI gated. Local-default sovereignty; no black-box dispatchers. Q16-A..Q16-D open. |
| 017 | ZFS root layout | review | post-Gate-5 | Q-005 resolution: `tank` single pool, raid0 across dual NVMe-PCIe-5, three tiered datasets (`models` 1M/lz4, `context` 16k/zstd-9/copies=2/sync=always, `agents` 128k/zstd-3). Mount base `/mnt/vault`. Operator-acknowledged no device-level redundancy; logical redundancy via copies=2 on irreducible state-fabric. Only sain-01 uses zfs-tiered; ext4 profiles SKIP cleanly via gates. |
| 018 | Kernel choice + tuning | review | post-Gate-5 | Q-007 resolution: dual strategy. sain-01 = kernel.org-stable ≥6.12 custom-tuned (-march=znver5, AVX-512, ZFS/VFIO/BPF-LSM built in). old-workstation + minimal = substrate-default (Debian linux-image-amd64). Build pipeline steps 02-04 only run for custom-tuned profiles. Q18-A..Q18-C open. |
| 019 | Reproducibility target | review | post-Gate-5 | Q-015 resolution: strong build-reproducibility (same inputs → same bytes for mkosi image + kernel .deb + whitelabel render + substrate emit, given pinned SOURCE_DATE_EPOCH + apt snapshot + kernel tag + compiler version); NOT bit-identical for operator-key-signed artifacts. Implementation gaps tracked (apt-snapshot enforcement, SOURCE_DATE_EPOCH in step 04, in-toto build provenance — Stage 2+). |
| 020 | CI infrastructure | review | post-Gate-5 | Q-010 resolution: GitHub Actions only for foundation phase. ~19 L3 + Layer 1 + Layer 2 + shellcheck on ubuntu-latest. No self-hosted, no CI-resident keys (SDD-015). Layer 4 (QEMU) + Layer 5 (hardware) deferred to operator-driven runs. Q10-A..Q10-C open. |
| 021 | Distro-base | review | post-Gate-5 | Q-016 resolution: Debian 13 (trixie) as the foundation-phase distro-base. SDD-003 implicitly chose this; SDD-021 makes it explicit + specifies the criteria for reconsideration (operator-driven). "Debian as Ark" framing honored — we depart from Debian visually + via Tetragon perimeter + whitelabel + custom kernel for sain-01, but the Ark stays Debian. |
| 022 | Disk encryption posture | review | post-Gate-5 | SDD-015 Q15-B resolution: ZFS native encryption for zfs-tiered profiles (sain-01 — tank/context + tank/agents; NOT tank/models which is HF-reconstructible); LUKS2 for ext4 profiles; passphrase + TPM2 PCR-7+11 binding default for sain-01 + headless; passphrase-only for developer + old-workstation; operator opt-out per profile. Orthogonal to secure_boot posture. Q22-A..Q22-C open. |
| 023 | Operator-derived alerts contract | review | post-Gate-5 | Round 89-90 codification: 6-rule in-tree engine over Layer B .prom files; two levels (ALERT/WARN); JSON + human surfaces via `sovereign-osctl alerts`; hourly meta-counters via `alerts-check.sh` recurrent hook. No Alertmanager / no rules.yml / no third-party SaaS — sovereignty-preserving. Q23-A..Q23-D open. |
| 024 | role-server + role-workstation hardening posture | review | post-Gate-5 | Rounds 96-104 codification: 5 server drop-ins (auditd/fail2ban/unattended/sshd-server/pwquality) + 4 workstation drop-ins (3 shared + workstation-tuned sshd; no fail2ban). Deployed by apply-server-hardening (headless) and apply-workstation-hardening (sain-01 + old-workstation) hooks. Load-bearing invariants pinned at Layer 1 lint (silent weakening fails CI); DEST_PREFIX-aware for chroot/image-build flows; operator override path via lexicographically-later drop-ins; `sshd -t` validation gate prevents lock-out. Q24-A..Q24-B open (Q24-C RESOLVED at R112; Q24-D RESOLVED at R101). |
| 025 | Observability CLI architecture | review | post-Gate-5 | Rounds 88-91 + 107 codification: 4 parallel `sovereign-osctl` verbs (`metrics`/`alerts`/`journal`/`history`) sharing dir resolution + bare-name `show` + `list`/`tail` patterns + exit codes 0/1/2/3 + `--json` empty-state contract (`[]`, never null). 2 raw + 2 derived; 2 Layer-A + 2 Layer-B. Future observability verbs follow the same contract; non-observability verbs (decommission/inference) deliberately don't. Q25-A..Q25-D open. |
| 026 | DFlash speculative-decoding integration | review | post-Gate-5 | Round 157 codification: master spec Block 7 (operator-added DFlash topic — arXiv:2602.06036, github.com/z-lab/dflash) materialized as `scripts/inference/dflash-wrap.sh` task-type-gated wrapper. Gating policy (operator-verbatim): code+math enabled (3× speedup); conversational+creative disabled ("does not work on creative tasks in general"). DFLASH_{ENABLE,DISABLE}_OVERRIDE env knobs; DISABLE wins when both set. Per-backend argv shaping for vllm/llama_cpp/transformers. Layer B metrics surface decision per request. Out-of-scope: empirical Layer 5 speedup benchmarking + router-by-task_type signal (R157 follow-up, CLOSED in R161 — router classify_task_type + X-Sovereign-Task-Type header). |
| 027 | Pulse algorithmic foundation — ternary + AVX-512 | review | post-Gate-5 | Round 164 codification: master spec § 15 (ternary `{-1, 0, +1}` weights, multiplication elimination) + § 16 (single-cycle AVX-512 ZMM on Zen 5, VPDPBUSD/VNNI fast path, 64×INT8 per ZMM register, bit-plane transposition). Reference Python module `scripts/pulse/lib/ternary_lut.py` lets operators step through pack_ternary / unpack_ternary / bit_plane_transpose / accumulate / accumulate_from_planes in a REPL. NOT a production kernel — bitnet.cpp + AVX-512 native code is the hot path (built by scripts/pulse/build-bitnet.sh per R152). Layer 2 unit tests (tests/unit/test_ternary_lut.py — 26 cases) lock in round-trip + naive-equivalence + property-based random correctness across all three execution paths. |
| 028 | Canonical-YAML source + auto-rendered consumer surfaces | review | post-Gate-5 | R202+R203 codification: single canonical YAML consumed by multiple parallel surfaces (shell inventory, shell dry-run executor, markdown operator doc, schema lint, freshness gate). Reference impl: `config/bootstrap/phases.yaml` + `scripts/bootstrap/lib/{load-phases,render-phases-md}.py` + `scripts/bootstrap/{phases,run}.sh` + `tests/lint/test_bootstrap_phases_yaml.py` + `tests/nspawn/test_bootstrap_docs.sh`. Drift between rendered artifacts and the canonical table is structurally impossible — every consumer re-parses on invocation. Pattern criteria + future candidate migrations enumerated. |
| 029 | Hardware-stack consolidation (Z-14 / Z-17 / Z-18 / Z-19) | review | post-Gate-5 | R239 (kernel-tuning) + R251 (bios-info + ASUS X870E-CREATOR WIFI advisories) + R252 (power-status PSU/UPS/budget) + R253 (graceful-shutdown guard) + R255 (virt-info CPU+KVM+IOMMU+PCIe+runtimes). Per-vector probe contract (JSON-shape stable + graceful degradation), operator-supplied config (PSU + kernel-tuning .toml), board-specific advisory table (X870E-CREATOR WIFI seed), graceful-shutdown triple-gate (verdict + arm + dry-run), dashboard surface contract per Z-1, cross-vector signal composition. Future R257-R262 roadmap: XMP detection, real-time wattage sampler, PSU OC toggle, KNOWN_BOARDS TOML, Z-14/Z-19 dashboard cards, schedule-manifest framework. |
| 030 | Operator-overlay doctrine (E5.M11 / R283) | review | post-Gate-5 | Shared `scripts/lib/operator_overlay.py` helper for layering operator TOML overlays on every consumer script. Doctrine: 5-tier path-resolution precedence (explicit arg → env → /etc → in-repo example → defaults); deep-merge operator-key-wins; lists replace (not concat); audit metadata (_source, _overlay_keys, _parse_error); malformed TOML never crashes the script. L1 lint guard pins the API contract. Adoption checklist + future-adoption candidates enumerated. Addresses §1a "endless flexibility and fine-tuning and adapting possible" by making every script operator-tunable without per-script flag explosion. |
| 031 | Cross-repo MCP-tool aggregator (E7.M5 / R286) | review | post-Gate-5 | `sovereign-osctl mcp-aggregate manifest` emits a unified MCP-tool catalog spanning 31 sovereign-os read-only tools (hardware / gpu / cpu / psu / network / kernel / pcie / dashboard / health / etc — every §1b-named axis) PLUS an optional `--upstream-selfdef host:port` selfdef MCP TCP descriptor (SD-R94). Closes Q-019 (referenced in SDD-002 §Q-D) without sovereign-os running its own MCP listener — the manifest is the deliverable; clients wire both endpoints natively. Operator overlay (SDD-030 adoption): `extra_tools` / `exclude_tools` knobs via `/etc/sovereign-os/mcp-aggregate.toml`. 9-assertion L3 test (`tests/nspawn/test_mcp_aggregate.sh`) pins cross-axis coverage (rename-breaks-test forcing function). Mutating tools deliberately excluded — defer to triple-gate follow-on. |
| 032 | Helper-library doctrine (E9.M13 / R330) | review | post-Gate-5 | Three-module `scripts/lib/` public API contract: `operator_overlay` (R283) read-side overlay loading; `apply_audit` (R327) audit-write primitive + query; `safe_apply` (R328) triple-gate + maintenance-window + audit-write apply ceremony. NEVER-raise contract uniform across all three. Import convention (`sys.path.insert(0, REPO_ROOT/scripts/lib)`) + 11-assertion L1 lint pin public API surface so future refactors cannot silently break consumer scripts spread across hardware/diagnostics/network/lifecycle/intelligence dirs. |
| 033 | Perpetual-intake doctrine (E9.M10 / R326 + R331-rename) | review | post-Gate-5 | Formalizes the E9.M3 intake pattern (§1b verbatim → TODO row → operator-runnable verb → L3-tested → ✓) that's produced 50+ rounds. 8 mandatory sections + L1 lint pinning section order + 7-item acceptance-criteria checkbox markdown + composition pattern examples (probe → advisor → rollup → meta; triple-gate apply). Originally numbered SDD-031 in R326; collided with pre-existing 031-mcp-aggregate.md; R331 renamed file + updated lint + INDEX in same commit. |
| 034 | Snapshot doctrine (E9.M15 / R336) | review | post-Gate-5 | 6-script snapshot family contract: R322 state-snapshot + R324 fleet-aggregator + R332 config-snapshot + R333 config-restore + R334 state-snapshot-diff + R335 config-snapshot-diff. Schema invariants (schema_version + round + sdd_vector fields), producer-consumer arrows, NEVER-raise contract, round-mismatch enforcement, semver schema-version evolution. L1 lint pins each script's SCHEMA_VERSION + ROUND + SDD_VECTOR constants AND verifies consumers carry round-mismatch handling. |
| 035 | Workload-mode adoption doctrine (E9.M16 / R343) | review | post-Gate-5 | Cross-advisor R338 workload-mode adoption contract proven across 4 distinct advisor shapes (R339 R337 fan-advisor curves; R340 R307 cpu-hotswap gov+EPP tuples; R341 R296 thermal-oc-budget margin deltas; R342 R304 memory-pressure-damper threshold+step deltas). Each adopter MUST: declare `_read_canonical_mode` + (`_apply_mode_modulation` or derive equivalent); carry `follow_workload_mode_coordinator` + `workload_mode_overlay_path` knobs; expose `WORKLOAD_MODE_TO_<SHAPE>` map; emit `workload_mode_canonical` + `workload_mode_source` JSON fields; use `"R338-canonical"` source tag. 12-assertion L1 lint pins contract; auto-picks-up future adopters. NEVER-raise inherited from SDD-032. R344 added R315 xmp-oc-room-advisor (5th adopter); R345 added R293 power-profiles (6th); 6/6 known adopters now ✓. |
| 036 | Inference-service hardening doctrine (E2.M8 / R346) | review | post-Gate-5 | Stricter systemd posture for the 4 inference daemons (pulse / logic-engine / oracle-core / router) on top of R171's 10-key baseline: requires `MemoryDenyWriteExecute` AND `RestrictAddressFamilies` (enumerated AF list). Inline-comment waiver pattern for codegen-needing services (`=false  # vLLM/Triton JIT-compile…`) matches existing `RestrictNamespaces=false  # podman` convention. L1 lint pins `INFERENCE_SERVICES` set + per-service hardening (auto-fails if a 5th inference backend lands without the stricter posture). Flips E2.M8 partial → ✓. |
| 037 | Verbatim-preservation doctrine (E10.M11 / R367) | review | post-Gate-5 | Formalizes the R355-R366 pattern (12 rounds covering 20+ master spec sections + dump-tail + macro-arc plan refinements): operator-verbatim content → discoverable verb (architecture-qa / coverage-map / state-fabric / etc) + L3 phrase assertions enforcing operator-EXACT text unchanged at push-time. 7-mandatory contract sections + typo-preservation rule + bidirectional-consistency pattern + implementation-deviation-documentation. 12-assertion L1 lint pins SDD-037 sections + architecture-qa catalog floors (≥4 Q-NN + ≥3 G-NN + ≥10 C-NN) + coverage-map floor (≥30 A-NN) + spec_ref-non-empty + every-axis-has-verb invariant + status-values-valid + Tetragon-4-binary-bidirectional-consistency + C-NN/A-NN ID monotonic patterns. Catches future regressions: silent paraphrase / silent compression / silent conflation. |
| 039 | Operator §1g 8-surface delivery contract | review | post-Gate-5 | R548 codification of the R453-R547 lattice. Operator-verbatim 8-surface vocabulary (core / cli / tui / api / mcp / dashboard / webapp / service); MODULE_COVERAGE-driven audit; structural-vs-FUTURE waiver precision (R478); 4-instrument compliance rollup (R458 — surface-map / doc-coverage / anti-min / ux-design-audit); historic R539 ceiling-closure milestone (twelfth §1g module at structural ceiling, rotation pool exhausted); R540-R547 surface-promotion lattice (milestone + selfdef + gaps verbs lifted to MCP, dashboard verb-row symmetry closed). Three way-forward vectors: quality-of-existing-surfaces (not new ones, per R478+R539), cross-repo binding completeness (SDD-038), defense-in-depth (surface-gap anti-min pattern catches silent omissions). Q-039-A/B/C deferred — operator decisions. |
| 038 | Cross-repo binding doctrine (sovereign-os ↔ selfdef / R470) | review | post-Gate-5 | Formalizes the R460-R469 dual-repo implementation arc: typed TOML manifests on the selfdef side (one Rust crate per operator-named taxonomy) consumed by sovereign-os instruments via dedicated `selfdef` verbs. Drift-detection sentinel pattern: every cross-repo crate exports a `const` array (e.g., `AUTH_TIERS`, `SURFACE_TAXONOMY`, `UX_DIMENSIONS`, `PATTERN_IDS`) with unit tests asserting verbatim-order match against sovereign-os source-of-truth; drift fails tests on BOTH sides. 7 currently-bound taxonomies (SD-R-DASHBOARD-MANIFEST-1 / SD-R-EVENT-LOG-1 / SD-R-AUTH-TIER-1 / SD-R-MULTI-SURFACE-AUDIT-1 / SD-R-UX-CHECKLIST-1 / SD-R-AUDIT-1 / SD-R-BASHRC-1). End-to-end acceptance test (R469) synthesizes complete selfdef deployment fixture + runs `compliance status` + asserts all 5 binding paths simultaneously + recovery + defense-in-depth schema rejection. Operator-DX rollups (R458 compliance status / R468 bashrc combo) reduce action-budget from N commands to 1, embodying §1h "high UX/DX". |
| 040 | Cockpit dashboard implementation bridge (M060 catalog → webapp pages) | active | post-Gate-5 | Bridges the M060 cockpit-dashboards milestone (21 catalogued dashboards D-00..D-20) to their single-file sovereignty-clean `/webapp/` implementations: maps each catalogued dashboard to its webapp page where one exists, identifies coverage gaps, orders the implementation backlog. Does NOT redesign — materializes the M060 catalog into ordered implementation work. Sister to the §1g Dashboard surface (E11.M2 master-dashboard / E11.M3 multi-surface). |
| 041 | Action-surface alert runbook coverage (close 64 dead runbook links) | active | post-Gate-5 | The 11 selfdef responder action-surface alert families (apparmor-profile-pivots / bpf-map-element-clears / capability-drops / env-scrubs / kernel-keyring-evictions / mfa-grant-revocations / mount-bindings / netns-isolations / process-tree-freezes / socket-fd-revocations / token-revocations) shipped 64 Prometheus alerts whose `runbook_url` anchors ALL pointed at non-existent deployment-guide sections — a paging hazard where a critical page landed on nothing. Authors 64 grounded runbook sections (Meaning from the alert's own description + Diagnosis referencing the real gauge metric / textfile units / `.prom` file + category-specific Fix) into `docs/operator/m060-deployment-guide.md`, and adds `tests/lint/test_action_surface_alert_runbook_coverage.py` locking rules↔runbook anchors in lockstep. Consumer-side operability half of selfdef's producer responder surfaces. |
| 042 | Management-path reconfig detector (the dropout gotcha's missing witness) | active | post-Gate-5 | SDD-first decomposition of M084 R14124–R14126: the dump-762 chain's EFFECT side is instrumented (guardian EOF sentinel + `sovereign_os_auditor_stream_eof_total` + `SovereignOsAuditorStreamEofChurn`), but the CAUSE side — management-NIC re-shuffles / lease drops — has no metric witness, leaving R14126 correlation manual. Specs a recurrent journald-cursor watcher (`net-reconfig-watch.sh`, sibling of the telemetry textfile hook; profile-resolved mgmt NIC, no hardcoded iface) emitting `sovereign_os_net_reconfig_events_total{kind=carrier-loss\|lease-drop\|addr-change}` + freshness gauge; diagnosis-tier by default (no second pager — the churn alert's runbook gains a one-query correlation). 3 open Q-rows (cadence / cause-side alert / networkd-restart classification). |
| 043 | Flexible tiered feature+model exploitation | review | post-Gate-5 | Makes CPU-feature exploitation AND model-tier assignment both per-profile, flexible, and composable, so 20+ `OS × GPU1 × GPU2` combos can mix ternary/RLM/TinyLM/LLM across a ≥3-tier runtime, each tier exploiting exactly the CPU/GPU ISA the declared hardware has. Grounds the existing-but-static substrate (runtime-profile schema, model catalog, `sovereign-cpu-dispatch`/`vnni`/`bitops`, router) into a phased plan: (1) profile `hardware.cpu.features`→KCFLAGS + crate feature-flags (VNNI/BF16/popcnt); (2) flexible tiers + VRAM-aware model/quantization selection via `tier_intent`; (3) profile generation from hardware+strategy; (4) MoE/MoTE + Validator + CoT "thinking" router. rowhammer/memory-integrity tracked as cross-cut. 5 open Q-rows. |
| 044 | Unified dashboard surface | review | post-Gate-5 | Closes the operator dashboard-gap: /master-dashboard + /panels showed "almost nothing" (undescribed flat list; 34 of 38 panel APIs unstarted; whole SDD-043 feature domains — Models, AVX-512 choice, tier_intent, generation, thinking router — plus selfdef with NO panel at all). Phase 1 SHIPPED: config/dashboard-catalog.yaml (42 described+categorized entries = 37 panels + 5 un-paneled domains with CLI access), the /panels index renders it as a categorized GLOBAL VIEW (+/catalog.json), make panel starts every scripts/operator/*-api.py, test_dashboard_catalog_complete locks completeness+descriptions. Phased plan for the net-new dashboards (Models browser / AVX matrix / Orchestration view / Runtime-Profile wizard / selfdef panel) + descriptions-in-meta lockstep. 4 open Q-rows. **Superseded on the "way forward" by SDD-045.** |
| 045 | Dashboard control-surface buildout (every dashboard a full control surface) | review | post-Gate-5 | Answers the operator's un-minimized directive "ALL DASHBOARDS ALMOST SHOULD HAVE FEATURES OPTIONS AND PROFILES … everything can be turned on and off … tons of modes and profiles … WHERE IS EVERYTHING". Defines the **Universal Dashboard Contract**: every one of 43 dashboards (38 shipped + 5 net-new) gains a 5-region skeleton — real M060 **description** (in 3 places: master-dashboard list + /panels + panel `<meta>`, lint-locked identical), a **Features** rail (every capability turnable on/off / actionable, copying the exact operator command), an **Options** rail (filters/thresholds/settings), and a **Profiles/Modes** selector. Maps the 11 real on/off+mode systems (OS profiles / runtime profiles §18 / flex-profiles / cpu-mode / gpu-mode / dashboard-toggles / auth-tiers / selfdef / perimeter / inference tiers / workload-knobs) into a shared control-systems registry, gives per-dashboard Features/Options/Profiles for ALL 38, specs the 5 net-new panels (models-catalog / cpu-avx-choice / orchestration / profile-generation / selfdef-management), and proves ~1180 features / 85 verb families / 709 crates all reach a dashboard (`test_feature_coverage.py` gate). 7-phase (A–G) roadmap; shared `webapp/_shared/control-surface.{js,css}` component. 4 open Q-rows. |
| 046 | root-ghostproxy endpoint binding (proxy mode disabled) | draft | post-Gate-5 | Activates the fourth repo from dormant to consumed, per the operator directive 2026-07-03 (verbatim): "Lets prepare root-ghostproxy for sovereign-os usage, we will use use the repo without the proxy mode enabled." Binds root-ghostproxy's endpoint AI-agent safety envelope (machine-level Claude Code + opencode hooks, agent brain, integrity sentinel) into the SAIN-01 lifecycle via two triple-gated hooks — `root-ghostproxy-endpoint-install` (post_install_first_boot; report-only until `SOVEREIGN_OS_CONFIRM_GHOSTPROXY_INSTALL=YES`) + `root-ghostproxy-verify` (weekly recurrent read-only `--check` drift witness) — both pinning `--mode endpoint` (never auto: SAIN-01's dual NICs would auto-promote to bridge and silently re-enable the proxy half). Consumes the sister repo through its own `install.sh` per SDD-001 authority (never forks the envelope); upstream contract locked by root-ghostproxy PR #3's 10/10 endpoint-mode regression test. Layer B metrics `sovereign_os_ghostproxy_endpoint_*`; lint gate `test_root_ghostproxy_binding_contract.py` (13 assertions). 4 open Q-rows (mandatory-flip after real run / image-bake / typed-manifest ninth compliance axis / which-user-$HOME). |
| 047 | Cockpit functional execution (realize R10274) | draft | post-Gate-5 | Execution layer atop SDD-045's control surface, per operator directive 2026-07-08 (verbatim): "we now have a sudoer strategy, we will fix everything that is a manual command so that the manual command is only the alternative but we will otherwise do the features functional from the panels / dashboard ... This is massive evolution." Makes panels EXECUTE controls instead of only copying the command (fallback), realizing R10274 (MS003-signed mutation proxy) while preserving R10212 (web never *arbitrarily* mutates; selfdef-owned selfdef/perimeter stay a signed proxy — only the 9 sovereign-os-owned controls execute locally). Phase 0 SHIPPED (`059f076c`): `scripts/operator/_action_exec.py` (registry-driven allowlist + selfdef-boundary hard-reject + placeholder validation + privileged key/confirm gate + `sudo -n` mechanism-isolated + OCSF-5001 audit, DRY_RUN-default, no live surface touched) + DRAFT `config/sudoers.d/sovereign-os-cockpit` (visudo-clean NOPASSWD allowlist, 9 owned verbs) + `tests/unit/test_action_exec.py` (23) + `tests/lint/test_cockpit_action_exec_sudoers.py` (4 drift-guards). Phases 1-3 (control-surface execute → ~175 per-panel buttons → invert ~48 contracts) gated on 4 open Q-rows (sudoer mechanism A/B/C/D / selfdef boundary / systemd NoNewPrivileges trade-off / branch landing strategy). |
| 048 | Approval authority (functional approve/deny/defer for D-06) | draft | post-Gate-5 | Write-path atop the D-06 read model, per operator directive 2026-07-08 (chose "Approval authority (d-06)" as the next greenfield engine after SDD-047's 5 feasible controls merged in PR #25). Makes the D-06 approve/deny/defer buttons functional via the SDD-047 R10274 `control-exec-api` rail: a new `approvals-decide` control (`sovereign-osctl approvals {approve|deny|defer} <id> --confirm`, the 18th control, privileged) that transitions the M065 SG1-SG5 gate state + records a durable decision. Read path is fully built (`approval-queue.py` reader, schema, M065 gate spec, operator-key presence gate, the reusable OCSF-5001 audit writer that auto-surfaces in D-05/D-16); the write path is greenfield — this ships the decision-writer (`scripts/lifecycle/approval-decide.py`, keeping the read core pristine), a minimal CLI `approvals request` producer, and the durable append-only `/var/log/sovereign-os/approval-decisions.jsonl` ledger. R10212 preserved (selfdef/perimeter untouched, `state_path` free of selfdef/tetragon, `approvals-api.py` stays 405). MS003 signing is presence-gated + audited with `signature: "unsigned-pending-MS003"` — real selfdef signing + real producers (cloud gate / stage-gate coordinators) are the Stage-4 follow-up. 5 Q-rows (A/B/C answered; D deferred-badge / E id-format proposed). |
| 049 | Model runtime actuation (functional load/unload/warm for D-03) | draft | post-Gate-5 | The second greenfield engine, per operator directive 2026-07-08 (chose the d-03 model-runtime engine after SDD-048 merged in PR #26). Makes the D-03 model-health load/unload/warm buttons FUNCTIONAL via the SDD-047 R10274 rail. No per-model hot-swap exists — a Trinity tier serves ONE model bound at systemd-unit start via a `*_MODEL` env — so "load model X into role R" = resolve the catalog id → (tier, on-disk path), write a highest-precedence env drop-in `/etc/sovereign-os/inference-<tier>.env`, and restart the tier unit (the `inference {start|stop|restart} <tier>` primitive already exists). Ships `scripts/models/{load,unload,warm}.py` (importing `model-health.py`'s reader helpers, keeping it pure) + 3 controls (`model-load` `sovereign-osctl models load <id> --confirm`, `model-unload` `{logic|oracle}`, `model-warm` `{logic|oracle}` — registry 18→21) + becomes the missing `/run/sovereign-os/model-state.json` producer (atomic). VRAM-fit REFUSE-by-default (`vram_gib_min` vs live free VRAM from `collect_gpus`) with a logged `--force` (Q-049-E). Minimal kv-warm = a `/v1/completions` prime request to the running vLLM server (logic :8082 / oracle :8083, Q-049-F). R10212 preserved (selfdef/perimeter untouched, `state_path` free of selfdef/tetragon, `model-health-api.py` stays 405). id→path resolution NOT string-munged (id → hf_repo_id → verified on-disk dir). 6 Q-rows (E VRAM-refuse / F kv-warm-now answered; A path-derivation / B unload-role-set / C path-absent / D auto-stop-conflict proposed). idle-auto-unload + LRU eviction are Stage 4. | operator-directed 2026-07-08 | SDD-049 on branch `claude/recover-projects-b0oT6` |
| 050 | Snapshot/rollback actuation (functional snapshot create/prune + recent-N rollback for D-08) | draft | post-Gate-5 | The third greenfield engine, per operator directive 2026-07-08 (chose the D-08 rollback-points engine after SDD-049 merged in PR #27). Makes the D-08 snapshot/rollback write buttons FUNCTIONAL via the SDD-047 R10274 rail. The READ side already exists (`sovereign-osctl rollback {snapshot,preview,commits}` → `rollback-points.py`, served at `/api/d-08/snapshot` + `/api/d-08/preview?to=`); the greenfield write is `snapshot create` (`zfs snapshot <ds>@<tag>`) + `snapshot prune` (`zfs destroy` aged snapshots). Ships 3 controls (`snapshot-create` `sovereign-osctl rollback create --dataset {os\|context\|models\|agents} --tag <tag> --confirm` — dataset is an enum of short keys resolved to the real `/`-bearing path internally, the SDD-049 pattern; `snapshot-prune` `--retain-days {14\|7\|30\|90} --confirm` — floor + refuse-by-default, `--force` CLI-only logged override; `rollback-recent` `--to {recent-1..recent-5} --confirm` — bounded positional-token enum resolved to the live `@`-name internally, making arbitrary-snapshot rollback web-functional without a `/`-bearing arg — registry 21→24, local 19→22). `rollback-apply` (latest-only one-click) is left UNTOUCHED (its `--to latest --confirm` change_cli is a pinned security contract). R10212 preserved (selfdef/perimeter untouched, `state_path` free of selfdef/tetragon, `rollback-api.py` stays 405 read-only). Dead read buttons re-wired to live `/api/d-08/*` reads (honest surface per "we do not minimize"). 6 Q-rows (C `--force`-CLI / F recent-N-enum answered; A dataset-set / B floor-size / D recent-depth / E diff-verb proposed). Structured `rollback diff` + auto-snapshot-before-commit are Stage 3. | operator-directed 2026-07-08 | SDD-050 on branch `claude/recover-projects-b0oT6` |
| 051 | Adapter promotion authority (functional promote/demote/rollback for D-11) | draft | post-Gate-5 | The fourth greenfield engine + the structural twin of SDD-048, per operator directive 2026-07-08 (chose the D-11 adapter-status engine after SDD-050 merged in PR #28). Makes the D-11 LoRA-adapter promote/demote/rollback buttons FUNCTIONAL via the SDD-047 R10274 rail. Read side is complete (`sovereign-osctl adapters {inventory,list,history}` → `adapter-foundry.py` overlaying the catalog's `class=lora-adapter` entries with the promotion registry `/var/lib/sovereign-os/adapters/registry.json`: per-adapter status + MS041 gates + history; served at `/api/adapters/inventory`); the write side is greenfield. Ships `scripts/inference/adapter-decide.py` (the writer — imports adapter-foundry's reader helpers, keeps it pure; atomic registry write + durable `/var/log/sovereign-os/adapter-decisions.jsonl` ledger + OCSF-5001 span) + a minimal `register` producer + ONE `adapter-decide` control (`sovereign-osctl adapters {promote\|demote\|rollback} <id> --confirm`, privileged — registry 24→25, mirroring SDD-048's single approvals-decide control). Promote is REFUSE-by-default unless the MS041 triple-gate (snapshot + test/eval + oracle-or-human all `passed`, R09697-R09711) is met — NO panel override (Q-051-A). MS003 signing delegated to selfdef (`unsigned-pending-MS003`, Q-051-E). R10212 preserved (selfdef/perimeter untouched, `state_path` free of selfdef/tetragon, `adapters-api.py` stays 405). export-CSV wired client-side; `train` (heavy M046 pipeline) deferred to Stage 3 (Q-051-B). 5 Q-rows (A/B answered; C/D/E proposed). | operator-directed 2026-07-08 | SDD-051 on branch `claude/recover-projects-b0oT6` |
| 052 | Memory-change authority (functional approve/reject sign-off for D-07) | draft | post-Gate-5 | The fifth greenfield engine + the third approvals-shaped twin, per operator directive 2026-07-08 (chose the D-07 memory-changes engine after SDD-051 merged in PR #29). Makes the D-07 memory-changes pending-change approve/reject buttons FUNCTIONAL via the SDD-047 R10274 rail. Read side is complete (`sovereign-osctl memory-changes {snapshot,types,lifecycle}` → `memory-changes.py` projecting the M028 Memory OS state `/run/sovereign-os/memory.json` — 8 type counts + 11-stage lifecycle + graph diffs + the `pending` queue; served at `/api/d-07/snapshot` + `/stream`, POST→405); the write side is greenfield. The `pending` queue is the ONLY id-addressable structure + the DESIGNED sign-off surface (per the core docstring: "pending promote/pin/forget operations awaiting operator sign-off"), so promote/pin are realized by APPROVING a pending change — no speculative per-memory-entry store (SB-077). Ships `scripts/intelligence/memory-decide.py` (the writer — imports memory-changes' reader helpers, keeps it pure; atomic registry write + durable `/var/log/sovereign-os/memory-decisions.jsonl` ledger + OCSF-5001 span) + a minimal `request` producer + ONE `memory-decide` control (`sovereign-osctl memory-changes {approve\|reject} <id> --confirm`, privileged — registry 25→26, mirroring SDD-048/051's single control). Scope = approve/reject + queued promote/pin; approving a pending `forget` is REFUSED (Stage 3, refuse-by-default + `--force` CLI, Q-052-A/B). MS003 signing delegated to selfdef (`unsigned-pending-MS003`, Q-052-E). R10212 preserved (selfdef/perimeter untouched, `state_path` free of selfdef/tetragon, `memory-changes-api.py` stays 405). Direct memory-id ops + forget + undo + snapshot deferred to Stage 3. 5 Q-rows (A/B answered; C/D/E proposed). | operator-directed 2026-07-08 | SDD-052 on branch `claude/recover-projects-b0oT6` |
| 053 | Session lifecycle authority (functional hibernate/resume/kill for D-01) | draft | post-Gate-5 | The sixth greenfield engine + the fourth registry-transition twin, per operator directive 2026-07-08 (chose the D-01 active-sessions engine after SDD-052 merged in PR #30). Makes the D-01 hibernate/resume/kill + hibernate-all buttons FUNCTIONAL via the SDD-047 R10274 rail. Read side is complete (`sovereign-osctl sessions {active,summary,steps}` → `session-registry.py` projecting the M057 session registry `/run/sovereign-os/sessions.json` — typed session entries with a 9-state M057 machine; served at `/api/sessions/active`, POST→405); the write side is greenfield. Sessions are typed registry entries (NOT raw PIDs), so hibernate/resume/kill transition a session's `state` field; the real M047 CRIU + ZFS checkpoint/restore is the M057 engine's job (Stage 4, deferred like memory-decide deferred the M028 effect). Ships `scripts/lifecycle/session-decide.py` (the writer — imports session-registry's reader helpers, keeps it pure; atomic write + durable `/var/log/sovereign-os/session-decisions.jsonl` ledger + OCSF-5001 span) + 2 controls (`session-decide` `sovereign-osctl sessions {hibernate\|resume\|kill} <id> --confirm` + `session-hibernate-all` `sovereign-osctl sessions hibernate-all --confirm` — registry 26→28, local 24→26). State guards: hibernate←active, resume←hibernated, kill non-terminal→archived (Q-053-A) refused on terminals; hibernate-all bulk-transitions every active→hibernated (Q-053-B). MS003 signing delegated to selfdef (`unsigned-pending-MS003`, Q-053-E). R10212 preserved (selfdef/perimeter untouched, `state_path` free of selfdef/tetragon, `sessions-api.py` stays 405). 5 Q-rows (A/B answered; C/D/E proposed). Real CRIU+ZFS effect + session producer + real signing deferred to Stage 3. | operator-directed 2026-07-08 | SDD-053 on branch `claude/recover-projects-b0oT6` |
| 054 | Eval-history wiring (functional run-suite/promote/export for D-10) | draft | post-Gate-5 | A WIRING pass (not a greenfield engine — no new writer/control), per operator directive 2026-07-08 (chose the D-10 eval-history panel after SDD-053 merged in PR #31). Completes D-10's functional execution by reusing controls that ALREADY exist: `run-suite-btn` was already functional (jumps to the `eval-run` control, SDD-047 PR #25); the per-adapter `promote` button now routes to the `adapter-decide` control (SDD-051 PR #29), whose `applies_to` is extended `[d-11-adapter-status, d-10-eval-history]` so it renders in D-10's rail (eval-history shows adapters pending promotion with eval gains — a natural promotion surface); `export-btn` becomes a real client-side CSV from the live `/api/evals/summary`. NO new control (registry stays 28), NO new writer, NO count bumps — only the adapter-decide render-target widens + the d-10 webapp re-wire (shared jumpToControl helper; fake `sovereign*` clipboard strings removed). R10212 unchanged (no new mutation path; reuses the already-gated eval-run + adapter-decide controls; evals-api.py stays 405). 3 Q-rows (A/B answered; C proposed). Batched multi-benchmark eval-suite runner + inline per-row adapter ops deferred to Stage 2. | operator-directed 2026-07-08 | SDD-054 on branch `claude/recover-projects-b0oT6` |
| 055 | LM orchestration wiring (functional apply/load/toggle/override/eval for D-21+D-22) | draft | post-Gate-5 | A WIRING pass (not a greenfield engine — no new writer/control), per operator directive 2026-07-08 (chose the D-21/D-22 LM-orchestration panels after SDD-054 merged in PR #32). Completes the D-21 (LM orchestration) + D-22 (LM status & operability) panels by reusing controls that ALREADY exist. NEW `docs/sdd/055-lm-orchestration-wiring.md`. Both panels already carry the full inline control-surface + reuse D-03's model-health grid. Button map: D-21 `apply` → `runtime-mode` (trinity profile switch); D-22 `load`/`toggle` → `inference-tier` (inference start/restart), `override` → `runtime-mode`, `eval`/`bench` → `eval-run`; `chat-send` kept honest-deferred (no inference-chat CLI — M058 producer pending; copies a real `inference status` verb). Extends 3 controls' `applies_to` (runtime-mode += d-21+d-22; inference-tier += d-21+d-22; eval-run += d-22) so they render in these panels' rails. NO new control (registry stays 28), NO new writer, NO count bumps — only render-targets widen + the d-21/d-22 webapp re-wire (shared jumpToControl helper; fake `sovereign*` clipboard strings removed). R10212 unchanged (no new mutation path; reuses already-gated controls; LM read APIs stay read-only). 3 Q-rows (A/B answered; C proposed). M058 chat inference producer + device-scoped bench deferred to Stage 2. | operator-directed 2026-07-08 | SDD-055 on branch `claude/recover-projects-b0oT6` |
| 056 | Hardware-pressure wiring (functional zpool-scrub for D-09) | draft | post-Gate-5 | A WIRING pass (not a greenfield engine), per operator directive 2026-07-08 (chose to finish D-09 after SDD-055 merged in PR #33 — closing the cockpit control-wiring sweep). Makes the D-09 hardware-pressure `zpool scrub` button EXECUTE via the existing `maintenance` control (which already has `applies_to: [d-09-hardware-pressure]` — no config change) instead of clipboard-copying the CLI. NEW `docs/sdd/056-hardware-pressure-wiring.md`. `scrub-btn` → `jumpToControl('maintenance')` (shared helper added); `dcgm-export-btn` kept as an honest read-only telemetry copy (`dcgmi dmon` — a diagnostic, no mutation). NO control change (registry stays 28), NO new writer, NO count bumps — a single webapp re-wire. R10212 unchanged (reuses the already-gated maintenance control; hardware-pressure read API stays read-only). This closes the "make dashboard controls functional" sweep — 9 panels wired across PRs #24–33 (d-06/d-03/d-08/d-11/d-07/d-01/d-10/d-21/d-22) + D-09 here. 2 Q-rows (A answered; B proposed). | operator-directed 2026-07-08 | SDD-056 on branch `claude/recover-projects-b0oT6` |
| 057 | Session save-state (M047 5-layer save-state + CRIU wrapper for D-01) | draft | post-Gate-5 | The first Stage-3 real-runtime backend — the M047 continuity effect behind D-01 hibernate/resume (SDD-053 Stage 3), per operator directive 2026-07-08 (chose M047 after the cockpit control-wiring sweep completed in PR #34). Gives hibernate/resume a real *save-state*: the E0451 FIVE-layer save-state (ZFS snapshot + CRIU checkpoint + replay log + memory record + profile state, per `crates/sovereign-save-state`) + a completeness gate, instead of only the SDD-053 registry `state` transition. NEW `docs/sdd/057-session-save-state.md`. Grounded reality: no `criu` executor + no M057 session-process runtime exist (sessions carry no pid), so a real CRIU checkpoint has no target today. Ships `scripts/lifecycle/save-state.py` — the orchestrator capturing the 4 producible layers via existing engines (zfs-snapshot → rollback-points.create SDD-050; memory-record → memory.json SDD-052 sha256; replay-log → durable ledger; profile-state → active-profile.env) + the CRIU WRAPPER (Q-057-A): `criu dump --tree <pid>` when the session carries a pid (host-gated), else `criu-checkpoint` flagged missing ("no target — pending M057 runtime"). `is_true_save_state` = all 5 → honestly reports PARTIAL (4/5) until a pid-bearing session exists. Manifest (`/var/lib/sovereign-os/save-state/<sid>/<ts>/manifest.json`) + ledger + OCSF-5001 span; restore emits the inverse plan (criu restore + zfs rollback). Wiring: session-registry pid/dataset read-only passthrough; hibernate→capture, resume→restore; osctl `sessions {save-state,restore}` + sudoers. Python↔Rust drift-guard lint keeps `_LAYERS` ≡ the crate's SaveLayer enum. R10212 preserved (selfdef/perimeter untouched, save-state paths free of selfdef/tetragon, session read API stays read-only; zfs+criu are the only host mutations, exec-rail + sudoers gated). NO new control (registry stays 28). 5 Q-rows (A answered; B/C/D/E proposed). The real M057 session-process runtime + the cockpit control + memory/profile restore execution are Stage 4. | operator-directed 2026-07-08 | SDD-057 on branch `claude/recover-projects-b0oT6` |
| 058 | M057 session-process runtime (the session producer that makes CRIU real) | draft | post-Gate-5 | SDD-057 Stage 4 — the producer that turns the 4/5 partial save-state into a true 5/5, per operator directive 2026-07-08 (chose the M057 runtime as the highest-leverage continuation after SDD-057 merged in PR #35). No producer wrote `sessions.json` + no process-spawning runtime existed, so SDD-057's save-state always flagged `criu-checkpoint` missing (no target pid). NEW `docs/sdd/058-session-process-runtime.md`. Ships `scripts/lifecycle/session-runtime.py` — the TASK-COMMAND producer (operator Q-058-A): `sessions start -- <cmd>` spawns the operator's real command under a transient `systemd-run --scope` (a real cgroup — the CRIU target, Q-058-B), captures the MainPID, and REGISTERS the session `{id:sess-<8hex>, state:active, step:1, pid, cgroup, dataset, started_at, task}` in `sessions.json` atomically (the missing producer). `stop` → `systemctl stop <scope>` + state→archived. SECURITY (R10212 + arbitrary-exec): `sessions start` is CLI-ONLY + DELIBERATELY NOT a cockpit control (no control-systems entry, no control-exec-api wiring, no cockpit sudoers) — the web can never reach arbitrary command exec; the task argv is a LIST (no shell). PAYOFF: a runtime pid-session → `save-state.capture` now captures the CRIU layer → true 5/5 `is_true_save_state` (proven end-to-end in `test_session_runtime.py`). DRY-RUN-default + host-gated `systemd-run`. NO new control (registry stays 28); osctl `sessions {start,stop,list}` CLI dispatch only. R10212 preserved. 5 Q-rows (A/B answered; C/D/E proposed). The full m009 12-step lifecycle orchestration + per-session ZFS datasets + a reaper + resume-into-checkpoint are Stage N. | operator-directed 2026-07-08 | SDD-058 on branch `claude/recover-projects-b0oT6` |
| 059 | M028 memory forget/undo (the memory-entry store + soft-delete + undo for D-07) | draft | post-Gate-5 | SDD-052 Stage 3 — the deferred destructive/reversal memory ops, per operator directive 2026-07-08 (chose M028 memory forget/undo after SDD-058 merged in PR #36). `forget` (R10184) + `undo` (R10185) were refused/deferred in SDD-052 because the M028 projection (`/run/sovereign-os/memory.json`) is aggregate-only — no addressable memory-entry store, no change-ledger. NEW `docs/sdd/059-memory-forget-undo.md`. Ships `scripts/intelligence/memory-store.py` — the M028 store (`/var/lib/sovereign-os/memory/store.json`, keyed by `mem-<hex>`) + a reversible change-ledger (`changes.json`) + a `register` producer + the ops: `forget` REFUSE-by-default (`--force` CLI-only, Q-052-B/Q-059-B) SOFT-DELETES (tombstone `state:forgotten` + ledger the prior state — never a hard remove, Q-059-A) so `undo` (R10185) can always restore. + 2 controls (`memory-forget` `sovereign-osctl memory-changes forget <id> --confirm` refuse-without-force / `memory-undo` `memory-changes undo <id> --confirm` — registry 28→30, local 26→28) via the `memory-changes)` osctl forget/undo/register sub-verb routing + sudoers. d-07 forget/undo buttons re-wired to the controls. R10212 preserved (selfdef/perimeter untouched, store paths free of selfdef/tetragon, `memory-changes-api.py` stays 405 read-only; the store write is the only host mutation, exec-rail + sudoers gated). Store↔projection decoupled now (real M028 producer reconciles counts — Stage N). 5 Q-rows (A/B answered; C/D/E proposed). The real admission-lifecycle producer + tombstone purge + a d-07 entry-list view are Stage N. | operator-directed 2026-07-08 | SDD-059 on branch `claude/recover-projects-b0oT6` |
| 060 | M028 memory-entry list view + tombstone purge (the D-07 Stage-N follow-up to SDD-059) | draft | post-Gate-5 | SDD-059 Stage N — the deferred memory-entry list view + the retention purge (SDD-059 Q-059-C), per operator directive 2026-07-09 (chose the M028 list-view + purge after SDD-059's forget/undo merged in PR #37). SDD-059 shipped forget/undo but the D-07 panel renders only the aggregate projection — no addressable `mem-<id>` list, so the operator has nothing to `forget` *from*; and `forget` only ever tombstones, so nothing hard-removes. NEW `docs/sdd/060-memory-entry-list-and-purge.md`. LIST: a new read endpoint `GET /api/d-07/entries` on `memory-changes-api.py` (importlib-loads `memory-store.py` for `store_list()` as a SECOND read source — `memory-changes.py` stays a pure projection reader, the API stays 405 on mutations) + a d-07 "M028 memory entries" table (mem-id/type/stage/state/summary/updated) with a per-active-row forget button → `jumpToControl('memory-forget', row.id)` (best-effort prefill of the control card's id input). PURGE: `memory-store.py` `purge(older_than_days=30, --confirm)` hard-removes `state:forgotten` entries past the retention window (30d default, `--older-than N` override) + marks each entry's non-reversed ledger forget-change `purged:true` (the ledger stays append-only — never a deleted audit row); `undo` gains a purged-guard (rejects "purged; cannot restore"). Purge is a CLI-only maintenance verb, DELIBERATELY NOT a cockpit control (Q-060-A): a purge is IRREVERSIBLE (undo cannot restore a hard-removed entry), strictly more dangerous than forget, so it is not web-triggerable at all (matches the `sessions start` CLI-only doctrine) — NO control-systems entry, NO cockpit sudoers, registry stays 30. R10212 preserved (selfdef/perimeter untouched, store paths free of selfdef/tetragon, `memory-changes.py` pure, `memory-changes-api.py` read-only; the purge store/ledger write is DRY-RUN-default + `--confirm` gated). Store↔projection still decoupled (real M028 producer reconciles counts — Stage N). 5 Q-rows (A CLI-only-purge / B 30d-retention / C store_list-source answered; D projection-reconciliation / E purge-scheduler proposed). The real admission-lifecycle producer + a purge scheduler + entry filtering are Stage N. | operator-directed 2026-07-09 | SDD-060 on branch `claude/recover-projects-b0oT6` |
| 061 | M046 adapter gate-producers (advance the MS041 triple-gate from real evidence; make D-11 promote functional end-to-end) | draft | post-Gate-5 | SDD-051 Stage 4 — the deferred real gate-producers (Q-051-E), per operator directive 2026-07-09 (chose the M046 gate-producers after SDD-060 merged in PR #38). `adapter-decide.register()` mints all four MS041 gates `pending` + `decide()` never touches `gates`, so NO producer advances a gate to `passed` — `promote` is unreachable except by a manual registry edit; D-11's readiness pill is stuck "not yet"; D-20 reports "eval harness deferred". NEW `docs/sdd/061-adapter-gate-producers.md`. Ships `scripts/inference/adapter-gate.py` — the gate-producer (separate from `adapter-decide.py` the consumer, mirroring memory-store↔memory-decide), advancing `gates.<gate>=passed` + a `gate_evidence[<gate>]` provenance sub-record ONLY from real evidence (SB-077 — never fabricate): `gate human <id> --confirm` (operator attestation — always producible), `gate snapshot <id> --confirm` (reuse SDD-050 `rollback-points.create` on `models`/tank/models → a real ZFS rollback-point), `gate eval <id> --confirm` (read a real passing record from `evals.jsonl` via the D-10 `eval-tracker` reader — does NOT run the heavy benchmark), `gate oracle <id> --confirm` (probe the oracle vLLM backend + minimal judge; honest-defer when unreachable). Grounded reality: eval-run + oracle are hardware-gated so they honest-defer today; snapshot + eval-record + human make a real promote reachable NOW (register → 3 gates → `_gate_unmet` empty → promote succeeds → pill green). ONE new control `adapter-gate-human` (`sovereign-osctl adapters gate human <id> --confirm`, privileged, `applies_to:[d-11-adapter-status]` — registry 30→31, local 28→29) so the human sign-off is a functional D-11 button (the standing /goal); eval/snapshot/oracle stay CLI-only non-privileged producers (heavy/host-gated). Wired via the `adapters gate)` osctl routing + sudoers (`adapters gate human *` only). d-11 gets a per-pending-row "sign off human gate ↓" button (`jumpToControl('adapter-gate-human', id)` + prefill); d-20's static check-row updated to the honest new state. R10212 PRESERVED — `adapter-foundry.py` pure, `adapters-api.py` read-only (405, NO new API mutation path), `adapter-decide.py` untouched (sole consumer), registry/evidence paths free of selfdef/tetragon; MS003 `unsigned-pending-MS003`. 5 Q-rows (A 4-gate-scope / B human-control / C eval-record-source answered; D oracle-judge / E snapshot-dataset proposed). The M046 training pipeline + full oracle judge + a gate scheduler are Stage N. | operator-directed 2026-07-09 | SDD-061 on branch `claude/recover-projects-b0oT6` |
| 062 | Functional D-22 live chat (the M058 inference producer — real single-prompt inference + a loopback chat proxy + live telemetry) | draft | post-Gate-5 | The M058 inference producer behind D-22's render-only chat composer, per operator directive 2026-07-09 ("the full deal, no minimizing" — chose functional web-chat over the render-only compromise, after SDD-061 merged in PR #39). The composer today only COPIES `inference status` (no single-prompt verb exists; `inference` is status/start/stop/route/health/logs) and nothing publishes live inference telemetry. NEW `docs/sdd/062-functional-lm-chat.md`. Three pieces: (1) `scripts/inference/prompt.py` — the real `inference prompt <text>` engine (POST `{messages,stream:true}` to the LOOPBACK router `/v1/chat/completions`, which proxies+streams the tier's body per `router.py:445-468`; streams token deltas, measures tokens/sec) + `publish_telemetry` (read-modify-write `model-state.json` `tokens_per_sec[role]` + `model-latency.json`, PRESERVING the SDD-049 `loaded` set) — reused by both the CLI verb and the web proxy; (2) `POST /api/lm-status/chat` on the D-22 daemon — a bounded, LOOPBACK-ONLY inference-query proxy → SSE token stream into `#chat-log`; (3) the webapp composer re-wired from clipboard-copy to the live POST+SSE. **R10212 posture (operator-sanctioned relaxation, kept NARROW):** a chat completion is a NON-MUTATING read-compute to a local model — NOT a host/state mutation; the D-22 daemon gains this ONE POST, and ALL state mutations (model load/unload, tier start/stop, adapter promote) stay 405 + exec-rail-only; the `/api/control/execute` rail is unchanged. Distinct from `sessions start` (SDD-058, CLI-only) which runs arbitrary host CODE. Bounds: loopback-only (never external), max prompt length + timeout, no prompt persistence, honest error when the backend is unreachable (SB-077 — telemetry is real-measured, empty without a backend). The read-only contract test is updated to permit the one inference-query POST (mutations still 405). 5 Q-rows (A functional-web-chat / B read-compute-framing / C real-telemetry answered; D per-prompt-tier-classify / E multi-turn-history proposed). Multi-turn history + per-device targeting + a metrics scraper are Stage N. | operator-directed 2026-07-09 | SDD-062 on branch `claude/recover-projects-b0oT6` |
| 063 | D-12 networking read model (the selfdef rules-mirror consumer — wire /api/d-12/snapshot to real state) | draft | post-Gate-5 | The unwired D-12 networking producer, per operator directive 2026-07-09 (chose the D-12 read-model wiring after SDD-062 merged in PR #40; confirmed the target = the standalone d-12-networking rules-mirror panel, the D-13 grants-mirror pattern, over the VLAN/interfaces model or the network-edge split). The D-12 panel renders from an inline `seed` mock (nftables Ring-0-4 traffic + summaries + rules + denied-egress, a `RulesMirrorSnapshot 1.0.0` shape) — its banner says "no /api/d-12/snapshot publisher wired yet". NEW `docs/sdd/063-networking-rules-mirror.md`. Ships `scripts/mirror/selfdef-rules-mirror.py` — a READ-ONLY consumer of the selfdef rules mirror (MS007 typed-mirror pattern, exactly like `selfdef-grants-mirror` D-13): `snapshot()` reads the published artifact (`SOVEREIGN_OS_SELFDEF_RULES_MIRROR`, default `/run/sovereign-os/selfdef-mirror/rules.json`) + projects the `RulesMirrorSnapshot` shape the webapp renders; ABSENT artifact → `mirror_status:offline` + zeroed summaries + empty ringTraffic/rules/denied (honest, SB-077 — never fabricates rules). + `scripts/operator/rules-mirror-api.py` (loopback :8133, read-only: `GET /api/d-12/snapshot` + `/stream` SSE + version/healthz/webapp/control-systems; 405 on all POST/PUT/DELETE) + `sovereign-rules-mirror-api.service` (R171-hardened) + osctl `rules-mirror {snapshot,summaries}` + catalog/master-dashboard registration + the d-12 webapp fetch wiring (fetch /api/d-12/snapshot, refactor render to consume it, `seed` stays as offline fallback, banner flips mock→online/offline, the `emit()` clipboard `selfdefctl` actions stay). R10113 + R10212 PRESERVED — sovereign-os NEVER runs nft or mutates IPS state (rules mutate via selfdefctl + MS003 on the IPS side only); the daemon is read-only (405); the mirror artifact is read-only. 5 Q-rows (A rules-mirror-target / B read-only / C artifact-path answered; D SSE-refresh / E selfdef-publisher-crate proposed). The selfdef-side rules-mirror publisher crate is Stage N (cross-repo); the consumer degrades to offline until it publishes. | operator-directed 2026-07-09 | SDD-063 on branch `claude/recover-projects-b0oT6` |
| 064 | M028 memory admission-lifecycle engine + projection reconcile (make D-07 real end-to-end) | draft | post-Gate-5 | SDD-059/060 Stage N — the deferred real M028 producer + the Q-059-D/Q-060-D projection reconciliation, per operator directive 2026-07-09 (chose the M028 admission producer after SDD-063 merged in PR #41; the per-panel wiring sweep is complete, so the remaining work is Stage-N backends behind wired panels). SDD-059/060's producer was a minimal `register()` stand-in + `memory.json`'s aggregate counts/lifecycle were always 0 (nothing writes them; the store↔projection are decoupled). GROUNDED (SB-077, surfaced + confirmed): there is NO real agent-memory source in sovereign-os (no graph/event-stream/mirror) — so the producer can't "populate from a real graph"; the honest build is the admission ENGINE fed by an `admit` verb (observations CLI/fixture-fed; the auto-feed event stream is a further Stage-N, like the rules-mirror publisher crate / how session-runtime built the M057 producer without a source). NEW `docs/sdd/064-memory-admission-engine.md`. Ships `scripts/intelligence/memory-admit.py` — the admission ENGINE (reuses the store memory-store.py owns): `admit(type,summary,--trigger <store-if>|--ignore <reason>,--trust)` applies the M028 value gate (8 store-if triggers user-corrected/task-outcome/repeated-pattern/new-fact/tool-worked/model-mistake/high-value-reuse/preference ADMIT; 5 ignore-if transient/low-trust/duplicate/noisy/unverified + trust<30 + duplicate → NOT stored) minting at stage `observe`; `advance(mem-id)` walks the 11-stage lifecycle (observe→classify→quarantine→link→score→store-raw→extract-facts→verify→promote→decay→archive, M00471 verbatim; idempotent at archive). + `memory-store.py` `reconcile()` — recomputes memory.json counts (8 types, active entries) + lifecycle occupancy FROM the store via read-modify-write PRESERVING the memory-decide-owned pending/history/diffs/profile (best-effort, never breaks a store write); wired into register/forget/undo/purge + a `reconcile` verb. osctl `memory-changes {admit,advance,reconcile}` routing. NO D-07 webapp change — the panel already renders counts/lifecycle from the snapshot, so once reconcile writes real values the tiles + 11-stage occupancy become real automatically (Q-059-D/Q-060-D CLOSED). SB-077: never fabricates a source; admission is value-gated. `memory-changes.py` stays a pure reader (405 API); the store entry + ledger shapes unchanged (admission fields additive); atomic writes + OCSF-5001 span; DRY-RUN default; MS003 unsigned-pending-MS003. 5 Q-rows (A engine-scope / B CLI-fed-observations / C trigger-set / D reconcile-ownership answered; E RLM-navigator/SLM-janitor/event-stream proposed). The real observation event stream + the RLM navigator + the SLM janitor are Stage N. | operator-directed 2026-07-09 | SDD-064 on branch `claude/recover-projects-b0oT6` |

## Slots reserved for Stage 2 onwards (preview, non-binding)

Subsequent SDDs (011+) cover Stage 2 build scripts, lifecycle management,
first-login assistant, inference-backend stack selection (Q-017), etc.
Numbering continues monotonically; specific titles land when their PR
opens.

## How to add an SDD

1. Pick the next free three-digit number.
2. Create `docs/sdd/NNN-<short-slug>.md`.
3. Open with the canonical status block:
   ```
   > Status: <draft | review | accepted | implemented | abandoned>
   > Owner: <name or team>
   > Last updated: <YYYY-MM-DD>
   > Closes findings: F-NNNN-MMM, ...   (or "none")
   > Derived from: <upstream artifacts>
   ```
4. Sections: Mission · Problem · Required coverage · Goals · Non-goals ·
   Open questions (Q-X rows) · Way forward · Cross-references.
5. Update this INDEX with the new row.
6. When the SDD's open questions resolve, append `D-NNN` entries to
   `docs/decisions.md` and annotate the SDD's `Q-X` rows in place with
   `**answered (D-NNN, YYYY-MM-DD)**`.
