# Standing operator mandate — 2026-05-17

> **Why this file exists.** The harness's `/goal` command sets a Stop-hook
> condition that *auto-clears* once any single check matches it. The
> operator's actual intent is the OPPOSITE: the goal text is a
> **multi-month, compound, long-running mandate** that drives months of
> Epic/Module/Task work, not a single completion gate. This file is the
> durable record. Future sessions read THIS file (not the ephemeral
> `/goal` Stop hook) as the source of truth.
>
> **Operator quote that triggered this file (verbatim, sacrosanct):**
>
> > "if I cannot set a goal and let it drive for over 4 hours this
> > feature is useless... the goal of the command goal is so that it
> > continue because you know it can even possibly never even reach the
> > end or its really really far so it can only slowly by slowly
> > progress inside the proper workflow.. SDD & TDD and SFIF and Design
> > Patterns and OOP and SRP and documentation and all. and even if it
> > a goal command you should register what I said so that you can
> > break it down and have identifiable and workable pieces and so we
> > can make sure we dont minimize or reduce or conflate or corrupt
> > what was said.. most of the thing I said they compound, meaning
> > that it multiply even at time where 1 thing lead to multiplied by
> > 2-3 multiplied by 2-3 multiplied by 2-3 multiplied by 2-3
> > multiplied by 2-3, etc, at some point its a lot of development
> > needed with all the layers and flexibilities and features and
> > options and modules and project and pieces. The goal is to have
> > the goal drive a very long PR or very long changes on the main if
> > given authority and planify long development, creating Epics,
> > Modules, Tasks, etc.. again here make sure you record this and
> > make sure we do some things about it. As always think before act.
> > do not confuse /goal with a normal prompt / a normal request... I
> > do not want to have to repeat any of this. Do not minimize anything
> > nor the situation."

## 1. The operator mandate (verbatim, sacrosanct)

Reproduced VERBATIM from the multiple `/goal` invocations across this
arc; no editorialization, no reduction. Each `/goal` is **additive**
— never replaces a prior one. The mandate is the **union** of every
verbatim block below.

When future rounds quote it, they quote from here.

### §1.0 — Re-instate directive (2026-05-17, operator paste-record session)

> "lets record and re-instate ... continue till you meet ALL MY
> REQUIREMENTS without MINIMIZING or rephrasing or compressing or
> conflating.. RETURN REREAD ALL THE RAW DUMP AND REPROCESS IF YOU
> NEED or JUST ask me question if you are lost"

Operative interpretation:
- The five `/goal` blocks the operator pasted in the same session
  are ALL part of the standing mandate. None supersedes another.
- "till there is really no work left" is the stop condition
  (operative for §1a, applies to all). Default = perpetual.
- Re-read raw dumps at `devops-solutions-information-hub/raw/`
  rather than re-asking the operator for context already given.
- Ask questions ONLY when genuinely ambiguous, not as a default.

### §1a — Branch + PR + ultimate-OS posture

> "/ goal continue till there is really no work left. you can keep
> working in selfdef branch and have a work PR (not blocked at
> draft) that keep cummulating and that when a massive group of
> changes and features are ready we merge it.. a branch relative
> the changes / additions and everything to the selfdef and its
> modules and integrations and the sovereign os. Keep in mind that
> you can improve at any layer the solution.... remember this is
> way more than a basic Debian 13 (non-GUI)... its a powerhouse OS
> with superpower features and a selfdef and advanced features so
> well suited. And observability, and operability and configuration
> and personalisation. ALl of this can be thought through and where
> they fit properly and how and such and DO NOT MINIMIZE.. this is
> going to be my ultimate selfdef with the ultimate modules and
> integrations and the Ultimate Sovereign OS for WorkStation AI of
> various profiles but clearly aimed to the specified system with
> the avx512 and the rtx pro 6000 and rtx3090 and 256gb ram ....
> Like I said you can work in selfdef too. both is fine, but in
> selfdef use a branch and a 'never ending' PR instead of the main
> like for the sovereign OS right now. This include all the
> requirements from the raw dumps including the flexibility and
> configuration possible, endless flexibility and fine-tuning and
> adapting possible: Wasm-to-AVX-512 AOT, A single 512-bit ZMM
> vector register can hold and manipulate.... 1-bit models, exploit
> of the hardware to the max including resaerch and continuously
> evolving specs to drive and evolve the SDD and TDD and the full
> IAC and User Experience and Developer experience and an assistant
> feel and clear path and options and modules combo features and
> super-features...."

Concrete operator-named additions in §1a not yet in the decomposition:
- **PR not-blocked-at-draft posture** for selfdef cycle-N PR
  (PR #198 lifted from draft 2026-05-17 per this directive).
- **Ultimate Sovereign OS for WorkStation AI** aimed at the SPECIFIC
  system: AVX-512 (Zen 5 9900X), RTX PRO 6000, RTX 3090, **256 GB RAM**.
- **Wasm-to-AVX-512 AOT pipeline** (Pulse §20 from raw dump): the
  `znver5`-targeted Cranelift/Wasmtime compile path that emits native
  AVX-512 machine code, bypassing JIT fallbacks.
- **1-bit / ternary models on ZMM registers** (raw dump §17.1 + §20):
  pack 2 bits per parameter, VPDPBUSD INT8 path, 5-12 tok/s on CPU.
- **Hardware exploit to the max** including continuous research +
  spec evolution (SDD + TDD evolve as research evolves).
- **Full IaC + Developer experience + assistant feel** + clear path
  + options + modules-combo-features + super-features.

### §1b — Multi-mode functioning + grey-out UX + REPL tiers (2026-05-17)

> "/ goal there are going to be multiple mode of functioning too,
> like LM Studio and LM Link maybe ? Unsloth ? ... not just a mode
> by profile but a profile that is flexible and allow not only the
> AI and the tools but also me to download, fine-tune, parameters,
> build, run, use and train and adapt and use and eval and etc.
> ... Hotswap from one CPU mode to another to another with some
> auto option(s). Same for the GPU I guess and this like the
> tracking of the state like the watt set consumption for the GPU
> ... with a warning if the RTX 3090 which should be sliglly reduce
> which isn't ... With scans too. with autohealth and doctor and
> analysis and event and notification and messaging. ... non docker
> vs docker install ? possible ? greyout the option that require it
> and/or offer the alternative ... obviously the module if not
> installed would not appear in the dashboard but only in the
> options of the dashboard which offer to install any previously
> non-installed modules or features ... It allow to see the
> management of the software raid and observe and operate and
> configure. to see all the logs files and need for log rotate,
> track files system usage and for each partitions and global and
> such. Offer insights. Allow to interoperate with an MCP via tools
> calls and/or MCP. ... Debian 13 Sovereign OS is a non-GUI by
> default. ... Everything via dashboard/UInterface or terminal
> tools OR AI, as my chose or even needs. ... Programming,
> Proto-Programing, Proto-Proto-Programming and inside REPL you
> do you own things and you even have custom CoT or such ... You do
> not need to wait for me to approve you PR, you can grow a PR for
> a long time ..."

### §1c, §1d, §1e — Hardware-stack expansion (2026-05-17, three times)

> "/ goal Its not only going to be an AI and an AI training station
> with an AI able system but only a guide into the experiece, into
> the field, into the kernel, into the hardware, into the OS, ...
> Memory too I guess and bios settings directives and admonition of
> things that might also not be possible on some board, possibly
> detecting the ASUS ProArt X870E-CREATOR WIFI and its settings and
> potential optimisations and fixes. pci lane splits and whatever
> like virtualization or what we find relevant via search online
> and such. Adapting / Considering the given PSU (probably not
> detectable ?) wattage and rating ? (me: be Quiet! Dark Power Pro
> 13 1600W Power Supply | ATX 3.1 Compliant | 80 Plus Titanium),
> considering XMP profile and OC profile and room for each and
> estimated at 100% usage and then real time tracking and
> intelligence around it. (Possibly heat too I guess) My PSU even
> have an overclock mode which might be important. Then there is
> the PSU/APC integration with the power mangement and the
> scheduled shutdown when battery reach a certain point as one
> default profile. (schedule/planifest/graceful on all levels,
> orderly). ... Apply what I said at scale and you have for a very
> long time of work. Take your time, do this right."

(§1c, §1d, §1e are three pastes of the same long /goal text — kept
as one canonical entry; the repetition itself is the signal: this
is the most-emphasized block.)

### §1f — full operator paste reproduced verbatim below (unchanged)

> "Its not only going to be an AI and an AI training station with an AI
> able system but only a guide into the experiece, into the field, into
> the kernel, into the hardware, into the OS, into the modules, into
> the features, the services, the configurations, the personalisations,
> the customizations. ? AI and the tools but also download, fine-tune,
> parameters, build, run, use and train and adapt and use and eval and
> etc. Lets think of all the angles. Also selfdef modules, modules
> features and advanced features and profiles. Hotswap, CPU mode and
> option(s). GPU too, watts, RTX 3090 details and possibilities
> established and non-established, same for the RTX Pro 6000 and the
> CPU and AVX512. autohealth and doctor, notification and messaging.
> networks and in and out, the DNS, the Cloudflared ? the tailscale,
> Traefik, non docker vs docker install ? when possible ? container
> level vs system level. dashboard, installs, non-configured, modules
> or features and how configure them. The management of the softwares,
> the "raid"s, observations and operations and configurations. logs,
> log rotate, system usage, partitions and global and such. insights.
> Interoperability, MCP, tools, deps) Debian 13 Base, Sovereign OS and
> vision, why non-GUI by default. server, dashboard or API and modules
> and tools vision. Everything via dashboard/UInterface or terminal
> tools OR AI. Python, System and GPU and LLM and multiple level and
> REPL. Programming, Proto-Programing, Proto-Proto-Programming and
> CoT and custom CoT, integrated intelligence modules, features and
> options and etc. You do not need to wait for me to approve you PR,
> you can grow a PR for a long time and progress the work or if its
> in Sovereign OS you just write in main still. All the previous
> requirements still remains. Again we deliver the top plus ultra
> solutions for selfdef and sovereign OS. We do not minimize anything
> and we do proper research online and processing of what I say and
> what we find and what we think and we move toward my solution
> endlessly. DO not stop after opening or updating a PR. continue
> endlessly. Kernel optimisation, OS, Services, Modules, Tools,
> Dashboards, Configurations, Options. Network, App, & In between.
> Memory too I guess and bios settings directives and admonition of
> things that might also not be possible on some board, possibly
> detecting the ASUS ProArt X870E-CREATOR WIFI and its settings and
> potential optimisations and fixes. pci lane splits and whatever like
> virtualization or what we find relevant via search online and such.
> Adapting / Considering the given PSU (probably not detectable ?)
> wattage and rating ? (me: be Quiet! Dark Power Pro 13 1600W Power
> Supply | ATX 3.1 Compliant | 80 Plus Titanium), considering XMP
> profile and OC profile and room for each and estimated at 100% usage
> and then real time tracking and intelligence around it. (Possibly
> heat too I guess) My PSU even have an overclock mode which might be
> important. Then there is the PSU/APC integration with the power
> management and the scheduled shutdown when battery reach a certain
> point as one default profile. (schedule/planifest/graceful on all
> levels, orderly). a lot but I trust you to break down planify and
> continue with the SDD and TDD and a Senior Architect DevOps Software
> Engineer Fullstack Expert & Mindset. Always a strong workflow and
> non-blocking but always toward the goal(s). Apply what I said at
> scale and you have for a very long time of work. Take your time, do
> this right."

### §1g — Documentation + master-dashboard + global history + auth tiers + firewall/VPN-bridge topology (2026-05-18, operator paste-record session)

> **"lets also make sure that there is a very clear and well defined
> documentation through and through which follow the high standards.
> Including the Readme.md and whatever extension files of it. For both
> Selfdef and its modules and features, and for Sovereign OS and its
> modules and etc... Maybe there can even be an option to add a reverse
> proxy nginx or such to do a master dashboard which regroup all those
> of different port under a single port and super-dashboard or other
> options to get an equivalent of it or such and whatnot. everything
> will also need to go through a thorough UX Design stage in order to
> be or quality, and again still SDD and the 300+ modules, options &
> features.
> Everything is not just core, not just cli, not just TUI, not just
> API, not just tool and MCP but also Dashboards and Web Apps and
> Services.
>
> Also here is a latest model interesting "Nvidia Nemotron 3" "Nvidia
> Nemotron 3 *" "Nano Omni...": you can start with online research
> obviously. Anyway make sure you break down everything and or
> re-address everything and keep progressing and evoluing and
> upgrading. I want a master piece, it takes a long time to deliver
> the utmost experience to all level and have it personalize and
> customizable and configurable and with proper observability and
> operability and control and intelligence and rules and polices and
> modes. Remember there is multiple levels and angle at everything and
> its normal if we have to weave things everywhere and support
> cross-features and augmentations. If you think something is really
> already done, ask yourself if you covered all angles and levels and
> layers and even if then improve it. Do not minimize or settle for
> less or think that you can make me believe that delivering less I
> asked for is right or a cheap version of it either.
>
> Some kind of global history too. tracking things happening, delta,
> differentials... apt changes and operations, or any cli or tool call
> I guess, in the management. more reliable and adapted than simply
> aggregating the .bash_history's. then there is the bashrc we can
> offer to configure it too and we can add our autocompletes and
> aliases and manual / helps and menus. all the places to get the
> remote management dashboard for X, Y or Z or the global one, the
> pages to display also the way to remote connect like for LM Studio
> or Unsloth ? a mode of access from no auth at all by default to
> basic auth to advanced auth to social auth to enterprise auth and
> network level access and etc.
>
> Like normal my AI will also be behind a firewall which will do a
> VPN bridge to my other network since my two LANs are over two
> different WAN and that each have an ISP router with NAT and then my
> Opnsense Firewall with another NAT. This can be detected too I
> guess and maybe it might require some configuration on it and we
> would say it to the user. Maybe we can even integrate features, I
> can even create a user and an API key and then it unlock other
> capabilities when its detected and we want to connect to it. Be
> aware of its state. I think that even if there isn't an Edge
> firewall its possible to install the equivalent or even more
> advanced if we want on this machine if we would be ready to pay the
> performance price but that it will accept the Edge Firewall and its
> level, even if its not a full IPS since its a 'Sharevdi Fanless
> Firewall Mini PC Firewall Router Intel J3710/N3710 Quad Core, 4X
> Intel 2.5GbE i226-V LAN Ports, 8G DDR3 128G SSD AES NI Network
> Gateway Test with pf-Sense/opn-Sense' and has limitations, it being
> 'fanless&cheap' but interesting to do the networting and VPN bridge
> part, and we can still offer to install our own other module and do
> whatever we want on the AI workstation no matter the edge router or
> firewall detected and bridge state and rules and etc."**

#### §1g key concretes (operator-named, NOT minimized)

- **Documentation through-and-through (high standards)** — README.md
  + extension files for **both** selfdef *and* sovereign-os, all
  modules, all features. Quality + UX-design stage upstream of every
  surface.
- **Master-dashboard / reverse-proxy aggregator** — optional nginx
  (or equivalent) consolidating per-port dashboards under a single
  super-dashboard port. Optional, alternative paths offered.
- **Multi-surface delivery** — core, CLI, TUI, API, tool, MCP,
  **Dashboards, Web Apps, Services** (NOT just CLI/MCP — operator
  explicitly enumerates dashboard + web-app + service tiers).
- **300+ modules / options / features** scale baseline.
- **Nvidia Nemotron 3 / Nano Omni** — new model family operator
  flagged for online research + integration.
- **Anti-minimization continued** — "If you think something is really
  already done, ask yourself if you covered all angles and levels and
  layers and even if then improve it. Do not minimize or settle for
  less."
- **Global history** — delta/differentials, apt changes, every CLI/
  tool call, more reliable than aggregating `.bash_history`. Separate
  surface in the management layer.
- **bashrc opt-in configuration** — autocompletes, aliases, manual/
  help, menus. Operator-discoverable.
- **Remote-connect documentation** — every dashboard has a documented
  remote-connect path (LM Studio, Unsloth, etc.).
- **Auth tier ladder** — no-auth (default) → basic auth → advanced
  auth → social auth → enterprise auth → network-level access.
- **Network topology — VPN bridge across two LANs/WANs/NATs**:
  - Two ISP routers (NAT 1)
  - Opnsense firewall (NAT 2) — running on
    `Sharevdi Fanless Firewall Mini PC` (Intel J3710/N3710 Quad Core,
    4× Intel 2.5GbE i226-V LAN, 8G DDR3, 128G SSD, AES NI)
  - VPN bridge between the two LANs (over different WAN)
  - sovereign-os MUST detect this configuration
  - MUST tell user when configuration is needed
  - Integration features unlock when operator creates user + API key
    in Opnsense AND sovereign-os detects connectivity to it
  - State-awareness of the firewall (alive, configured, reachable)
- **Edge-firewall alternatives** — if no edge firewall present,
  offer to install equivalent (or more advanced — IPS-class) **on
  the AI workstation itself**, with explicit performance-cost
  disclosure. Compatible with whatever edge firewall is present,
  even if it's the fanless+cheap variant limited by hardware.

### §1h — Two ultimate solutions + perfectioning + high UX/DX (2026-05-18, operator paste-record session — 6th /goal)

> **"continue till you meet ALL MY REQUIREMENTS without MINIMIZING or
> rephrasing or compressing or conflating.. RETURN REREAD ALL THE RAW
> DUMP AND REPROCESS IF YOU NEED or JUST ask me question if you are
> lost. Continue Endlessly to toward the two ultimate solutions and
> their perfectioning and high UX/DX."**

#### §1h key concretes (operator-named, NOT minimized)

- **"the two ultimate solutions"** — operator now names the duality
  explicitly: **selfdef** + **sovereign-os**. Both must reach the
  ultimate level (master-piece bar). Neither can be compromised for
  the other.
- **"perfectioning"** — operator-coined verb (NOT "perfecting" — the
  operator-named form is preserved verbatim). Iterative refinement
  toward perfection; never declared done.
- **"high UX/DX"** — explicit pairing of User Experience AND
  Developer Experience. Both are first-class targets. Drift to
  prioritize one over the other = mandate violation.
- **All prior /goal blocks (§1.0 – §1g) remain in force** — this
  /goal is additive, not replacement. Per § 6 anti-corruption
  invariants, prior verbatim text stays untouched.

## 2. Standing rules (sacrosanct — applies to EVERY round)

- **Never minimize, reduce, conflate, or corrupt** the operator's
  words. Quote verbatim when citing.
- **Compound mindset.** Each named axis multiplies — sub-axes ×
  sub-features × sub-options. Plan for months, not days.
- **SDD + TDD + SFIF + SRP + OOP + Design Patterns + documentation.**
  Every round is a Senior Architect / DevOps / Fullstack engineer
  delivering "top plus ultra" — not a junior shipping the smallest
  thing.
- **Always non-blocking, always toward the goal.** Don't pause for
  PR merges, don't stop at milestones.
- **Direct push to `sovereign-os` `main`; never-ending PR on
  `selfdef` cycle-N branch.** All previous requirements still apply
  (operator keys never in-repo, `#![forbid(unsafe_code)]`, etc.).

## 3. Epic / Module / Task decomposition

This is the **structural** view of the mandate. Each Epic is months
of work; each Module is weeks; each Task is rounds. Future rounds
SHOULD cite the Epic + Module + Task IDs they advance.

When a round closes a Task, mark it ✓ here. When ALL Tasks of a
Module close, mark the Module ✓. When all Modules of an Epic close,
the Epic is closed — but the operator may add new Modules to it.

### Epic E1 — Hardware-stack visibility & control

> "Kernel optimisation, OS, ... Memory too I guess and bios settings
> directives ... pci lane splits and whatever like virtualization ...
> the given PSU ... XMP profile and OC profile ... Real time tracking
> and intelligence ... PSU/APC integration ... scheduled shutdown
> when battery reach a certain point ... graceful on all levels,
> orderly."

| ID | Module | Status | Rounds |
|----|--------|--------|--------|
| E1.M1 | Kernel tuning (sysctl + GRUB cmdline presets per workload) | ✓ shipped | R239 |
| E1.M2 | BIOS + baseboard + memory probe + board-specific advisories (incl. ASUS X870E-CREATOR WIFI) | ✓ shipped | R251, R260 |
| E1.M3 | Memory XMP/EXPO posture detection + AMD/Intel-aware hints | ✓ shipped | R257 |
| E1.M4 | Virtualization probe (CPU virt flags + KVM + IOMMU + PCIe + container runtimes) | ✓ shipped | R255 |
| E1.M5 | PSU + UPS + wattage budget + OC mode multiplier | ✓ shipped | R252, R259 |
| E1.M6 | Graceful UPS-battery shutdown guard (per-minute timer, triple-gate) | ✓ shipped | R253 |
| E1.M7 | Per-minute wattage time-series sampler (4 Layer B metrics) | ✓ shipped | R258 |
| E1.M8 | Graceful drain-then-poweroff manifest framework | ✓ shipped | R262 |
| E1.M9 | GPU power policy + watt deviance + remediation | ✓ shipped (prior) | R219, R249 |
| E1.M10 | CPU mode hotswap + auto recommender | ✓ shipped (prior) | R221, R230 |
| E1.M11 | Heat integration with budget + thermal-aware advisories | ✓ shipped | R265 |
| E1.M12 | PCIe lane allocation policy advisor (when both GPUs populated) | ✓ shipped | R270 |
| E1.M13 | RTX 3090 + RTX PRO 6000 dual-card-specific advisories | ✓ shipped | R271 |
| E1.M14 | AVX-512 utilization probe + workload-fit advisor | ✓ shipped | R272 |
| E1.M15 | Memory pressure / OOM watcher + Layer B metrics | ✓ shipped | R269 |
| E1.M16 | **256 GB DDR5 RAM-specific advisor** (operator-system: 256 GB total; ZFS ARC clamp = 128 GB per master spec § 19 + GGUF / model-context budget tracker) [from §1a + raw-dump §1.1, §3] | ✓ shipped | R279 |
| E1.M17 | **Wasm-to-AVX-512 AOT pipeline** (`wasmtime compile --target znver5` + `relaxed-simd=true` + `WASMTIME_COMPARE_OPTIONS` env enforcement; master spec § 20 "The Pulse Implementation") [from §1a + raw-dump § 20] | ✓ shipped | R281 |
| E1.M18 | **1-bit / ternary ZMM utilization probe** (live: are we ACTUALLY using VPDPBUSD with packed 2-bit weights via bitnet.cpp / T-MAC? OR fallback FP16 — measured by perf-stat retired-instruction counters) [from §1a + raw-dump § 17.1] | ✓ shipped | R280 |
| E1.M19 | **Hardware-exploit-to-the-max research loop** (continuously evolving SDD + TDD as new BitNet / DFlash / VPDPBUSD findings land; "research mode" verb that surfaces upstream changes from bitnet.cpp + transformers + vllm) | ✓ shipped | R287 (`sovereign-osctl research-loop status / topics`) |
| E1.M20 | **XMP + OC headroom estimation under projected 100% usage + real-time deviance** [from §1b verbatim: "considering XMP profile and OC profile and room for each and estimated at 100% usage and then real time tracking and intelligence around it. (Possibly heat too I guess)"] | ✓ shipped | R292 (`sovereign-osctl oc-headroom status/advisory/inputs`) |
| E1.M21 | **Operator-pull power-management default-profile registry** (battery-threshold shutdown as one default profile; cross-profile graceful drain on all levels) [from §1b verbatim: "the PSU/APC integration with the power mangement and the scheduled shutdown when battery reach a certain point as one default profile. (schedule/planifest/graceful on all levels, orderly)"] | ✓ shipped | R293 (`sovereign-osctl power-profiles list/show/simulate/active`) |
| E1.M22 | **PSU OC-mode orchestration** (be Quiet! Dark Power Pro 13 1600W ATX 3.1 Titanium with overclock-mode toggle — wattage-budget recalc, projected dual-GPU headroom, real-time deviance under load) [from §1b verbatim: "My PSU even have an overclock mode which might be important"] | ✓ shipped | R294 (`sovereign-osctl psu-oc state/budget/projection`) |
| E1.M23 | **GPU possibility catalog — established vs non-established per-card capabilities** (RTX 3090 + RTX PRO 6000 specifically — what's tested working on this stack vs what's theoretical/untested) [from §1b verbatim: "RTX 3090 details and possibilities established and non-established, same for the RTX Pro 6000 and the CPU and AVX512"] | ✓ shipped | R295 (`sovereign-osctl gpu-possibility list/show/gaps`) |
| E1.M24 | **ASUS X870E-CREATOR WIFI BIOS directives catalog** — specific named BIOS settings per AI workload (EXPO XMP, PCIe 5.0 stability, Resizable BAR, IOMMU/VT-d, SVM, AVX-512 enable/disable, Above 4G Decoding, fan curves) with per-setting operator rationale; cross-checks against probed state where possible [from §1b verbatim: "bios settings directives and admonition of things that might also not be possible on some board, possibly detecting the ASUS ProArt X870E-CREATOR WIFI and its settings and potential optimisations and fixes"] | ✓ shipped | R299 (`sovereign-osctl bios-directives list/show/check`) |
| E1.M25 | **Holistic operator-posture rollup** — synthesizes R292/R294/R296/R298/R299 verdicts (oc-headroom, psu-oc, thermal-oc, storage-health, bios-directives) into ONE worst-axis verdict on the dashboard + a CLI verb [from §1b verbatim: "Everything via dashboard/UInterface or terminal tools OR AI"] | ✓ shipped | R300 (`sovereign-osctl operator-posture status/advisory` + new dashboard card) |
| E1.M26 | **PCIe lane split detection** — actual `lspci -vv` parse of per-device LnkCap vs LnkSta (current width + speed) + dual-GPU split state; complements R270 pcie-policy (advisory) with concrete runtime measurement [from §1b verbatim: "pci lane splits and whatever like virtualization or what we find relevant via search online and such"] | ✓ shipped | R301 (`sovereign-osctl pcie-lane-detect status/gpu/degraded`) |
| E1.M27 | **APC/UPS battery escalation ladder** — multi-threshold cascade beyond R293's single-profile registry: pre-alert at battery_minutes_remaining > N, drain inference workloads at M, drain ALL workloads at K, hard shutdown at <K [from §1b verbatim: "the PSU/APC integration with the power mangement and the scheduled shutdown when battery reach a certain point as one default profile. (schedule/planifest/graceful on all levels, orderly)"] | ✓ shipped | R302 (`sovereign-osctl battery-ladder list/show/simulate`) |
| E1.M28 | **GPU per-model-per-mode wattage catalog** — RTX 3090 + RTX PRO 6000 idle / typical / peak / OC-peak watts per operational mode (low / balanced / perf / oc); cross-refs R292 oc-headroom for dual-card budget projection [from §1b verbatim: "GPU too, watts, RTX 3090 details and possibilities established and non-established, same for the RTX Pro 6000"] | ✓ shipped | R303 (`sovereign-osctl gpu-wattage list/show/budget`) |
| E1.M29 | **Memory-pressure → OC-profile dampening advisor** — composes R269 memory-pressure + R292 oc-headroom: when memory pressure spikes, recommend backing off OC profile to free per-GPU power for VRAM-spill swap-in; emits operator-readable "dampen OC by N steps" directive [from §1b verbatim: continuous compose of "memory" axis with "OC profile and room for each"] | ✓ shipped | R304 (`sovereign-osctl memory-pressure-damper status/advisory`) |
| E1.M30 | **Kernel cmdline parameter advisor** — parse `/proc/cmdline` actual + diff against operator-pinned AI-workload recommended set (iommu=pt, amd_iommu=on, transparent_hugepage=madvise, mitigations=off when operator-trusted, nohz_full, isolcpus); emits add/remove diff + per-param rationale [from §1b verbatim: "Kernel optimisation, OS, Services, Modules, Tools, Dashboards, Configurations, Options"] | ✓ shipped | R305 (`sovereign-osctl kernel-cmdline status/diff/apply-hint`) |
| E1.M31 | **CPU hotswap mode detection** — per-CPU current governor / EPP / driver state + available transitions + per-mode operator rationale; complements R221/R230 (E1.M10 cpu-mode + recommender) with the actual runtime detection layer [from §1b verbatim: "Hotswap, CPU mode and option(s)"] | ✓ shipped | R307 (`sovereign-osctl cpu-hotswap status/per-cpu/transitions/swap-hint`) |
| E1.M32 | **ASUS ProArt X870E-CREATOR WIFI board-specific tuning advisor** — operator's exact board: PCIe slot allocation table, M.2 slot speed matrix, dual-GPU bifurcation modes, BIOS-flashback recipe, memory training timeout, known issues. Extends R251 generic bios-info [from §1b verbatim: "possibly detecting the ASUS ProArt X870E-CREATOR WIFI and its settings and potential optimisations and fixes. pci lane splits and whatever"] | ✓ shipped | R312 (`sovereign-osctl board-advisor status/advise/slot-map`) |
| E1.M33 | **be Quiet! Dark Power Pro 13 1600W OC-mode orchestration** — operator's exact PSU has a physical OC-switch (combines multiple +12V rails into one stronger rail). OS can't detect switch state → operator-pull declaration via overlay + per-state recommendation matrix: when OC mode ON, raises max safe gpu_oc_multiplier ceiling for R294 psu-oc compositions; when OFF, falls back to multi-rail-conservative limits. Composes with R292/R294/R303 [from §1b verbatim: "be Quiet! Dark Power Pro 13 1600W Power Supply ... My PSU even have an overclock mode which might be important"] | ✓ shipped | R313 (`sovereign-osctl psu-oc-mode status/recipe/recommend`) |
| E1.M34 | **PSU/APC default-profile orchestration** — curated named profiles (conservative / balanced / aggressive) that bundle battery_pct thresholds × per-threshold actions × drain ordering × notify dispatch × shutdown commit, ready out-of-box. Composes R253 graceful-shutdown + R302 battery-ladder + R262 drain. [from §1b verbatim: "PSU/APC integration with the power management and the scheduled shutdown when battery reach a certain point as one default profile. (schedule/planifest/graceful on all levels, orderly)"] | ✓ shipped | R314 (`sovereign-osctl apc-profile list/show/apply-hint`) |
| E1.M35 | **XMP/OC profile room estimator under dual-GPU sustained load** — composes PSU rated W + estimated extra W of XMP/EXPO memory + CPU OC multiplier extra W + dual-GPU sustained load → budget-left + safe-(XMP, CPU-OC, GPU-OC)-combination matrix. [from §1b verbatim: "considering XMP profile and OC profile and room for each and estimated at 100% usage and then real time tracking and intelligence around it"] | ✓ shipped | R315 (`sovereign-osctl xmp-oc-room status/budget/recommend`) |
| E1.M36 | **Real-time wattage+heat trend watcher** — periodic-sample daemon that records (wattage, GPU temp, CPU temp) tuples to JSONL + applies rolling-window trend intelligence: stable / climbing / climbing-fast verdict per signal. Composes R258 wattage-sampler + R265 heat-integration. Enables operator to spot "wattage rising faster than thermal can dissipate" minutes before OPP/thermal-throttle [from §1b verbatim: "real time tracking and intelligence around it"] | ✓ shipped | R316 (`sovereign-osctl wattage-heat-trend tick/status/history`) |
| E1.M37 | **Operator hardware-inventory catalog** — single-source-of-truth declarative catalog of operator's EXACT hardware (CPU/GPU/PSU/UPS/RAM/NVMe/board) with operator-overridable per-slot location info. Defaults from operator's verbatim spec drop: SMT2200C UPS (2200VA/1980W), 2× Corsair Vengeance DDR5 128GB 6400 CL42 CMK128GX5M2B6400C42 (256GB total across 4 DIMMs), 2× Samsung 990 EVO Plus 2TB Gen4 x4 / Gen5 x2. Each component cross-refs its related advisor for operator-pull navigation. [from §1b verbatim operator spec drop + "memory place in one or even multiple place for RAM, DISK, GPU, etc."] | ✓ shipped | R317 (`sovereign-osctl inventory list/show/audit`) |
| E1.M38 | **Heat-tied OC auto-throttle with triple-gate** — composes R296 thermal-oc-budget + R304 mem-pressure-damper + R315 xmp-oc-room to recommend a damped gpu_oc_multiplier; `apply` verb writes the new value to /etc/sovereign-os/oc-headroom.toml overlay BUT only when SOVEREIGN_OS_CONFIRM_DESTROY=YES + --confirm-throttle + --apply ALL present (triple-gate). Preserves NEVER-AUTO-MUTATES doctrine via explicit operator gate. [from §1b verbatim — operator-flagged stop-hook gap "no heat-tied OC auto-throttle"] | ✓ shipped | R318 (`sovereign-osctl heat-oc-throttle status/recommend/apply`) |
| E1.M39 | **Fan / cooling awareness advisor** — lm-sensors fan RPM readout + per-mode (idle / inference-ready / training / oc-burst) recommended fan curves + software-override gating advice (BIOS dependent per-board; ASUS X870E-Creator WiFi requires "Q-Fan Tuning enabled" + "Allow Software Override = true" + "Fan Profile = Manual"). Operator-pull "is my cooling actually set up correctly for my current workload?" [from §1b verbatim operator spec drop: "is it also going to be aware of my fans ? or my fan settings? bios and such ? and what it should be vs what it is and software side override? ... obviously for a AI workstation there are certain recommandations especially during training and whatever mode that require readiness of inference"] | ✓ shipped | R337 (`sovereign-osctl fan-advisor status/recommend/modes/bios-gate`) |
| E1.M40 | **R315 xmp-oc-room-advisor surfaces R317 inventory-catalog caveats** — operator-actionable buried caveats now bubble to operator-pull verdicts. Specifically: the 4-DIMM CMK128GX5M2B6400C42 XMP-stability caveat ("XMP may fail to train at advertised 6400MHz with 4 DIMM populated; operator may need to drop to 6000MHz or use AMD's EXPO kit-compatibility lookup") surfaces in `xmp_stability_warnings` JSON field when xmp_enabled=true. NEW `inventory_caveats` field on `status` JSON lists every R315-tagged catalog entry with non-null caveat + severity tag (warn|info). Cross-references R317 catalog without coupling — import-via-importlib pattern + NEVER-raise contract preserves advisor liveness if catalog vanishes. [from §1b verbatim operator hardware-spec drop: "2x CORSAIR Vengeance DDR5 RAM 128GB (2x64GB)... (CMK128GX5M2B6400C42)" + catalog op_caveat that was buried until now] | ✓ shipped | R347 (xmp-oc-room-advisor.py _load_inventory_caveats + xmp_stability_warnings + inventory_caveats fields + 8 L3 covering field-present + schema + xmp-on-vs-off + severity + bidirectional consistency + NEVER-raise + R338-modulation-interop) |
| E9.M17 | **Promote inventory-consult to scripts/lib/ + adopt in R252** — R347 inline pattern (xmp-oc-room-advisor reads R317 catalog) extracted into `scripts/lib/inventory_consult.py` per SDD-032 §4 helper-library doctrine. Public API: `find_advisor_caveats(round_id)` + `caveats_matching(round_id, contains_any=, contains_all=)`. R315 refactored to use helper in same commit (proves API by porting first consumer). R252 power-status added as SECOND consumer — surfaces UPS SMT2200C operator-actionable caveat to `advisories` JSON, AND escalates verdict ok→attention when UPS reports OnBattery AND a `warn`-severity caveat exists. SDD-032 extended from 3 modules to 4; doctrine lint extended to pin inventory_consult API surface + R348 cross-ref. NEVER-raise preserved across helper + both consumers (missing/broken catalog → empty list, not crash). [from R347 — natural promotion when 2nd consumer arrives, per SDD-032 doctrine] | ✓ shipped | R348 (scripts/lib/inventory_consult.py + R315 refactor + R252 cmd_advisories surfacing + SDD-032 §4 + 8 L1 lint assertions + 7 L3 covering helper-API + multi-consumer + NEVER-raise) |
| E2.M27 | **Workload-mode coordinator** — single source of truth for current operator workload mode (idle / inference-ready / training / oc-burst); other advisors (R337 fan-advisor, future R296 thermal, future R304 mem-damper) read THIS as the canonical mode instead of each carrying their own active_mode default. `set` verb mutates under triple-gate via R328 safe_apply. `affected-advisors` lists which surfaces should adopt the mode. First adopter of R328 safe_apply helper beyond R318. [from §1.0 — propagate the R337 mode concept across all mode-aware advisors] | ✓ shipped | R338 (`sovereign-osctl workload-mode status/modes/affected-advisors/set`) |
| E2.M28 | **R337 fan-advisor adopts R338 workload-mode as canonical** — fan-advisor first checks /etc/sovereign-os/workload-mode.toml for active_mode; falls back to its own overlay knob when unset; explicit `--mode` flag still wins above both. `follow_workload_mode_coordinator=false` overlay knob is an opt-out. R338 registry updated to show R337 adopted_in_round=R339. [from §1.0 — first cross-advisor adoption of R338 workload-mode] | ✓ shipped | R339 (fan-advisor.py canonical-mode reader + 10 L3 covering canonical/fallback/explicit-flag/opt-out/malformed/end-to-end) |
| E2.M29 | **R307 cpu-hotswap adopts R338 workload-mode as canonical** — when workload mode active, cpu-hotswap derives pinned_mode + pinned_epp from `WORKLOAD_MODE_TO_GOV_EPP` map (idle → powersave/power; inference-ready → schedutil/balance_performance; training → performance/performance; oc-burst → performance/performance). Explicit overlay pinned_mode/pinned_epp wins over canonical. Same fallback chain + opt-out knob as R339. Proves R339 pattern replicates. [from §1.0 — second cross-advisor adoption of R338 workload-mode] | ✓ shipped | R340 (cpu-hotswap.py canonical-mode reader + WORKLOAD_MODE_TO_GOV_EPP map + 10 L3 covering 4-mode derivation + explicit-wins + fallback + opt-out + malformed + end-to-end) |
| E2.M30 | **R296 thermal-oc-budget adopts R338 workload-mode as canonical** — per-mode margin modulation: training tightens cpu_tjmax_watch_margin (5°C below tjmax instead of default 10°C) + raises gpu_temp_watch_c (83°C instead of 80°C) for thermal-trade-throughput posture; idle loosens watch thresholds for early-warning conservatism. Critical thresholds preserved (hardware-damage line never moves). Third R338 adopter — pattern proven across 3 advisor shapes (curves, governor, margins). [from §1.0 — third cross-advisor adoption of R338 workload-mode] | ✓ shipped | R341 (thermal-oc-budget.py canonical-mode reader + WORKLOAD_MODE_TO_MARGIN_DELTA map + 10 L3 covering 4-mode modulation + critical-threshold ordering invariant + fallback + opt-out + end-to-end; bonus R296 test #9 sed-escape bug fixed) |
| E2.M31 | **R304 memory-pressure-damper adopts R338 workload-mode as canonical** — per-mode threshold + step modulation: training raises warn_avg10 (50% instead of 30%; sustained memory pressure expected during fine-tune) + gentler mild step (2% instead of 5%); idle/oc-burst lower thresholds for early warning. Floor invariant: warn≥5%, crit≥warn, mild≥0.01. Fourth R338 adopter — completes original 4-advisor registry. [from §1.0 — fourth cross-advisor adoption of R338 workload-mode] | ✓ shipped | R342 (memory-pressure-oc-damper.py canonical-mode reader + WORKLOAD_MODE_TO_DAMPER_DELTA map + 10 L3 covering 4-mode modulation + floor invariant + fallback + opt-out + end-to-end) |
| E2.M32 | **R315 xmp-oc-room-advisor adopts R338 workload-mode as canonical** — first POST-SDD-035 adopter (validates the formal contract works for advisors beyond original 4-set). Modulates 4 runtime knobs (xmp_enabled / cpu_oc_multiplier / gpu_oc_notch / dual_gpu_active) per WORKLOAD_MODE_TO_RUNTIME_KNOBS map: idle = single-GPU only (PRO 6000); training = dual-GPU + 10% CPU/GPU OC; oc-burst = max-everything. SDD-035 L1 lint auto-picks-up R315 via ADOPTERS list. [from §1.0 — first post-SDD-035 adopter, contract generalization proof] | ✓ shipped | R344 (xmp-oc-room-advisor.py canonical-mode reader + WORKLOAD_MODE_TO_RUNTIME_KNOBS map + 10 L3 covering 4-mode modulation + idle/training wattage gap + fallback + opt-out + end-to-end; SDD-035 lint auto-picked-up adopter via ADOPTERS append) |
| E2.M33 | **R293 power-profiles adopts R338 workload-mode as canonical** — closes SDD-035 deferred candidate (R293 was marked "different shape — defer" in Future-quarter §). Maps canonical mode → recommended profile name string via WORKLOAD_MODE_TO_PROFILE_NAME (idle→ac-loss-graceful-suspend; inference-ready→battery-threshold-graceful-shutdown; training→thermal-budget-throttle; oc-burst→psu-headroom-warn). Recommendation is ADDITIVE — operator's `default: true` profile remains the `active_profile`; `workload_mode_recommended_profile` is a separate surface. Sixth R338 adopter; full 6/6 known-adopter registry. Demonstrates contract generalizes even to shape-mismatch advisors (string vs runtime knobs vs curves). [from §1.0 — close all deferred SDD-035 candidates] | ✓ shipped | R345 (scripts/power/profiles.py canonical-mode reader + WORKLOAD_MODE_TO_PROFILE_NAME map + load_profiles() 2-tuple → 3-tuple signature (cfg now surfaces R338 knobs) + 10 L3 covering 4-mode resolution + additive-recommendation + fallback + opt-out + end-to-end; SDD-035 lint auto-picked-up adopter via ADOPTERS append; SDD-035 doc Future-quarter § struck-through; R338 affected-advisors registry now 6/6 adopted, 0/6 future_adoption) |
| E2.M34 | **Non-configured modules detector** — operator-pull "what have I installed but not yet configured?" Closes a §1b axis double-pasted in the operator's hook drop ("installs, non-configured, modules or features and how configure them"). `sovereign-osctl module-state list/show/recommend`. 16-module default catalog covering power/hardware/kernel/ai/install/dashboard/notification/intelligence axes. Per-module verdict in {fully-configured / installed-not-configured / running-without-overlay / config-only-no-runtime / shipped-but-untouched} derived from 4 probed signals (has_example_config + has_etc_config + has_systemd_unit + systemctl active/enabled). For each attention item, surfaces the operator-runnable configure_verb (typically `cp config/<name>.toml.example /etc/sovereign-os/<name>.toml`). L3 #10 enforces every configure_verb cites a real example file on disk (catches authoring drift). systemctl probe NEVER-raises on containers/tests (treats "not booted as init" as un-probed). [from §1b verbatim hook drop, repeated 2x in mandate] | ✓ shipped | R351 (`sovereign-osctl module-state list/show/recommend` + 16-module × 5-verdict matrix + 12 L3 covering schema + empty-etc → all-unconfigured + configured-flip + show-known/unknown + recommend-with-next-step + summary-density + axis-filter + state-filter + configure_verb-points-to-real-file + osctl-dispatch-with-rc + fully-configured-fleet-rc=0) |

### Epic E2 — Software-stack visibility & control

> "OS, Services, Modules, Tools ... Debian 13 Base, Sovereign OS ...
> non-GUI by default ... server, dashboard or API and modules and
> tools vision."

| ID | Module | Status | Rounds |
|----|--------|--------|--------|
| E2.M1 | Services inventory (live + shipped catalog + failures + timers) | ✓ shipped | R240 |
| E2.M2 | Selfdef modules diff / install-options / install-plan / config-scaffold / apply-plan | ✓ shipped (selfdef) | SD-R83 → SD-R93 |
| E2.M3 | Install paths matrix (container vs system, network-state-aware) | ✓ shipped | R237 |
| E2.M4 | Software RAID observation + operation + configuration | ✓ shipped (prior) | R223 |
| E2.M5 | Logs + log-rotate + filesystem usage + insights synthesizer | ✓ shipped | R222, R234 |
| E2.M6 | Module features sub-configuration (operator-pull TOML overrides per module) | ✓ shipped (selfdef) | SD-R99 |
| E2.M7 | Advanced module-features lifecycle (enable/disable individual features within a module) | ✓ shipped (selfdef) | SD-R100 |
| E2.M8 | systemd service hardening lint (R171 doctrine extension) | ✓ shipped | R171 + R346/SDD-036 (R171 pins 10-key baseline across all 24 service units; R346 adds inference-service stricter posture — MemoryDenyWriteExecute + RestrictAddressFamilies — for the 4 prompt-handling daemons; inline-comment waiver honored for codegen-needing services per SDD-036; L1 lint auto-fails if a 5th inference backend lands without stricter posture) |
| E2.M9 | Service-dependency graph visualizer (operator's drain ordering) | ✓ shipped | R277 |
| E2.M10 | **Heat budget tied to OC profile** — per-component thermal headroom (CPU pkg vs tjmax, per-GPU temp vs max) cross-referenced against R292 oc-headroom verdict + R294 PSU OC-mode state; emits "OC posture is thermally safe / tight / over" verdict [from §1b verbatim: "considering XMP profile and OC profile and room for each and estimated at 100% usage and then real time tracking and intelligence around it. (Possibly heat too I guess)"] | ✓ shipped | R296 (`sovereign-osctl thermal-oc-budget status/advisory/inputs`) |
| E2.M11 | **Network install-layer advisor — VPN + reverse-proxy + DNS docker vs system** (Cloudflare Tunnel vs Tailscale vs Traefik — when does each run docker, when system, when both work?) [from §1b verbatim: "the DNS, the Cloudflared ? the tailscale, Traefik, non docker vs docker install ? when possible ? container level vs system level"] | ✓ shipped | R297 (`sovereign-osctl network-install-advisor list/show/coexist/recommend`) |
| E2.M12 | **Unified storage health rollup** — composes R222 (logs) + R223 (RAID) + R228 (fs) + R234 (insights) into ONE operator-pull "is my storage layer healthy?" verdict: log-rotate posture, RAID state, partition free space, journal size, system-usage trends [from §1b verbatim: "logs, log rotate, system usage, partitions and global and such. insights"] | ✓ shipped | R298 (`sovereign-osctl storage-health status/advisory/inputs`) |
| E2.M13 | **Debian 13 base-system hardening catalog** — OS-level hardening probes beyond BIOS layer (R299): sysctl hardening (kernel.dmesg_restrict, net.ipv4.conf.all.rp_filter, kernel.kptr_restrict), AppArmor/SELinux state, unattended-upgrades posture, auditd presence, fail2ban presence, sshd cfg (PermitRootLogin, PasswordAuth), per-item rationale + runtime probe [from §1b verbatim: "Debian 13 Base , Sovereign OS and vision, why non-GUI by default. server, dashboard or API and modules and tools vision"] | ✓ shipped | R306 (`sovereign-osctl hardening-base list/show/check`) |
| E2.M14 | **Doctor / autohealth synthesizer** — periodic cross-axis health pass that composes R226 health-scan + R300 operator-posture + R298 storage-health + R296 thermal-oc into a single "doctor verdict" + emits R254 notify-dispatch commands when severity crosses threshold [from §1b verbatim: "autohealth and doctor , notification and messaging"] | ✓ shipped | R308 (`sovereign-osctl autohealth tick/status/history/advisory`) |
| E2.M15 | **Sovereign-os integrated-intelligence CoT registry** — operator-pull CoT routines that compose multiple sovereign-os verbs into named decision flows (oc-go-no-go / health-triage / boot-troubleshoot / psu-budget / pre-shutdown / storage-cleanup). Sovereign-os counterpart to selfdef SD-R98 @selfdef_macro [from §1b verbatim: "Programming, Proto-Programing, Proto-Proto-Programming and CoT and custom CoT, integrated intelligence modules, features and options"] | ✓ shipped | R309 (`sovereign-osctl cot list/show/run`) |
| E2.M16 | **Container-vs-system install-mode advisor** — per-installable component, advise system-level vs container install based on isolation needs, dependency footprint, IPC requirements, root-required, GPU passthrough; emits operator-readable tradeoff matrix [from §1b verbatim: "non docker vs docker install ? when possible ? container level vs system level"] | ✓ shipped | R310 (`sovereign-osctl install-mode list/show/recommend`) |
| E2.M18 | **Unified state snapshot** — single command that runs ALL read-only advisors in parallel + emits one consolidated audit doc (hardware + posture + power + thermal + memory + network + storage + autohealth + ...). Operator-pull "what's the COMPLETE state of this host right now?" — useful for fleet aggregation + before-and-after change auditing. [from §1.0 — meta-rollup over the perpetual E9.M3 intake loop] | ✓ shipped | R322 (`sovereign-osctl snapshot snapshot/audit`) |
| E2.M19 | **Maintenance-window scheduler** — operator declares named time windows (e.g., `weekly-deep-maintenance` Tue 02:00–04:00 UTC; `daily-light-touch` 03:30–04:00 daily); other advisors query `can-run-now <window>` before acting. Operator-pull discipline for graceful/orderly/scheduled actions. [from §1b verbatim: "schedule/planifest/graceful on all levels, orderly"] | ✓ shipped | R323 (`sovereign-osctl maintenance-window list/show/can-run-now/active`) |
| E2.M20 | **Fleet snapshot aggregator** — ingests R322 unified state snapshots from multiple hosts (one JSON per file or stdin) + emits cross-host rollup: per-axis verdict distribution, host-level summary, outlier detection. Operator-pull "how is my whole fleet doing?" surface — composes with R322 for the producer side. [from §1.0 — fleet-tier extension of the perpetual E9.M3 intake loop] | ✓ shipped | R324 (`sovereign-osctl fleet-aggregate aggregate/by-axis/outliers`) |
| E2.M21 | **Operator-overlay drift detector** — scans `/etc/sovereign-os/*.toml` (or `--overlay-dir`); parses each overlay; flattens to dotted-path keys + emits per-overlay drift report listing the knobs the operator has overridden vs shipped defaults. Operator-pull "what have I customized on this host?" Audits parse-errors + cross-overlay rollup. [from §1.0 — companion to R283 overlay doctrine + R322 state snapshot] | ✓ shipped | R325 (`sovereign-osctl overlay-drift list/show/audit`) |
| E2.M22 | **Next-action advisor** — examines R322 unified state snapshot + R325 overlay drift + R321 rounds catalog and emits operator-readable ranked list of "most-impactful verb to run next" (e.g., "R318 heat-oc-throttle is recommending dampening — operator could run apply"; "R306 hardening-base check found 3 mismatches"; etc.). Operator-pull "what should I do now?" [from §1.0 — composition of read-only verbs into decision support] | ✓ shipped | R329 (`sovereign-osctl next-action list/top`) |
| E2.M23 | **Config-snapshot for backup/migration** — captures complete operator-customized state into ONE portable JSON: every overlay file body (R325) + active maintenance windows (R323) + audit-log tail (R327) + inventory (R317) + helper-library version manifest (R330). Operator-pull "preserve my host's full customization for backup or migration to a new host." Composes with R322 state snapshot for the runtime-state side. [from §1.0 — durability layer for the perpetual loop's accumulated config] | ✓ shipped | R332 (`sovereign-osctl config-snapshot capture/audit`) |
| E2.M24 | **Config-restore companion to R332** — reads an R332 snapshot JSON + verifies sha256s + replays overlays back to disk under triple-gate. Restore-side of the backup-restore loop. Per-overlay diff vs current state so operator sees what would change. Refuses to write when sha256 mismatches (tampered snapshot protection). Idempotent: identical-content overlays skipped. [from §1.0 — completes the R332 backup half with a verify + replay path] | ✓ shipped | R333 (`sovereign-osctl config-restore verify/apply`) |
| E2.M25 | **Snapshot-diff verb** — given two R322 unified state snapshots, emits per-probe diff across 6 categories: rc_changes / verdict_changes / new_attention (regression) / resolved_attention (improvement) / new_probes / removed_probes. Pre/post-change auditing companion. rc=1 when any new-attention or rc-regression; rc=0 when no regression detected. [from §1.0 — diff-side companion to R322 producer + R324 fleet consumer] | ✓ shipped | R334 (`sovereign-osctl snapshot-diff diff --before A --after B`) |
| E2.M26 | **Config-snapshot-diff verb** — given two R332 config snapshots, emits per-overlay drift: added overlays, removed overlays, content-changed overlays (sha256 mismatch + key-level dotted-path diff). Sibling to R334 (which diffs RUNTIME state) — this diffs CONFIG state. [from §1.0 — diff-side completion for the R332/R333 backup-restore loop] | ✓ shipped | R335 (`sovereign-osctl config-snapshot-diff diff --before A --after B`) |

### Epic E3 — Network visibility & control

> "networks and in and out, the DNS, the Cloudflared ? the tailscale,
> Traefik, non docker vs docker install ? when possible ? container
> level vs system level."

| ID | Module | Status | Rounds |
|----|--------|--------|--------|
| E3.M1 | Network state probe (internet / DNS / cloudflared / tailscale / traefik / docker) | ✓ shipped (prior) | R220 |
| E3.M2 | Cloudflared / Tailscale / Traefik per-service posture advisor | ✓ shipped | R263 |
| E3.M3 | Container-vs-system install-path matrix with grey-out logic | ✓ shipped | R237 |
| E3.M4 | DNS provider posture (Cloudflare/Quad9/AdGuard advisories) | ✓ shipped | R268 |
| E3.M5 | Reverse-proxy config validator (Traefik / Caddy / nginx) | ✓ shipped | R275 |
| E3.M6 | Network performance baseline + drift detection | ✓ shipped | R276 |
| E3.M7 | **Network-runtime-stack advisor** — per-service runtime probe (cloudflared / tailscale / traefik / DNS provider / suricata) + per-service troubleshoot guide with operator-pull diagnostic steps. Composes R220 + R263 + R268 + R275 into ONE deeper rollup with per-service troubleshooting. Closes stop-hook-flagged "no comprehensive DNS/Cloudflared/Tailscale/Traefik network advisor surface beyond install-mode" [from §1b verbatim: "networks and in and out, the DNS, the Cloudflared ? the tailscale, Traefik"] | ✓ shipped | R319 (`sovereign-osctl network-stack list/status/troubleshoot`) |

### Epic E4 — Dashboard / Operator UX

> "dashboard ... Everything via dashboard/UInterface or terminal tools
> OR AI ... installs, non-configured, modules or features and how
> configure them."

| ID | Module | Status | Rounds |
|----|--------|--------|--------|
| E4.M1 | Dashboard HTTP SEED + cards aggregator (R225 contract) | ✓ shipped | R225 |
| E4.M2 | 18 cards spanning every shipped axis | ✓ shipped | R227-R261 |
| E4.M3 | Per-model detail sub-route (/api/models/<slug>) | ✓ shipped | R233 |
| E4.M4 | Grid view for terminal-friendly snapshot | ✓ shipped | R248 |
| E4.M5 | Dashboard auth + allowlist + ACL | ✓ shipped | R250 |
| E4.M6 | Network-state-reactive grey-out of install/options when prerequisite unreachable | ✓ shipped | R274 |
| E4.M7 | Dashboard event timeline (R246 → live tail) | ✓ shipped | R246 |
| E4.M8 | Mobile-friendly card layout (CSS only, no JS framework) | ✓ shipped | R288 (responsive CSS grid + phone breakpoint + print media-query) |
| E4.M9 | Dashboard editable forms for module configuration | ✓ shipped | R289 (dashboard `/modules` + `/modules/<slug>` form → emits `selfdefctl modules feature-set` commands) |
| E10.M92 | **E11.M5 substantive feature — `sovereign-osctl global-history` (operator §1g delta/differentials across 6 sources)** — extends R387-R447 + R443 (osctl help DX) operational-artifact pinning to `scripts/operator/global-history.py` (NEW 480-line Python) + `scripts/sovereign-osctl` (global-history dispatcher + help text) + `tests/lint/test_global_history_contract.py` (NEW 27 L1 assertions). SUBSTANTIVE FEATURE WORK. Per operator §1g verbatim: "Some kind of global history too. tracking things happening, delta, differentials... apt changes and operations, or any cli or tool call I guess... more reliable and adapted than simply aggregating the .bash_history's". 6-source taxonomy: apt (history.log) + dpkg (dpkg.log) + shell (.bash_history with HISTTIMEFORMAT support) + osctl (~/.sovereign-os/history/*.jsonl) + events (cross-cutting JSONL state) + modules (selfdef + sovereign-os module events). 4 verbs: recent / summary / sources / delta. --since accepts ISO 8601 OR relative (24h/7d/2w/1m). --source comma-filter. --json/--human dual-format. SDD-016 metric: sovereign_os_operator_global_history_query_total{verb,source,result}. Read-only (≤1 write_text for metric emission only — lint enforces). Operator-discoverable env overrides for all 6 source paths (zsh/macOS/operator-adapt). Sister surface DISTINCT from existing `history` (build-run logs) + `events` (JSONL state-only aggregator) + `journal` (systemd-journal). 27-assertion L1 lint enforces 6-source completeness + reader-function-per-source + CLI surface + read-only safety + osctl integration + smoke-test via subprocess. E11.M5 status: TODO → ✓ shipped. **Cumulative SDD-037 family: 1027 → 1054 assertions** (67 lints now). [from §1g E11.M5 verbatim — third substantive feature round of the §1g/§1h arc; pattern: lint-mechanization + feature work alternating, both addressing the operator's "two ultimate solutions" mandate] | ✓ shipped | R448 (E11.M5 fully shipped — global-history.py + osctl integration + L1 lint + metric inventory documentation) |
| E10.M91 | **E11.M6 substantive feature — `sovereign-osctl bashrc install/uninstall/status/dump` (operator §1g autocompletes + aliases + manual/helps + menus)** — extends R387-R446 + R413/R443 operational-artifact pinning to `scripts/operator/bashrc-install.sh` (NEW 244-line script) + `scripts/sovereign-osctl` (bashrc dispatcher + help text) + `tests/lint/test_bashrc_install_contract.py` (NEW 26 L1 assertions) + `docs/observability/dashboards/README.md` (metric inventory). SUBSTANTIVE FEATURE WORK. Per operator §1g verbatim: "the bashrc we can offer to configure it too and we can add our autocompletes and aliases and manual / helps and menus". Sentinel-bounded block pattern: edits OUTSIDE the sentinels survive every install/uninstall cycle (operator-anti-destruction). Block ships: 10 operator-discoverable aliases (sosctl/soshelp/sosstatus/sosmodels/soshealth/sosdoctor/sosthermal/soswatt/soshist/sosmorning) + soshelp-menu quick-help function (categorized command groupings) + tab-completion covering 25+ top-level subcommands + 2nd-level completion for profiles/whitelabel/perimeter/models/morning-brief/bashrc. CLI surface: 4 subcommands (install/uninstall/status/dump). Idempotent install (re-run replaces block via sed sentinel delete); reversible uninstall (keeps .sovereign-os-bak backup). zsh-compatible via SOVEREIGN_OS_BASHRC_PATH env override. SDD-016 metric: sovereign_os_operator_bashrc_install_total{action,result}. 26-assertion L1 lint enforces: sentinel pattern + idempotency + ≥8 aliases + soshelp-menu function + completion-uses-`complete` builtin + completion covers operator-named subcommands + DRY_RUN safety + osctl dispatcher routes + help-text documents all 4 subcommands. E11.M6 status: TODO → ✓ shipped. **Cumulative SDD-037 family: 1001 → 1027 assertions** (66 lints now). [from §1g E11.M6 verbatim — second substantive feature round of the §1g/§1h arc (R446 was first, addressing E11.M4 model catalog); this round is fully shipped (no 'partial' status), addressing operator-discoverable bashrc surface end-to-end including the operator-anti-destruction sentinel pattern for safe operator edits OUTSIDE the managed block] | ✓ shipped | R447 (E11.M6 fully shipped — bashrc-install.sh + osctl integration + L1 lint + metric inventory) |
| E10.M90 | **E11.M4 substantive progress — Nemotron 3 Nano Omni catalog enrichment + schema enum expansion (online research + 2 new catalog variants + schema additions)** — extends R387-R445 + R427 (catalog content lint) operational-artifact pinning to `models/catalog.yaml` + `schemas/model-catalog.schema.yaml`. SUBSTANTIVE WORK (not just lint): online research via WebSearch confirmed Nemotron 3 Nano Omni released 2026-04-28, 30B total / 3B active per token (MoE — A3B suffix on HF repo id), hybrid Mamba-Transformer MoE backbone + C-RADIOv4-H vision encoder + Parakeet-TDT-0.6B-v2 audio encoder, multimodal any-to-any (text+image+video+audio), benchmark wins (MMlongbench-Doc, OCRBenchV2, WorldSense, DailyOmni), ~9× throughput vs alternatives. Added 2 new catalog entries (FP8 variant 32GB VRAM for batches/adapters fit; NVFP4 variant 22.4GB checkpoint for Blackwell-native FP4 + RTX 3090 thin-margin fit). Enriched existing BF16 entry's operator_note with verified facts + §1g binding. Schema enum expanded: added `nvfp4` to quantization (Blackwell-native FP4) + `audio` to purpose (Nemotron 3 ASR/audio modality). Schema conformance test caught the gap (2 schema errors → 0). R427 catalog content lint still passes (20/20). **Cumulative SDD-037 family: 1001 → 1001 assertions** (65 lints — this round is FEATURE WORK + schema not a new lint; assertion count steady, real value: filled the §1g E11.M4 directive with verified research). [from §1g verbatim "Nvidia Nemotron 3... Nano Omni... you can start with online research obviously" — operator-discoverable model catalog now has the operator-named model with 3 quantization variants for the operator's quantization-options directive ('all the best selection of models adapted for various size and at various quantization or for various specific purpose')] | ✓ shipped | R446 (models/catalog.yaml +2 entries + 1 enriched / schemas/model-catalog.schema.yaml +`nvfp4` quant + `audio` purpose / E11.M4 partial→done in research+catalog tracks) |
| E10.M89 | **Epic E11 UX surface enumeration lint (§1h 'high UX/DX' — UX-track companion to R443 DX-track)** — extends R387-R444 + R443 (DX side) operational-artifact pinning to Epic E11 Module descriptions. `tests/lint/test_e11_ux_surface_coverage.py` (14 assertions): mandate doc exists + E11 has ≥12 Modules + **every non-meta E11.M MUST declare an operator-facing surface** from §1g taxonomy (core/CLI/TUI/API/MCP/Dashboard/Web App/Service + README/auth/Opnsense/bashrc/reverse-proxy etc. — drift = operator can't tell what they'll SEE/CLICK/TYPE) + meta Modules exempted (E11.M10 UX-design-process / E11.M11 anti-minimization-audit / E11.M12 selfdef-PR-meta) + **per-Module UX surface verbatim**: E11.M1 references README + E11.M2 references reverse-proxy/nginx + E11.M3 lists all §1g surfaces (CLI/TUI/API/MCP/Dashboard) + E11.M4 references model-catalog + E11.M5 references management-layer/history surface + E11.M6 names 'bashrc install' verb + E11.M7 documents per-dashboard auth-tier config + **structural quality**: every E11.M row ≥100 chars (no single-line stubs; operator can tell what ships) + ≥50% of rows cite §1g/§1h source + **§1g/§1h verbatim integrity**: §1g lists full surface taxonomy (8 surfaces) + §1h pairs UX + DX explicitly. **Cumulative SDD-037 family: 987 → 1001 assertions** (65 lints now — MILESTONE: 1000+ assertions). [from §1h 'high UX/DX' — R443 covered DX side (developer sees command in help); R445 covers UX side (operator sees the surface from the Module description); together they enforce §1h's pairing] | ✓ shipped | R445 (tests/lint/test_e11_ux_surface_coverage.py + 14 L1 assertions enforcing §1g surface taxonomy + per-Module UX enumeration; 1000-ASSERTION MILESTONE) |
| E10.M88 | **Epic E11 cross-repo coverage lint (§1h 'two ultimate solutions' duality enforcement)** — extends R387-R443 + R384/R417/R422/R425/R437 (bidirectional consistency patterns) operational-artifact pinning to Epic E11's 12-Module decomposition. `tests/lint/test_epic_e11_cross_repo_coverage.py` (14 assertions): mandate doc exists + Epic E11 present + ≥12 Modules (§1g decomposition floor) + **operator-verbatim content per Module**: E11.M12 documents never-ending PR pattern + selfdef + branch reference + E11.M1 names BOTH selfdef AND sovereign-os (cross-repo docs) + E11.M4 references Nemotron 3 (operator-named §1g model) + E11.M8 documents Opnsense + VPN-bridge / NAT (operator-named topology) + E11.M9 documents Sharevdi + workstation-side IPS alternative + E11.M7 auth tier ladder verbatim (no-auth/basic/advanced/social/enterprise) + E11.M3 multi-surface enumeration (CLI/TUI/API/MCP/Dashboard) + E11.M5 global-history concepts (delta/differential/apt/history) + **structural quality**: every E11.M row has status field (TODO/shipped/partial/in-flight/deferred — drift = audit gap) + ≥60% of E11 rows cite operator-verbatim source (anti-fabrication discipline) + E11.M12 has concrete PR/branch reference. **Cumulative SDD-037 family: 973 → 987 assertions** (64 lints now). [from §1h verbatim 'two ultimate solutions' — sovereign-os + selfdef duality MUST be mechanized in the mandate itself, not just at runtime; this lint catches drift where an E11.M description loses its operator-verbatim binding or cross-repo reference] | ✓ shipped | R444 (tests/lint/test_epic_e11_cross_repo_coverage.py + 14 L1 assertions enforcing §1g/§1h Epic E11 content + cross-repo duality) |
| E10.M87 | **sovereign-osctl DX (Developer Experience) help-coverage lint — first §1h "high UX/DX" track lint** — extends R387-R442 + R413 (handler↔dispatcher bidirectional) operational-artifact pinning to `scripts/sovereign-osctl` cmd_help() body. `tests/lint/test_osctl_dx_help_coverage.py` (12 assertions): osctl exists + ≥20 discoverable handlers + cmd_help body extractable (≥500 chars) + uses heredoc (cat <<EOF) + **every cmd_<name> handler MUST appear in cmd_help body** (DX bar: operator runs `sovereign-osctl help` and sees every command; drift = handler exists but invisible in help) + USAGE: line present + COMMANDS: section header present + ≥5 top-level command groups (operator-discoverable surface breadth) + ≥15 substantive command lines (≥10-char descriptions; drift = single-word entries = operator confused) + --json flag mentioned (fleet-aggregation DX surface) + **combined R413 + R443 bidirectional**: handlers in dispatcher case statement MUST also be in cmd_help body (drift = dispatched command without docs = DX gap) + Q-019 origin reference present (operator-discovery context). **Cumulative SDD-037 family: 961 → 973 assertions** (63 lints now). [from §1h verbatim "high UX/DX" — first lint specifically aligned to the DX track; pairs with R413's handler↔dispatcher (UX-side: command works) by adding the DX-side: command is documented; the bidirectional triangle is now dispatcher ↔ handler ↔ help] | ✓ shipped | R443 (tests/lint/test_osctl_dx_help_coverage.py + 12 L1 assertions covering osctl DX help-coverage; first §1h "high UX/DX" track lint) |
| E10.M86 | **§1h verbatim recording + R441 lint extended to cover §1h (the 6th /goal block — 'two ultimate solutions + perfectioning + high UX/DX')** — records §1h verbatim in SACROSANCT § 1 + extends `tests/lint/test_mandate_section_1_subsections.py` from 16→20 assertions. New assertions: §1h present with 2026-05-18 date stamp + operator-verbatim phrases preserved ('two ultimate solutions' / 'perfectioning' (operator-coined, NOT 'perfecting') / 'high UX/DX' / 'continue till you meet ALL' / 'REPROCESS' / 'Continue Endlessly') + §1h key-concretes name both targets (selfdef + sovereign-os) + UX/DX pairs User Experience AND Developer Experience (drift = prioritize one = mandate violation) + EXPECTED_SUBSECTIONS list updated to include §1h (R441's structural test now enforces 6 anchors instead of 5). **Cumulative SDD-037 family: 957 → 961 assertions** (62 lints still; R441 expanded from 16→20 assertions). [from /goal 6th block paste — operator's perpetual mandate continues with refined framing ('two ultimate solutions' explicitly names selfdef + sovereign-os duality; 'perfectioning' is operator-coined iterative refinement; 'high UX/DX' pairs UX + DX as first-class)] | ✓ shipped | R442 (mandate doc §1h subsection recorded + test_mandate_section_1_subsections.py extended +4 assertions; EXPECTED_SUBSECTIONS now includes §1h) |
| E10.M85 | **Operator-mandate § 1 subsection chain coverage lint + §1g content invariants (extension of R436 meta-pinning)** — extends R387-R440 + R436 operational-artifact pinning to `docs/standing-directives/2026-05-17-operator-mandate.md` § 1 SUBSECTION chain. `tests/lint/test_mandate_section_1_subsections.py` (16 assertions): mandate doc exists + § 1 present + ≥5 subsections (drift below = §1 subsection deleted = anti-corruption violation) + expected anchors present (§1.0, §1a, §1b, §1f, §1g — drift = operator paste not recorded) + §1g header has 2026-05-18 date stamp + **§1g operator-verbatim quote integrity** (10 phrase fragments: 'very clear and well defined' / 'proxy nginx' / 'master dashboard' / '300+ modules' / 'Nemotron 3' / 'global history' / 'bashrc' / 'Opnsense' / 'Sharevdi' / 'i226-V' — all SACROSANCT) + §1g hardware specs preserved (J3710/N3710 CPU + 2.5GbE NIC — operator-named Sharevdi mini PC) + §1g auth tier ladder complete (no/basic/advanced/social/enterprise) + §1g multi-surface enumeration complete (CLI/TUI/API/MCP/Dashboard/Web App/Service) + **Epic E11 section present** (§1g decomposition target — drift = decomposition lost) + E11 has ≥10 Modules + E11.M ids well-formed + E11.M ids unique + E11.M sequence has no gaps + §1g has key-concretes subsection (operator-discoverable decomposition surface) + § 1 subsections in chronological order (§1.0 first, §1g latest at this point — drift = chain reordered). **Cumulative SDD-037 family: 941 → 957 assertions** (62 lints now). [from /goal §1g paste — R436 covered mandate doc invariants broadly; R441 adds CONTENT-level coverage for § 1's subsection chain (the operator-verbatim accumulator). Each /goal block lands as a new subsection (§1.0, §1a-§1g now; future: §1h+). The chain MUST stay chronological + verbatim + decomposed into Epic Modules. This is the highest-leverage meta-lint: it catches drift where a /goal block was pasted but never recorded.] | ✓ shipped | R441 (tests/lint/test_mandate_section_1_subsections.py + 16 L1 assertions covering § 1 subsection chain + §1g verbatim content invariants + Epic E11 decomposition presence) |
| E10.M84 | **R440 milestone — verbatim-preservation arc consolidation handoff refresh (86 rounds, R355-R440)** — refreshes `docs/handoff/006-verbatim-preservation-arc.md` to cover R431-R439 (10 more rounds since R430 milestone). Final state at R440: **61 L1 lints / 941 assertions / 30 bugs caught**, 45+ operational artifacts pinned (R387-R439), **18 bidirectional-consistency lints** (added R433 selfdef-tune↔build-bitnet), Catalogue-source pinning COMPLETE (architecture-qa + coverage-map + selfdef bridge), Doc-family pinning COMPLETE (handoffs + SDDs + decisions + mandate meta-pin). 1450 tests green. 217+ mandate rows / ~178KB. Grade A stable across 86 rounds. New R431-R439 extensions table added documenting each round's surface + lint count. R431-R439 pivoted from build/runtime infrastructure to CATALOG SOURCES (R431+R432) + INTELLIGENCE VERBS (R434) + DOC-FAMILY pinning (R435+R438+R439) + META-PINNING (R436). The arc itself stays open per operator's perpetual mandate — R440 closes another 10-round documentation consolidation phase. [from /goal re-process — R400 + R430 milestones consolidated R355-R429; R440 consolidates R431-R439 the same way; the arc's growth slowed from infrastructure (large surfaces) to catalog/doc-family (more focused surfaces) but kept shipping per perpetual mandate] | ✓ shipped | R440 (handoff 006 refresh + R431-R439 consolidation summary) |
| E10.M83 | **decisions.md audit-trail content + format lint (operator-named append-only D-NNN catalog)** — extends R387-R438 + R384 operational-artifact pinning to `docs/decisions.md` + `docs/src/decisions.md` (mdbook summary mirror). `tests/lint/test_decisions_log_content.py` (18 assertions): canonical decisions.md exists + mdbook summary mirror exists + documents append-only contract (drift = format docs lost = future agents edit-in-place) + documents D-NNN/YYYY-MM-DD format template + documents two-artifact pattern (SDD Q-X rows annotated + new D-NNN appended) + **≥15 D-NNN entries** (operator-named 18+ floor; accidental-deletion detection) + D-NNN well-formed (no D-NN.5 drift) + D-NNN unique + D-NNN strictly sequential 1..N (operator-named no-gap policy) + every entry has YYYY-MM-DD date stamp (chronological anchor) + every entry has Decision: field + ≥70% have Question: field (SDD Q-X verbatim capture) + ≥70% have Source: field (operator-discoverable cross-reference to SDD) + ≥50% have Reversibility: field + Reversibility values in known set ({fully-reversible, partial, locked} + legacy {high, medium, low, reversible, irreversible}) + **decisions ↔ SDDs cross-reference**: every docs/sdd/NNN-... cited in Source: line MUST exist on disk (phantom-source detection) + ## Open questions section present (operator-discoverable unresolved Q-NNN catalog) + mdbook summary mirror references canonical decisions.md (chain integrity). **Cumulative SDD-037 family: 923 → 941 assertions** (61 lints now). [from /goal re-process — R384 covered handoff INDEX bidirectional; R438 covered SDD content; R439 pins the decisions audit trail — the OPERATOR-NAMED chronological record of D-NNN resolutions; the cross-reference check from decisions → SDD catches phantom-source drift where decisions cite SDDs that no longer exist] | ✓ shipped | R439 (tests/lint/test_decisions_log_content.py + 18 L1 assertions covering D-NNN append-only audit trail + cross-reference to SDDs) |
| E10.M82 | **SDD content + INDEX extended invariants lint (CONTENT-level pinning beyond the pre-existing filename↔INDEX bidirectional)** — extends R387-R437 + R384 + the pre-existing `tests/lint/test_sdd_index_consistency.py` (3 assertions covering filename↔INDEX bidirectional already) operational-artifact pinning to `docs/sdd/*.md` (39 SDD documents + INDEX.md). `tests/lint/test_sdd_content_extended.py` (15 assertions): ≥30 SDDs (operator-named 38+ floor — accidental-deletion detection) + numbering gaps ≤5 (drift = missed renumber) + no duplicate SDD numbers + every SDD has H1 title + H1 ≥10 chars substantive + every SDD ≥500 chars (drift to stub) + every SDD has ≥2 ## sections (operator-discoverable structure) + INDEX has purpose/numbering policy header + INDEX documents 'three-digit zero-padded never recycled' policy (operator-named numbering rule) + INDEX has Status column (operator-discoverable accepted vs review) + **SDD-037 anchor present** (verbatim-preservation arc — this lint family's codified SDD) + SDD-037 documents verbatim/preservation/NO REPHRASING doctrine + filename pattern NNN-lower-hyphenated.md (no NNN_Snake_Case or CamelCase drift) + **SDD-000 charter present** (operator-named project foundation) + charter documents mission/Debian/doctrine. **Cumulative SDD-037 family: 908 → 923 assertions** (60 lints now — MILESTONE: 60 L1 LINTS). [from /goal re-process — R384 covered handoff INDEX bidirectional; pre-existing test_sdd_index_consistency.py covers SDD filename↔INDEX; R438 adds CONTENT-level invariants completing the SDD-doc-family contract; SDD-037 (this lint family's own doctrine) + SDD-000 (charter) anchor checks catch drift where the foundation docs get deleted or stubbed] | ✓ shipped | R438 (tests/lint/test_sdd_content_extended.py + 15 L1 assertions covering SDD content invariants + SDD-037/SDD-000 anchor checks; 60-LINT MILESTONE) |
| E10.M81 | **Selfdef cross-repo bridge surface lint (operator-named SD-R<N> integration points)** — extends R387-R436 + R418/R433 operational-artifact pinning to the operator-named selfdef cross-repo integration: `scripts/sovereign-osctl` + `scripts/models/selfdef-models.py` + `docs/sdd/031-mcp-aggregate.md`. `tests/lint/test_selfdef_cross_repo_bridge.py` (17 assertions): osctl references selfdefctl CLI + **guards selfdefctl availability** (command -v selfdefctl >/dev/null 2>&1 — drift = crash on hosts without selfdef installed) + SD-R19 round reference (selfdefctl hardware tune --format env-file) + SD-R94 / selfdef-mcp reference (MCP TCP transport) + --upstream-selfdef host:port flag in mcp-aggregate manifest + selfdef-models.py exists (R182/R183 bridge) + documents SD-R34 model-registry + supports list + check-hardware subcommands + SDD-031 MCP aggregate doc exists + documents SD-R94 + SD-R98 @selfdef_macro registry + 'namespace: selfdef' tool surface + **SD-R<N> reference well-formedness** (SD-R followed by 1-3 digits) + **no fabricated SD-R<N>** (cap ≤200; drift to SD-R9999 = fabrication) + sovereign-osctl help documents mcp-aggregate verb + **no hardcoded clone-relative paths** (no /cyberpunk042/selfdef + no ../selfdef/ + no /home/<user>/selfdef/ — /etc/selfdef + /var/lib/selfdef are operator-named system paths, allowed). **Cumulative SDD-037 family: 891 → 908 assertions** (59 lints now). [from /goal re-process — R418/R433 covered the selfdef-tune lib bridge specifically; R437 covers the BROADER cross-repo binding surface across multiple sovereign-os files; the SD-R fabrication check + path-relativity check catch two distinct drift modes — fabricated round numbers AND hardcoded clone-path assumptions that break operator's actual layout] | ✓ shipped | R437 (tests/lint/test_selfdef_cross_repo_bridge.py + 17 L1 assertions covering selfdef cross-repo integration points + SD-R<N> hygiene + path-relativity) |
| E10.M80 | **Operator-mandate doc structural invariants lint (meta-pinning the doc that records every E.M row)** — extends R387-R435 + R367 operational-artifact pinning to `docs/standing-directives/2026-05-17-operator-mandate.md` itself (the META document the SACROSANCT § 6 forbids deletion / rewrite of operator verbatim text). `tests/lint/test_operator_mandate_doc_invariants.py` (15 assertions): mandate doc exists + has sections § 1 / § 4 / § 5 / § 6 (operator-named 6-section structure) + § 1 labeled 'verbatim, sacrosanct' + Epic 10 (verbatim-preservation arc Epic) present + at least 60 E.M rows accumulated (accidental-deletion detection) + every E.M id well-formed (E<N>.M<N> — no drift to E10:M01 etc.) + no duplicate E.M ids (drift = same Module shipped twice = inflated count) + E10.M ids sequential within reasonable gaps (≤10) + **§ 6 anti-corruption invariants documented** (NO rewriting operator verbatim, NO deleting TODO Modules, new Modules quote operator-verbatim source) + § 4 documents round-ID format rules (operator-discoverable: how to cite rounds in commits) + **operator verbatim quote integrity (SACROSANCT)**: 'continue till you meet ALL MY REQUIREMENTS' verbatim phrase present + 'REPROCESS' (from 'RETURN REREAD ALL THE RAW DUMP AND REPROCESS') + 'continue endlessly' framing + 'Do not minimize' phrase + recent R-arc rounds (R420/R425/R430) recorded as E.M rows (drift = round shipped without recording) + file ≥50KB (accidental truncation detection). **Cumulative SDD-037 family: 876 → 891 assertions** (58 lints now). [from /goal re-process — this is meta-pinning: the lint that protects the doc that records the rounds; § 6 anti-corruption invariants are operator-named SACROSANCT and a future agent rewriting them = mandate violation; the operator verbatim quote integrity check catches drift where someone rephrases the 'continue till you meet ALL MY REQUIREMENTS' text = SACROSANCT violation] | ✓ shipped | R436 (tests/lint/test_operator_mandate_doc_invariants.py + 15 L1 assertions meta-pinning the mandate doc itself) |
| E10.M79 | **Handoff document content contract lint (R384 extension — content invariants for the 6-handoff arc)** — extends R387-R434 + R384 operational-artifact pinning to `docs/handoff/*.md` (6 handoff documents + INDEX.md). `tests/lint/test_handoff_docs_content.py` (16 assertions): handoff dir exists + INDEX.md exists + all 6 expected handoffs present + handoff count ≥ 6 (operator-named arc; drift = deletion) + every handoff has H1 title in first 5 lines + H1 title ≥10 chars substantive (operator-discoverable identity — 004 is legitimately titled 'Operator Friction Audit' rather than 'Handoff' verbatim, allowed) + every handoff ≥500 chars substantive (drift to stub = no useful cold-start signpost) + every handoff has ≥2 ## sections (operator-discoverable cold-start anchors) + **handoff 006 specifics**: title references 'Verbatim' + documents perpetual mandate ('continue endlessly') + has 'Final state' section (cumulative numbers operator-discoverable) + **R384 bidirectional check reinforcement**: INDEX.md references every handoff file by name + has format template (operator-discoverable: how to write new handoff) + lists handoff-landing criteria (when to write: stage gates / session anchors / cross-repo arcs) + **supersedes chain integrity**: every handoff (002+) references its predecessor either via INDEX 'Supersedes' column OR 'Supersedes:' line in body (drift = chain has gaps) + no-orphan check (extra handoffs beyond expected ≤ 3 — forward-compat). **Cumulative SDD-037 family: 860 → 876 assertions** (57 lints now). [from /goal re-process — R384 covered the BIDIRECTIONAL filename↔INDEX check; R435 extends to handoff document CONTENT invariants (each handoff must be substantive cold-start signpost); the supersedes chain check catches drift where a handoff is added without linking back to its predecessor = next-session agent loses arc context] | ✓ shipped | R435 (tests/lint/test_handoff_docs_content.py + 16 L1 assertions covering handoff content contract + supersedes chain integrity) |
| E10.M78 | **Operator-pull intelligence verbs contract lint (doctrine-status + quarterly-review + morning-brief + next-action-advisor)** — extends R387-R433 + R382/R386 operational-artifact pinning to the 4 operator-pull intelligence verbs in `scripts/intelligence/`. `tests/lint/test_intelligence_verbs_contract.py` (17 assertions): all 4 verbs exist + python3 shebang + non-trivial module docstring (operator-discovery: WHY this verb exists) + **doctrine-status specific**: R376 / SDD-037 round provenance documented + invokes pytest (its core purpose is to wrap pytest for operators) + targets tests/lint/ path + has status + tally + run subcommands + **cross-verb invariants**: all 4 use argparse + all support --json output (fleet aggregation) + --human output (terminal display) + document exit codes (operator-discoverable failure-mode surface) + main() entry function + __name__ == '__main__' dispatch (operator runs as script) + operator-overlay TOML support (SDD-030 verbatim — operator can extend verb with custom config at /etc/sovereign-os/<verb>.toml) + **bidirectional consistency with sovereign-osctl R413**: doctrine-status verb dispatched by osctl wrapper (drift = verb exists but not invocable via operator-facing CLI). **Cumulative SDD-037 family: 843 → 860 assertions** (56 lints now). [from /goal re-process — R413 covered the sovereign-osctl dispatcher; R434 pins the 4 operator-pull intelligence verbs that osctl invokes; each verb is the operator-discoverable surface for one dimension (doctrine health / quarterly state / daily orient / next-action); drift in CLI shape silently breaks fleet aggregation + operator orientation flow] | ✓ shipped | R434 (tests/lint/test_intelligence_verbs_contract.py + 17 L1 assertions covering 4 operator-pull intelligence verbs + bidirectional consistency with osctl dispatcher) |
| E10.M77 | **selfdef-tune.sh cross-repo bridge detailed contract lint + 18th bidirectional-consistency lint (lib exports ↔ build-bitnet.sh consumer reads)** — extends R387-R432 + R418/R428 operational-artifact pinning to `scripts/build/lib/selfdef-tune.sh` (cross-repo flag-derivation loop). `tests/lint/test_selfdef_tune_lib_contract.py` (22 assertions): file exists + header documents cross-repo bridge + **operator-named 3-source preference order** (selfdefctl → capabilities.json → native fallback) + references selfdefctl CLI + SD-R19 round provenance + /var/lib/selfdef/hardware-capabilities.json path + SD-R10 round provenance + native-fallback path (drift = lib fails on minimal hosts) + **function contract**: selfdef_tune_load() public entry + selfdef_tune__try_selfdefctl + try_capabilities_json + fallback_native helpers + **exported env vars**: SELFDEF_HARDWARE_MARCH + CFLAGS + KCFLAGS + AVX512_VNNI + AVX512_BF16 + TUNE_SOURCE marker + WASM_AOT forward-compat (SD-R30) + idempotency documented (drift = repeated calls clobber operator-set values) + set_default helper respects pre-existing vars (caller-wins) + **18th bidirectional-consistency lint**: build-bitnet.sh (R428 consumer) reads SELFDEF_HARDWARE_MARCH + CFLAGS — both must agree on var names (BIDIRECTIONAL: rename in lib breaks consumer silently) + consumer (build-bitnet.sh) sources selfdef-tune.sh (drift = consumer ignores operator-tuning) + robustness: handles missing selfdefctl gracefully (command -v + return 1) + handles empty output gracefully (-z empty check + rc capture). **Cumulative SDD-037 family: 821 → 843 assertions** (55 lints now). **18 bidirectional-consistency lints**: R367 / R373 / R384 / R399 / R403 / R410 / R412 / R417 / R419 / R420 / R421 / R422 / R424 / R425 / R426 / R427 / R428 / R433. [from /goal re-process — R418 covered build/lib/* infrastructure broadly; R428 covered the build-bitnet.sh consumer; R433 closes the DETAILED CONTRACT of the cross-repo bridge that those two depend on; the 3-source preference order is operator-named and any drift breaks downstream callers silently] | ✓ shipped | R433 (tests/lint/test_selfdef_tune_lib_contract.py + 22 L1 assertions + 18th bidirectional-consistency lint covering selfdef-tune lib exports ↔ build-bitnet consumer reads) |
| E10.M76 | **coverage-map.py 32-axis demand-coverage catalog source contract lint** — extends R387-R431 + R423/R431 operational-artifact pinning to `scripts/intelligence/coverage-map.py` (32 operator-named demand axes A-01..A-32). `tests/lint/test_coverage_axes_catalog.py` (18 assertions): file exists + module exports DEFAULT_AXES (R423 aggregator looks up this name) + ≥30 axes (operator-named 32-axis matrix; drift = shrinkage) + every axis has id + A-NN slug pattern + IDs unique + IDs strictly sequential A-01..A-NN (drift = coverage holes in matrix display) + every axis has axis_verbatim ≥10 chars (operator-verbatim demand text NO REPHRASING) + every axis has source citation + status in known set ({shipped, partial, TODO, ✓ shipped, deferred, in-flight}) + implementing_verbs + sdd_refs + mandate_rows initialized as lists (operator-discoverable structural slots) + **shipped axes have evidence** (at least one of implementing_verbs/sdd_refs/mandate_rows; drift = false confidence in coverage) + sdd_refs match SDD-NNN or bare NNN format + mandate_rows match E.M / E.MN / SD-RN operator-named format + **≥50% axes shipped** (operator-named coverage floor; drift = matrix slipping toward TODO majority) + at least one axis sourced from master spec § (L0 dump binding) + at least one axis has implementing_verbs (operator drillthrough surface) + module documents operator-verbatim contract in header. **Cumulative SDD-037 family: 803 → 821 assertions** (54 lints now). [from /goal re-process — R431 covered the architecture-qa concept catalog; R432 covers the SISTER catalog (32 demand axes); together they form the operator-pull discovery surface that R423 aggregates; the shipped-axes-have-evidence check catches drift where status flips to shipped without any concrete proof] | ✓ shipped | R432 (tests/lint/test_coverage_axes_catalog.py + 18 L1 assertions pinning the 32-axis demand-coverage matrix) |
| E10.M75 | **architecture-qa.py catalog source contract lint (the 27 C-NN concepts + Q-NN questions + gotchas master catalog that R423 aggregates)** — extends R387-R430 + R423 operational-artifact pinning to `scripts/intelligence/architecture-qa.py` (1396 lines — operator-named § 13-17 master catalog). `tests/lint/test_architecture_qa_catalog.py` (21 assertions): file exists + module exports ARCHITECTURE_QUESTIONS + ARCHITECTURE_GOTCHAS + ARCHITECTURE_CONCEPTS (R423 aggregator depends on these names) + ≥20 concepts (operator-named 27-entry catalog floor; drift = shrinkage) + every concept has id + C-NN slug pattern + IDs unique (drift = R423 emits duplicate items) + every concept has name + non-trivial explanation (≥20 chars; drift = inert catalog entry) + spec_ref present + spec_ref anchors to master spec §, macro-arc plan, operator hook drop, or operator dump (operator-discovery context) + tags non-empty (searchable index) + Q-NN questions have id/question/answer/spec_ref + Q-NN slug pattern + G-NN gotchas have id/name + **operator-verbatim section coverage**: at least one concept references § 15 (1-Bit Paradigm / ternary) + § 16 (AVX-512 Hardware Fusion) + § 17 (Genesis Trinity) + **NO REPHRASING contract**: module header documents 'NO REPHRASING' or 'verbatim' (drift = future agent rephrases operator-exact text) + per-entry binding contract documented (id + tags + spec_ref) + spec_ref hygiene: no §N where N>30 (catches fabricated section numbers; master spec caps ~§23) + no empty explanations + answers >20 chars (substantive operator-verbatim). **Cumulative SDD-037 family: 782 → 803 assertions** (53 lints now — MILESTONE: 800+ assertions). [from /goal re-process — R423 covered the verbatim-render AGGREGATOR; R431 covers the CATALOG SOURCE itself; the 27 operator-named C-NN concepts span § 13-17 master spec content + macro-arc plan + operator hook drop; drift in any entry = R423 emits silently shrunken surface to operator] | ✓ shipped | R431 (tests/lint/test_architecture_qa_catalog.py + 21 L1 assertions pinning the 27-concept + 4-question + 3-gotcha master catalog; 800-ASSERTION MILESTONE) |
| E10.M74 | **R430 milestone — verbatim-preservation arc consolidation handoff refresh (76 rounds, R355-R430)** — refreshes `docs/handoff/006-verbatim-preservation-arc.md` to cover R401-R429 (30 more rounds since R400 milestone). Final state at R430: **52 L1 lints / 782 assertions / 30 bugs caught**, 30+ operational artifacts pinned (R387-R429), 4 Trinity-side systemd Descriptions pinned (R397), **17 bidirectional-consistency lints** (R367/R373/R384/R399/R403/R410/R412/R417/R419/R420/R421/R422/R424/R425/R426/R427/R428), Trinity-side pinning COMPLETE (Pulse + Weaver + Auditor + ZFS + VFIO + Tetragon + service-script duality across Tetragon/Guardian/Weaver). 1291 tests green. 207+ mandate rows / ~165KB. Grade A stable across 76 rounds. New R401-R429 extensions table added documenting each round's surface + bugs caught (10 bugs total across 7 rounds). The arc itself stays open per operator's perpetual mandate — R430 just closes the documentation consolidation phase. State preservation: full R355-R430 arc state accessible from handoff doc 006 for next-session cold-start. [from /goal re-process — R400 consolidated R355-R399 documentation; R430 consolidates R400-R429 the same way; the arc's bidirectional-consistency-lint family is the highest-value pattern, growing from 4 (at R400) to 17 (at R430)] | ✓ shipped | R430 (handoff 006 refresh + 30-round consolidation summary) |
| E10.M73 | **Debian preseed Phase-I install entry surface + § 8 ZT + sovereignty + bidirectional consistency with cloud-init R414** — extends R387-R428 + R414/R425 operational-artifact pinning to `config/preseed/sain-01.preseed.example.cfg` (operator-named alternate install entry — net-install / interactive media alternative to cloud-init NoCloud). `tests/lint/test_preseed_content_verbatim.py` (20 assertions): preseed file exists + en_US.UTF-8 locale (matches cloud-init R414) + UTC clock (reproducibility) + hostname sain-01 + **§ 8 ZT verbatim**: passwd/root-login=false (drift = root login enabled) + creates operator user (matches cloud-init account name) + allow-password-weak=false (operator-named strength gate) + initial password placeholder marker (operator MUST replace) + **sovereignty deny-list**: popularity-contest participate=false (phone-home opt-out) + apt-setup/enable-source-repositories=false + **bootloader**: grub to /dev/nvme0n1 (SAIN-01 NVMe target) + only_debian=true + **package selection**: ssh-server task (remote-management surface) + pkgsel/upgrade=none (deterministic install) + **bidirectional consistency with cloud-init R414 (4 invariants)**: (1) late_command writes /etc/sovereign-os/active-profile=sain-01 (drift = installed system has no profile); (2) late_command writes active-whitelabel=default; (3) late_command writes SOVEREIGN_OS_PROFILE=sain-01 env (downstream sovereign-osctl); (4) late_command enables sovereign-firstboot.target (drift = ConditionFirstBoot=yes hooks never fire) + operator-discoverable usage docs in header + canonical Debian preseed reference link. **Cumulative SDD-037 family: 762 → 782 assertions** (52 lints now). [from /goal re-process — R414 covered cloud-init NoCloud entry path; R425 covered the § 12 5-phase pipeline; R429 pins the ALTERNATE install entry — both must converge on the same post-install state (active-profile + active-whitelabel + firstboot.target enabled); the cross-entry bidirectional check catches drift where preseed and cloud-init diverge silently = installed system differs depending on install path] | ✓ shipped | R429 (tests/lint/test_preseed_content_verbatim.py + 20 L1 assertions covering preseed Phase-I + § 8 ZT + sovereignty + cloud-init bidirectional consistency) |
| E10.M72 | **Pulse runtime build script (bitnet.cpp) § 15-16 + § 17.1 verbatim contract + 17th bidirectional-consistency lint (build script BITNET_MODEL_REPO ↔ bitnet.py DEFAULT_MODEL ↔ model catalog hf_repo_id ↔ start-pulse.sh model id)** — extends R387-R427 + R394/R404/R427 operational-artifact pinning to `scripts/pulse/build-bitnet.sh` (Stage-2 operator-named Pulse build). `tests/lint/test_pulse_build_bitnet_contract.py` (20 assertions): file exists + executable + 'set -euo pipefail' + documents § 15-16 (1-Bit Paradigm + AVX-512 Fusion) + § 17.1 Pulse Module 1 + **§ 16 verbatim compile flags**: -march=znver5 (operator-named Zen 5; drift to znver4/generic = lost CPU-specific optimization) + AVX-512 family flags (-mavx512f/dq/bw/vl/bf16/fp16) + avx512_vnni runtime check (operator-discoverable warning when missing; drift = silent degradation) + operator-discoverable env contract: SOVEREIGN_OS_DRY_RUN + BITNET_SKIP_MODEL + BITNET_SKIP_BUILD env vars + /usr/local default install prefix (FHS) + sovereign_os_pulse_build_total metric (SDD-016) + last_run_timestamp gauge (staleness) + idempotency check (operator-named: re-runs are idempotent) + **17th bidirectional-consistency lint (4-way)**: (1) build BITNET_MODEL_REPO default ↔ bitnet.py DEFAULT_MODEL (drift = build fetches one model, runtime serves another); (2) build default ↔ models/catalog.yaml hf_repo_id for Pulse model (drift = build fetches model not in catalog); (3) build default ↔ start-pulse.sh PULSE_MODEL slug ('bitnet-b1.58-2B-4T' in both) + checks git + cmake + make + g++ prerequisites (operator-discoverable preflight) + uses --depth 1 shallow clone (drift = wasted bandwidth). **Cumulative SDD-037 family: 742 → 762 assertions** (51 lints now). **17 bidirectional-consistency lints**: R367 / R373 / R384 / R399 / R403 / R410 / R412 / R417 / R419 / R420 / R421 / R422 / R424 / R425 / R426 / R427 / R428. [from /goal re-process — R394 covered the script's verbatim header content; R404 pinned the BitnetBackend.DEFAULT_MODEL; R427 pinned the model catalog; R428 closes the 4-way ring across build-script + backend-adapter + catalog + start-script ensuring all 4 agree on the operator-named default BitNet model; § 16 verbatim AVX-512 compile flags pinned at push-time] | ✓ shipped | R428 (tests/lint/test_pulse_build_bitnet_contract.py + 20 L1 assertions + 17th bidirectional-consistency lint covering 4-way default-model ring) |
| E10.M71 | **Model catalog content lint + 16th bidirectional-consistency lint (catalog tiers ↔ § 17.1 Trinity set + verified-real ↔ hf_repo_id presence)** — extends R387-R426 + R404/R422 operational-artifact pinning to `models/catalog.yaml` (17-model Genesis Trinity catalog). `tests/lint/test_model_catalog_content.py` (20 assertions): catalog YAML exists + schema_version 1.x family + at least one model per Trinity tier (pulse + logic + oracle MUST be populated; drift = tier has no model = workload profiles can't reach it) + **16th bidirectional-consistency lint**: (1) every model's tier in known {pulse, logic, oracle, router} set (drift = unreachable from workload + router); (2) every status=verified-real entry has hf_repo_id (drift = catalog claims real-world binding it doesn't have = false confidence) + every model has id+tier+class+quantization+size_class+purpose+status fields (operator-discoverable shape) + class in known taxonomy set {llm, slm, rlm, ternary-lm, lora-adapter, embed, vision, multimodal, code, mixture, speculative, reranker} + status in {verified-real, aspirational, operator-must-confirm} + size_class in {xs, s, m, l, xl, xxl} + lora-adapter class entries have base_model (drift = orphan adapter) + **§ 17.1 Pulse operator-named content**: pulse tier has BitNet model + ternary-lm class + ternary-1.58bit quantization (drift = Pulse loses its operator-named compute optimization) + **§ 17.3 Oracle**: at least one large (l/xl/xxl) model (Blackwell handles deep reasoning) + catalog references sovereign-osctl models command surface (operator-discovery) + docs/src/model-catalog.md rendered artifact contains all Trinity tiers (stale-render detection). **Cumulative SDD-037 family: 722 → 742 assertions** (50 lints now — MILESTONE: 50 L1 LINTS). **16 bidirectional-consistency lints**: R367 / R373 / R384 / R399 / R403 / R410 / R412 / R417 / R419 / R420 / R421 / R422 / R424 / R425 / R426 / R427. [from /goal re-process — R404 pinned the backend ADAPTER classes; R422 pinned the runtime workload PROFILES; R427 pins the MODEL CATALOG that those layers reference; the verified-real ↔ hf_repo_id bidirectional check catches false-confidence drift where catalog claims a model is real without HF Hub evidence] | ✓ shipped | R427 (tests/lint/test_model_catalog_content.py + 20 L1 assertions + 16th bidirectional-consistency lint covering catalog tiers ↔ Trinity set + verified-real ↔ hf_repo_id; 50-LINT MILESTONE) |
| E10.M70 | **whitelabel/default.yaml content lint + 15th bidirectional-consistency lint (whitelabel legal_floor ↔ render.py LEGAL_FLOOR_PATTERNS + template references ↔ on-disk files)** — extends R387-R425 + R388/R407 operational-artifact pinning to `whitelabel/default.yaml` (operator-named brand baseline). `tests/lint/test_whitelabel_default_yaml_content.py` (21 assertions): YAML file exists + default dir + templates dir exist + parses cleanly + schema_version 1.0.0 + **identity block**: id='sovereign-default' (composition reference; drift breaks profile-mixin resolution) + all required fields (id/name/version/status/maintainer/description) + maintainer='cyberpunk042' (operator-named attribution; drift = attribution erased) + compliance_target='dfsg-only' (SDD-006 baseline; drift relaxes floor) + **branding block**: all required fields (os_id/os_name/os_pretty_name/os_version/os_codename/vendor/home_url/bug_report_url/support_url/motd) + os_id='sovereign' (drift = wrong /etc/os-release ID downstream) + os_codename='trinity' (§ 17 Genesis Trinity binding) + vendor='cyberpunk042' (matches cloud-init ISO publisher R414) + all URLs reference cyberpunk042/sovereign-os (operator-discoverable canonical refs) + **15th bidirectional-consistency lint**: every template in surfaces[].template MUST exist in whitelabel/default/templates/ (drift = render engine fails at runtime on missing template) + 5 operator-named templates present (os-release/issue/motd/installer-welcome/dpkg-origins-sovereign) + os-release.tmpl has ${os_*} placeholder (drift to hardcoded = whitelabel can't override) + **legal_floor ↔ render.py bidirectional**: every path in legal_floor.preserved MUST appear in render.py LEGAL_FLOOR_PATTERNS (drift = YAML claims protection render engine doesn't enforce = SDD-006 violation slips through) + /etc/debian_version preserved + debian-logo* + debian-swirl* preserved + rationale documents Debian/DFSG/SDD-006. **Cumulative SDD-037 family: 701 → 722 assertions** (49 lints now). **15 bidirectional-consistency lints**: R367 / R373 / R384 / R399 / R403 / R410 / R412 / R417 / R419 / R420 / R421 / R422 / R424 / R425 / R426. [from /goal re-process — R388 covered surface content; R407 covered the render engine; R426 covers the BRIDGE YAML that the render engine consumes; the legal_floor ↔ render.py bidirectional check is operator-mandate-grade because SDD-006 § 'Legal floor' is sovereignty contract — drift between what whitelabel CLAIMS to preserve and what render engine ACTUALLY blocks = silent legal exposure] | ✓ shipped | R426 (tests/lint/test_whitelabel_default_yaml_content.py + 21 L1 assertions + 15th bidirectional-consistency lint covering whitelabel legal_floor ↔ render.py + templates ↔ on-disk files) |
| E10.M69 | **Bootstrap phases.yaml ↔ phases.sh + render-phases-md.py § 12 verbatim 5-phase chronological pipeline + 14th bidirectional-consistency lint (YAML ↔ shell consumer ↔ markdown renderer ↔ artifact paths)** — extends R387-R424 + R389/R410 operational-artifact pinning to `config/bootstrap/phases.yaml` + `scripts/bootstrap/phases.sh` + `scripts/bootstrap/lib/render-phases-md.py` + `docs/src/bootstrap-phases.md`. `tests/lint/test_bootstrap_phases_consistency.py` (19 assertions): all 3 source files exist + YAML has exactly 5 phases (§ 12 verbatim) + **14th bidirectional-consistency lint** (4-way): (1) YAML phase IDs match Roman-numeral [I, II, III, IV, V] AND phases.sh references each (drift = inventory tool misses phase); (2) YAML phase names match § 12 verbatim AND phases.sh references them (operator-discoverable inventory); (3) every artifact path in YAML resolves to a real file/dir on disk (drift = phases.sh inventory shows ✗ missing); (4) render-phases-md.py sources from phases.yaml (drift = doc generator hardcodes phases out of band) + every phase has id+name+description+preconditions+postconditions+artifacts (operator-discoverable shape) + every phase has non-empty artifacts/preconditions/postconditions (drift = operationally invisible / no gate / no result) + phase names match § 12 verbatim ('Minimal Trixie Base' / 'Zen 5 Kernel Compilation' / 'Storage Layer + DKMS (ZFS)' / 'Container + Network Edge Isolation' / 'Tetragon eBPF + Guardian + State Fabric Mount') + YAML documents § 12 + phases.sh quotes § 12 verbatim 'Each phase must be completed and validated before the downstream phase is initiated' + phase ordering strictly Roman I→V (chronological — drift breaks order operator-named for SAIN-01) + render-phases-md.py uses yaml.safe_load (security CVE) + rendered docs/src/bootstrap-phases.md exists + references § 12 + contains all 5 Roman numerals (stale-render detection). **Cumulative SDD-037 family: 682 → 701 assertions** (48 lints now — MILESTONE: 700+ assertions). **14 bidirectional-consistency lints**: R367 / R373 / R384 / R399 / R403 / R410 / R412 / R417 / R419 / R420 / R421 / R422 / R424 / R425. [from /goal re-process — R389 covered the bootstrap YAML schema lint, R410 covered the verify-grid ↔ verify.sh bidirectional check; R425 covers the analogous 4-way triangle for the § 12 5-phase pipeline; the artifact-paths-resolve check catches drift where YAML references a renamed/deleted file; § 12 chronological ordering is operator-named for SAIN-01 hardware bring-up] | ✓ shipped | R425 (tests/lint/test_bootstrap_phases_consistency.py + 19 L1 assertions + 14th bidirectional-consistency lint covering § 12 phases YAML ↔ shell consumer ↔ markdown renderer ↔ artifact paths; 700-ASSERTION MILESTONE) |
| E10.M68 | **JSON-schema set consistency lint + 13th bidirectional-consistency lint (schema file ↔ conformance test ↔ data files triangle)** — extends R387-R423 + R417/R422 operational-artifact pinning to `schemas/{mixin,profile,runtime-profile,whitelabel,model-catalog}.schema.yaml` (5-schema set). `tests/lint/test_schemas_consistency.py` (18 assertions): all 5 schemas exist + count matches + **every schema** declares $schema (JSON Schema dialect URI; drift = validator can't pick algorithm) + declares $id (cross-schema $ref resolution) + has title with 'sovereign-os' branding + root type=object + non-empty required[] list (drift = schema accepts anything) + requires schema_version (operator-named version pin) + properties{} block defines required fields + **13th bidirectional-consistency lint**: every schemas/<X>.schema.yaml MUST have matching tests/schema/test_<X>_schema_conformance.py validator test (drift = schema becomes documentation-only without runtime enforcement) + every conformance test references its schema by filename (drift = test validates wrong schema) + schema-specific contracts: profile.schema requires identity block + mixin.schema requires 'mixin' block (composition contract; drift = mixin.id-less files pass) + runtime-profile.schema requires runtime_profile block + whitelabel.schema requires identity block + **≥3 of 5 schemas use $defs** (operator pattern for shared types; drift to inline = loses cross-schema reuse) + profile↔mixin schema cross-reference (relative path) + **≥3 of 5 schemas set additionalProperties:false at root** (strict mode catches typos + unknown keys). **Cumulative SDD-037 family: 664 → 682 assertions** (47 lints now). **13 bidirectional-consistency lints**: R367 / R373 / R384 / R399 / R403 / R410 / R412 / R417 / R419 / R420 / R421 / R422 / R424. [from /goal re-process — R417/R422 pinned data files; R424 pins the schemas they validate against + the conformance-test triangle; the 13th bidirectional check catches drift where a schema is added/renamed but no validator test follows = schema is silently inert] | ✓ shipped | R424 (tests/lint/test_schemas_consistency.py + 18 L1 assertions + 13th bidirectional-consistency lint covering schema↔test↔data triangle) |
| E10.M67 | **verbatim-render catalog aggregator R369 doctrine contract lint** — extends R387-R422 + R381/R382 operational-artifact pinning to `scripts/intelligence/verbatim-render.py` (the central catalog aggregator pulling verbatim phrases from 6 source modules) + `docs/src/verbatim-surface.md` (the rendered artifact). `tests/lint/test_verbatim_render_contract.py` (22 assertions): render script exists + surface.md exists + **all 6 source modules referenced** (architecture-qa.py + coverage-map.py + ccd-pinning.py + state-fabric.py + network/topology.py + repl.py — drift dropping one = items silently disappear from manifest) + architecture-qa loads QUESTIONS + GOTCHAS + CONCEPTS + coverage loads DEFAULT_AXES + ccd loads DEFAULT_LAYER_CATALOG + state-fabric loads DEFAULT_FILE_MATRIX + DEFAULT_ZFS_PROPERTIES + network loads DEFAULT_INTERFACES + TOPOLOGY_DIAGRAM_VERBATIM + repl loads DEFAULT_MODES + **CLI subcommand contract**: render + summary + manifest all defined (drift renaming a subcommand = sovereign-osctl wrappers break) + render_markdown / render_summary / render_manifest functions exist + manifest entries carry sovereign-osctl architecture-qa + coverage verbs (operator-runnable surface) + --json/--human format flags supported + argparse-based CLI (operator-discoverable -h) + **NEVER-raises promise**: _gather_catalogs() documents 'NEVER-raises' + uses getattr(mod, ATTR, []) defaults (drift = AttributeError crashes whole render) + SCHEMA_VERSION + ROUND constants defined (consumer-tool compatibility + R-arc traceability) + surface.md non-empty (>1000 chars; regenerator-broken detection) + surface.md references master spec § (operator-discovery context preserved). **Cumulative SDD-037 family: 642 → 664 assertions** (46 lints now). [from /goal re-process — R381/R382 + earlier rounds populated the source catalogs; R423 pins the AGGREGATOR that pulls those catalogs into the operator-discoverable surface; the getattr-with-default pattern is operator-discoverable graceful-degradation; drift in any of the 6 source-module references = silent shrinkage of operator-visible verbatim surface] | ✓ shipped | R423 (tests/lint/test_verbatim_render_contract.py + 22 L1 assertions pinning verbatim-render aggregator's 6-source contract + CLI surface + NEVER-raises promise) |
| E10.M66 | **Runtime-profile YAMLs § 18 verbatim 3-mode Trinity workload contract + 12th bidirectional-consistency lint (runtime_profile.id↔filename + engines↔R404 backend adapters + hw_compat↔real profiles)** — extends R387-R421 + R404 operational-artifact pinning to `profiles/runtime/{ultra-sovereign-efficiency,deep-context-synthesis,high-concurrency-burst}.yaml`. `tests/lint/test_runtime_profiles_verbatim.py` (18 assertions): all 3 § 18 verbatim profiles exist + count matches expected + every profile has schema_version 1.0.0 + **12th bidirectional-consistency lint** (3-way): (1) runtime_profile.id MUST match filename stem (drift = workload-mode set fails silently); (2) every engine in allocations[].engine MUST be in known backend set {bitnet.cpp, vllm, vllm-vulkan, llama.cpp} = R404 adapter set (drift = workload references nonexistent backend); (3) every hardware_profile_compat value MUST be a real profile YAML in profiles/ (drift = workload advertises compat with nonexistent profile) + every allocation tier in § 17.1 Trinity set {pulse, logic, oracle} + **§ 18 Profile 1 verbatim (Ultra-Sovereign Efficiency / CPU Focused)**: tier=pulse only (drift = not CPU-only anymore) + engine=bitnet.cpp + core_mask starts at 0 (CCD 0 SRP — drift to '8-15' = CCD 1 = § 17.1 violation) + verbatim operator framing in comments ('continuous background state monitoring' / 'near-zero power draw') + **§ 18 Profile 2 verbatim (Deep-Context Synthesis / Oracle)**: tier=oracle + engine=vllm (matches R404 VllmBackend.for_oracle_core) + **§ 18 Profile 3 verbatim (High-Concurrency Burst / full Trinity)**: ALL 3 tiers engaged simultaneously (drift dropping any = not 'burst' anymore) + pulse uses ≥0- core baseline + every profile references master spec § 18 + every profile has description + every profile has ≥1 allocation (drift = silent no-op workload mode). **Cumulative SDD-037 family: 624 → 642 assertions** (45 lints now). **12 bidirectional-consistency lints**: R367 / R373 / R384 / R399 / R403 / R410 / R412 / R417 / R419 / R420 / R421 / R422. [from /goal re-process — R404 covered backend ADAPTER classes (BitnetBackend / VllmBackend / LlamaCppBackend); R422 covers the WORKLOAD MODE YAMLs that select which adapter+model combo to run; the 3-way bidirectional lint catches drift across YAML-filename-adapter-hardware boundary; § 18 verbatim 3-profile catalog is operator-named Trinity workload contract] | ✓ shipped | R422 (tests/lint/test_runtime_profiles_verbatim.py + 18 L1 assertions + 12th bidirectional-consistency lint covering runtime_profile.id↔filename + engines↔R404 adapters + hw_compat↔real profiles) |
| E10.M65 | **Guardian Loop core service↔script + § 10/§ 17 verbatim Auditor contract + 11th bidirectional-consistency lint (service ReadWritePaths ↔ script write paths + script socket ↔ Tetragon events socket)** — extends R387-R420 + R392/R397 operational-artifact pinning to `scripts/auditor/guardian-core.py` ↔ `systemd/system/sovereign-guardian-core.service`. `tests/lint/test_guardian_core_service_bidir.py` (20 assertions): both files exist + service has ExecStart= + ExecStart references 'guardian' binary (drift = unit runs wrong process) + **Requires=tetragon.service** (§ 10.2 verbatim — drift = guardian no-ops without event source) + **After=tetragon.service** (ordering — Requires alone doesn't enforce order) + **Restart=always** (§ 17 'always-on' Auditor — drift to on-failure = clean exit unguards perimeter) + RestartSec ≤ 5s (perimeter unguard window cap) + service documents master spec § 10 + **11th bidirectional-consistency lint**: script writes /mnt/vault/context/security_audit.log AND service ReadWritePaths MUST include /mnt/vault/context (ProtectSystem=strict blocks writes outside ReadWritePaths silently = audit log loss) + service references /var/run/tetragon (ReadOnlyPaths) + **script's GUARDIAN_SOCKET_PATH default = /var/run/tetragon/tetragon.events** matches Tetragon load hook's policy dir (drift between script's READ path and Tetragon's WRITE path = guardian tails non-existent file = silent perimeter no-op) + § 10 verbatim quote in script header ('lightweight, native Linux event supervisor' / 'autonomous circuit breaker' / 'Tetragon eBPF UNIX socket') + § 17 Trinity/Auditor naming + 'podman kill' neutralization (§ 10.1 verbatim — drift to docker breaks rootless) + appends to security_audit.log (§ 7.1 verbatim path) + GUARDIAN_DRY_RUN env support + **SDD-016 Layer B metrics**: sovereign_os_auditor_neutralization_total + event_parse_total + last_neutralization_timestamp (per-kill + parse-error + staleness detection surfaces). **Cumulative SDD-037 family: 604 → 624 assertions** (44 lints now). **11 bidirectional-consistency lints**: R367 / R373 / R384 / R399 / R403 / R410 / R412 / R417 / R419 / R420 / R421. [from /goal re-process — R392 covered guardian-core.py CONTENT pinning (§ 10/§ 17 verbatim); R397 covered Description= identity; R421 closes the SERVICE↔SCRIPT runtime invocation cycle + the bidirectional check between script's write paths and unit's ReadWritePaths catches a subtle ProtectSystem=strict trap where the unit's hardening silently swallows the script's writes; this is § 17 'Auditor (Immutable Gatekeeper)' contract preserved at push-time] | ✓ shipped | R421 (tests/lint/test_guardian_core_service_bidir.py + 20 L1 assertions + 11th bidirectional-consistency lint covering guardian service↔script paths) |
| E10.M64 | **Weaver atomic-state ↔ tank/context dataset 10th bidirectional-consistency lint + § 21 verbatim state-fabric durability chain** — extends R387-R419 + R393/R396 operational-artifact pinning to `scripts/weaver/atomic-state.py` ↔ `profiles/sain-01.yaml` tank/context dataset. `tests/lint/test_weaver_atomic_state_bidir.py` (16 assertions): atomic-state.py file exists + sain-01 declares tank/context dataset + **10th bidirectional-consistency lint**: WRITER (atomic-state.py header) assumes 'tank/context has sync=always' AND STORAGE (sain-01.yaml) MUST declare sync=always on tank/context (drift = writer assumes durability storage doesn't provide = silent power-loss data loss window) + tank/context copies=2 verbatim (state-fabric durability — drift to copies=1 = single-bit-rot corrupts) + **bidirectional 4-state-fabric-file consistency**: IDENTITY.md + SOUL.md + AGENTS.md + CLAUDE.md MUST match between writer STATE_FILES tuple AND master spec § 7.1 verbatim list + STATE_FILES tuple has exactly 4 entries (drift = recordsize/copies tuning was sized for 4 files) + **§ 21.1 verbatim writer semantics**: O_DIRECT (bypass page cache; drift = writes sit in volatile page cache) + O_SYNC (synchronous commit) + O_TRUNC (drift = partial overwrite on short writes) + os.rename atomic commit (load-bearing § 21.1 primitive; drift = readers see partial state) + 4K alignment (NVMe physical block — drift = O_DIRECT rejects EINVAL on strict filesystems) + O_DIRECT→O_SYNC fallback for tmpfs/overlayfs (drift = tests/containers fail on every write) + sovereign_os_weaver_atomic_write_total counter (SDD-016) + sovereign_os_weaver_atomic_write_last_timestamp gauge per file (staleness detection) + master spec § 21 reference in header + /mnt/vault/context default path (operator-named state-fabric mount; drift = writes land off tank/context = no sync=always benefit) + WEAVER_DRY_RUN env support. **Cumulative SDD-037 family: 588 → 604 assertions** (43 lints now — MILESTONE: 600+ assertions). **10 bidirectional-consistency lints**: R367 / R373 / R384 / R399 / R403 / R410 / R412 / R417 / R419 / R420. [from /goal re-process — R393 covered atomic-state.py verbatim § 21.1 semantics, R396 covered ZFS dataset spec, R420 closes the BIDIRECTIONAL CONSISTENCY between the writer and the storage layer it writes to; drift between WRITER assumption and STORAGE provision = silent state-fabric corruption window on power loss; the 10th bidirectional lint is operator-mandate-grade because state-fabric durability is operator-named § 21 'lockless loopback write sequence on the ZFS layer'] | ✓ shipped | R420 (tests/lint/test_weaver_atomic_state_bidir.py + 16 L1 assertions + 10th bidirectional-consistency lint; 600-ASSERTION MILESTONE) |
| E10.M63 | **Tetragon policy LOAD + VERIFY operator-verbatim §5 + E104 contract + 9th bidirectional-consistency lint (policy name+dir LOAD↔VERIFY)** — extends R387-R418 + R390 operational-artifact pinning to `scripts/hooks/post-install/tetragon-policy-load.sh` + `scripts/hooks/recurrent/tetragon-policy-verify.sh`. `tests/lint/test_tetragon_policy_load_verbatim.py` (22 assertions): both hooks exist + **LOAD side operator-verbatim**: apiVersion=cilium.io/v1alpha1 (Tetragon CRD; drift breaks policy load) + kind=TracingPolicy + policy name=sovereign-kernel-fence (operator-named §5) + __x64_sys_execve kprobe (operator-named §5 — drift = no execve perimeter) + 4-binary allowlist (python3 + nvidia-smi + vllm + podman — R390 verbatim set) + Sigkill action (drift to log-only = weakened perimeter from kill→record) + PID 1 exclusion (drift = killing PID 1 reboots host) + followForks:true (drift = fork-spawned children bypass) + /etc/tetragon/tracing-policies install path + require_root + tetragon service restart (drift = file written but daemon serves old policy) + per-result metric + **VERIFY side**: sovereign_os_perimeter_status gauge (1=healthy 0=drift; SDD-016 verbatim) + sovereign_os_perimeter_verify_last_run_timestamp ('verifier overdue' detection) + tetragon-not-installed check + systemctl is-active check + security_audit.log write on drift + audit log on /mnt/vault/context (R393 state-fabric durability) + **9th bidirectional-consistency lint**: load.sh metadata.name MUST equal sovereign-kernel-fence AND verify.sh MUST grep for the same string (drift = verifier never matches in journal = false-drift alarms forever) + policy DIR path matches between load + verify (drift = verifier checks wrong path) + E104/SAIN-01 milestone reference in header. **Cumulative SDD-037 family: 566 → 588 assertions** (42 lints now). **9 bidirectional-consistency lints**: R367 / R373 / R384 / R399 / R403 / R410 / R412 / R417 / R419. [from /goal re-process — R390 covered the Tetragon policy CONTENT pinning (allowlist + Sigkill etc.); R419 closes the LOAD↔VERIFY runtime cycle around that content; the 9th bidirectional lint catches drift where the policy gets renamed in load.sh but verify.sh still greps for the old name = perimeter forever appears 'drifted' to the operator] | ✓ shipped | R419 (tests/lint/test_tetragon_policy_load_verbatim.py + 22 L1 assertions + 9th bidirectional-consistency lint covering Tetragon policy name+dir between load + verify hooks) |
| E10.M62 | **build/lib/* infrastructure contract lint (the foundation R397-R417 lints depend on)** — extends R387-R417 operational-artifact pinning to the 6 build-pipeline + hook-infrastructure libs: `scripts/build/lib/{common,observability,state,logging,runtime-profile,selfdef-tune}.sh`. `tests/lint/test_build_lib_contract.py` (25 assertions): all 6 libs exist + **common.sh contract** (require_root + require_command + require_file + require_dir + profile_field + load_profile + confirm + err trap — the foundation R411 lifecycle hooks invoke; renaming any of these = mass silent breakage across the hook fleet) + **observability.sh SDD-016 contract**: emit_metric (operator-named SDD-016 entry point; renaming = ALL telemetry breaks) + emit_metric_set (bulk variant for R412 recurrent hooks) + source-guard (double-source breaks bash state) + node_exporter/textfile_collector path (SDD-016 verbatim) + atomic-write tempfile+rename (drift = scraper reads partial files = bogus metrics) + SOVEREIGN_OS_METRICS_DISABLE env (CI escape hatch) + SOVEREIGN_OS_DRY_RUN + telemetry_sink profile-field gate (operator-named per-profile metric-emission knob) + **operator IaC bar verbatim "observable and operable, at all stages of lifecycle"** in header (sacrosanct framing — drift loses the WHY) + **state.sh resume contract** (state_step_start + _complete + _fail + _should_run + _inputs_hash — R405 9-step pipeline depends on all 5) + **selfdef-tune.sh** references 'selfdef' (cross-repo binding context) + **runtime-profile.sh contract**: runtime_profile_override + runtime_profile_log_active (R402 Trinity start scripts depend on both) + **logging.sh** has log_step_header + log_info + log_warn + log_error (every step script + hook uses these). **Cumulative SDD-037 family: 541 → 566 assertions** (41 lints now). [from /goal re-process — every R-arc lint from R387 onward assumed these lib functions exist with these names; R418 closes the loop by pinning the INFRASTRUCTURE LAYER that all the prior lints depend on; if emit_metric ever gets renamed to write_metric, EVERY hook breaks at runtime while the R-arc lints stay green because they only check the hook source for the string 'emit_metric' — R418 catches that drift at the lib layer instead] | ✓ shipped | R418 (tests/lint/test_build_lib_contract.py + 25 L1 assertions pinning the infrastructure-layer functions that R397-R417 lints depend on) |
| E10.M61 | **Profile mixin operator-verbatim content lint + 8th bidirectional-consistency lint (mixin.id↔filename + R415-hardening↔role-server packages)** — extends R387-R416 + R385/R415 operational-artifact pinning to `profiles/mixins/*.yaml` (6-mixin composition layer). `tests/lint/test_profile_mixins_verbatim.py` (19 assertions): all 6 mixins exist (observability-tier-1 + role-developer + role-headless + role-server + role-workstation + whitelabel-default) + mixin count matches expected (drift = ungated mixin add) + every mixin has schema_version 1.0.0 + **8th bidirectional-consistency lint** (mixin.id MUST equal filename stem — drift = profile composition silently fails when profile lists the mixin id) + every mixin has non-empty description (operator-discovery context) + **bidirectional R415↔R417 consistency**: role-server.packages includes auditd + fail2ban + unattended-upgrades + chrony + openssh-server (R415's apply-server-hardening.sh drops rules/configs that REQUIRE these packages installed; drift = audit rules drop into system without auditd daemon) + role-server.packages.deny includes operator-named phone-home set (popularity-contest + apport + whoopsie + snapd + ubuntu-advantage-tools — sovereignty violation if missing) + role-server denies GUI bits (xserver-common + gdm3 + plymouth-themes — drift = headless server pulls X11) + role-server denies network-manager (R401 §8.1 — systemd-networkd is operator-named choice) + role-workstation includes podman (operator-named container runtime differentiator) + mixin id consistency for role-developer + observability-tier-1 + whitelabel-default + role-server hook script references exist on disk (drift = install fails on missing script) + **sovereignty floor enforcement**: phone-home packages MUST NOT appear in ANY mixin's required package list (only in deny:) — drift = mixin silently installs phone-home defeating the deny-list. **Cumulative SDD-037 family: 522 → 541 assertions** (40 lints now — MILESTONE: 40 L1 lints). **8 bidirectional-consistency lints**: R367 / R373 / R384 / R399 / R403 / R410 / R412 / R417. [from /goal re-process — R415 pinned the hardening drop-in files + apply hooks; R417 pins the MIXIN that supplies the packages required by those hooks; the R415↔R417 bidirectional check catches drift where hardening hook expects a package but mixin doesn't install it = silent install failure] | ✓ shipped | R417 (tests/lint/test_profile_mixins_verbatim.py + 19 L1 assertions + 8th bidirectional-consistency lint covering mixin.id↔filename + R415-hardening↔role-server packages; 40-LINT MILESTONE) |
| E10.M60 | **Systemd .service unit defense-in-depth hardening posture lint (R171 + SDD-023 fleet-wide enforcement)** — extends R387-R415 + R397 operational-artifact pinning to all 24 `systemd/system/sovereign-*.service` units. `tests/lint/test_systemd_hardening_posture.py` (12 assertions): ≥20 service units present (sanity floor) + **every service has ProtectSystem=** strict OR full OR true (R171 — drift = unhardened /etc + /usr write surface) + every service has NoNewPrivileges=true (drift = setuid escalation surface) + every service has PrivateTmp=true (drift = inter-service /tmp data leakage) + every service has ProtectHome= true/read-only/tmpfs (drift = /home + /root readable) + every service has RestrictNamespaces=(true OR false-with-justifying-inline-comment for podman containers) + units carry R171 / 'defense-in-depth' marker (≥50% — operator-discovery WHY are these keys here) + **VFIO-bind MUST use ProtectSystem=full** (writes to /etc/modprobe.d + /sys/bus/pci; drift to strict = silent install failure) + **ZFS-arc-clamp MUST use ProtectSystem=full** (writes to /sys/module/zfs/parameters; same reason) + ≥70% of units have ProtectKernelTunables=true (defense-in-depth coverage floor) + ≥80% of units have Documentation= link (operator-discovery — 'systemctl cat <unit>' shows docs). **Cumulative SDD-037 family: 510 → 522 assertions** (39 lints now). [from /goal re-process — R397 covered Description= identity, R411 covered lifecycle hooks broadly; R416 closes the FLEET-WIDE defense-in-depth surface across the full 24-unit systemd inventory; the per-unit hardening-key check catches drift where any single unit's hardening posture silently degrades; the operator-discoverable WHY comment requirement on RestrictNamespaces=false catches accidental-drift vs intentional-disable] | ✓ shipped | R416 (tests/lint/test_systemd_hardening_posture.py + 12 L1 assertions covering 24-unit defense-in-depth fleet) |
| E10.M59 | **Server + workstation hardening hooks operator-verbatim SDD-023 + §8 ZT sshd posture lint** — extends R387-R414 operational-artifact pinning to `scripts/hooks/post-install/apply-server-hardening.sh` + `apply-workstation-hardening.sh` + `config/server/*` (5 hardening drop-ins). `tests/lint/test_hardening_hooks_verbatim.py` (22 assertions): both hardening hooks exist + 5 server drop-ins exist (auditd.rules + fail2ban-jail + unattended-upgrades + sshd.conf + pwquality.conf) + server hook detects role-server mixin + SKIPs cleanly for non-role-server profiles (drift = workstation gets server config silently) + honors SOVEREIGN_OS_DRY_RUN + **OPERATOR-CRITICAL ORDERING**: 'sshd -t' validates config BEFORE 'systemctl reload ssh' (drift = bad config locks operator out of own system) + SOVEREIGN_OS_HARDENING_DEST_PREFIX support (operator can apply to chroot/container/image tree offline) + cmp idempotency check (operator-verbatim IaC bar — drift = every re-run touches mtimes, breaks cache hashes) + per-result metric (SDD-016) + workstation hook detects role-workstation mixin + SKIPs cleanly + honors DRY_RUN + **§8 ZT sshd verbatim**: PermitRootLogin=no (root SSH closed) + PasswordAuthentication=no (SSH-key-only) + PubkeyAuthentication=yes + AuthenticationMethods=publickey (drift to 'publickey,password' silently re-enables password) + PermitEmptyPasswords=no (defense-in-depth) + X11Forwarding=no (§8 ZT default) + MaxAuthTries ≤ 6 (brute-force surface cap) + **SDD-023 auditd verbatim**: -f 2 panic-on-disk-full ('audit silence is worse than denial of service' — drift to -f 0/1 = silent audit loss) + -b ≥ 8192 buffer (operator-named server-class baseline; drift = audit drops under burst). **Cumulative SDD-037 family: 488 → 510 assertions** (38 lints now — MILESTONE: 500+ assertions). [from /goal re-process — R411 covered lifecycle hooks broadly; R415 pins the operator-name profile-class-differentiator hardening surfaces; the sshd -t ordering check catches an operator-lockout-grade ordering bug; SDD-023 + §8 ZT verbatim mechanized at push-time across the full SSH hardening contract] | ✓ shipped | R415 (tests/lint/test_hardening_hooks_verbatim.py + 22 L1 assertions pinning SDD-023 + §8 ZT sshd posture; 500-ASSERTION MILESTONE) |
| E10.M58 | **Cloud-init user-data templates operator-verbatim Q-018 + ZT-posture lint + 1 real bug caught & fixed** — extends R387-R413 operational-artifact pinning to `config/cloud-init/<profile>.user-data.example.yaml` (5 per-profile unattended-install templates). `tests/lint/test_cloud_init_templates_verbatim.py` (13 assertions): all 5 profile templates exist (sain-01 + developer + headless + minimal + old-workstation) + every template starts with #cloud-config (cloud-init schema; drift = silent fall-through to interactive prompts) + every template parses as YAML + every template declares hostname (operator-named identity) + every template has sudo-capable user (operator elevation path) + every template lock_passwd:true (§8 ZT — SSH-key-only by default; drift = SSH password auth open at install) + **bidirectional consistency**: active-profile write_files content MUST match filename profile id (drift = installer boots with wrong profile = wrong hardware tunings) + every template sets active-whitelabel='default' (operator-named baseline) + NoCloud usage documentation present (operator-discovery) + active-profile.env file with SOVEREIGN_OS_PROFILE= line (downstream sovereign-osctl env reader) + SSH key placeholder clearly marked (operator might miss replacing) + locale UTF-8 (drift to POSIX breaks non-ASCII content) + timezone UTC (reproducibility-friendly default). **Caught 1 real bug (#30) per L3 discipline**: old-workstation.user-data.example.yaml was missing /etc/sovereign-os/active-whitelabel write_files entry — installed system would have no whitelabel = sovereign-osctl whitelabel resolver fails. Fixed inline. **Cumulative SDD-037 family: 475 → 488 assertions** (37 lints now). **Bug count: 29 → 30 bugs caught.** [from /goal re-process — R413 covered the runtime CLI; R414 covers the install-time bootstrap; the bidirectional filename↔active-profile consistency catches drift where template filename and content disagree — silent wrong-profile installer; bug #30 was a real cross-template-omission caught by lint] | ✓ shipped | R414 (tests/lint/test_cloud_init_templates_verbatim.py + 13 L1 assertions + 1 bug fixed in config/cloud-init/old-workstation.user-data.example.yaml) |
| E10.M57 | **sovereign-osctl operator-CLI contract lint + Q-019 verbatim sacrosanct preservation** — extends R387-R412 + R383 operational-artifact pinning to `scripts/sovereign-osctl` (6471-line operator-facing single-entry-point CLI dispatcher). `tests/lint/test_sovereign_osctl_contract.py` (17 assertions): osctl file exists + executable bit set + **Q-019 verbatim quote preserved** (operator's exact wording 'even once installed and configured it will be possible to manage the OS like we need to even if we need to add such an additional tool and even service possibly or even multiple adapted if need be') + 'sacrosanct' marker present (defense-in-depth against well-meaning reformatting) + 'set -euo pipefail' bash strict mode (SDD-001 — unset-var bugs would slip to operator's running system) + all 25 operator-named subcommands have matching cmd_<name> handlers + all 25 routed in main dispatcher case statement + cmd_help + cmd_version handlers exist (operator-discovery + identity surface) + 3 lib search paths present (/usr/local/lib/sovereign-os + /usr/lib/sovereign-os + /opt/sovereign-os — operator can install to any PREFIX) + SOVEREIGN_OS_LIB env override (custom lib location) + explicit error+exit-1 on missing lib (drift = silent confusing failure) + /etc/sovereign-os/active-profile read (operator's installed-profile selection; drift = always reverts to hardcoded default) + sain-01 default fallback (operator-named primary profile) + R-arc operator-pull verbs in help (architecture-qa / coverage / layers / search — R383 discoverability) + **bidirectional handler↔dispatcher consistency** (orphan handlers ≤8, no orphan-dispatched references to nonexistent handlers). **Cumulative SDD-037 family: 458 → 475 assertions** (36 lints now). [from /goal re-process — R383 covered help-text discoverability; R413 covers the BROADER CLI contract: every operator-named subcommand has a handler, multi-prefix install support, Q-019 verbatim framing preserved as sacrosanct; the bidirectional handler↔dispatcher consistency catches orphan handlers (dead code) AND orphan dispatcher entries (silent fail)] | ✓ shipped | R413 (tests/lint/test_sovereign_osctl_contract.py + 17 L1 assertions pinning sovereign-osctl Q-019 framing + 25 subcommand contract + multi-prefix install support) |
| E10.M56 | **Recurrent-hook contract lint + 7th bidirectional-consistency lint (timer↔hook↔service triangle)** — extends R387-R411 operational-artifact pinning to `scripts/hooks/recurrent/` (11 timer-driven hooks) + `systemd/system/sovereign-*.timer` + `.service` units. `tests/lint/test_recurrent_hooks_contract.py` (18 assertions): all 11 recurrent hooks exist + hook count matches exactly + **7th bidirectional-consistency lint** (timer↔hook↔service triangle): every hook has matching .timer unit (drift = hook exists but never fires) + every hook has matching .service unit + every timer references existing service (drift = systemd errors on every fire) + every service has ExecStart= directive + every hook sources build/lib/common.sh + every hook sources observability.sh + every hook emits metric (bash-direct OR delegated via --emit-metrics) + **timer cadence verbatim**: zfs-scrub runs Sunday weekly (operator-named low-impact window; drift to daily = excessive I/O), security-update-check daily (drift = stale CVE awareness), notify-dispatch has active cadence, thermal-watch ≤5 min (catch transient spikes) + zfs-scrub invokes 'zpool scrub' (operator-named ZFS integrity command) + zfs-scrub emits sovereign_os_zfs_pool_health gauge (Grafana stat surface) + tetragon-policy-verify require_root (eBPF state read) + wattage-sample STEP_ID pinned (SDD-026 Z-18 + R258 correlation) + power-shutdown-guard STEP_ID pinned (SDD-026 Z-18 + R253). **Cumulative SDD-037 family: 440 → 458 assertions** (35 lints now). **7 bidirectional-consistency lints**: R367 / R373 / R384 / R399 / R403 / R410 / R412. [from /goal re-process — R411 covered pre/during/decommission lifecycle hooks; R412 closes the RECURRENT cadence side; the timer↔hook↔service triangle bidirectional consistency catches drift where any 1 of 3 corners exists without the others (silent dead code OR systemd-error-spam)] | ✓ shipped | R412 (tests/lint/test_recurrent_hooks_contract.py + 18 L1 assertions + 7th bidirectional-consistency lint covering timer↔hook↔service triangle) |
| E10.M55 | **Lifecycle-hook contract lint (pre/during/decommission) + 3 real bugs caught & fixed — including 2 OPERATOR-MANDATE-GRADE safety violations** — extends R387-R410 operational-artifact pinning to the 3 lifecycle-hook families: `scripts/hooks/pre-install/` (4 hooks) + `scripts/hooks/during-install/` (4 hooks) + `scripts/hooks/decommission/` (3 hooks). `tests/lint/test_lifecycle_hooks_contract.py` (19 assertions): all 11 lifecycle hooks exist + **PRE-INSTALL contract**: every hook honors SOVEREIGN_OS_DRY_RUN + emits sovereign_os_pre_install_* metric + STEP_ID matches filename + friction-audit emits both _total counter AND _failures gauge (per-item visibility) + **DURING-INSTALL contract**: every hook require_root (writes to /dev or /etc) + honors SOVEREIGN_OS_DRY_RUN (critical: drift = CI executes real disk format) + emits sovereign_os_during_install_* metric + STEP_ID matches filename + zfs-pool-create handles raid0+single topology (SDD-005 + Q-005 verbatim) + mok-enroll uses mokutil (operator-named MOK tool) + **DECOMMISSION contract (SACROSANCT)**: every hook checks SOVEREIGN_OS_CONFIRM_DESTROY (operator standing mandate — drift = ANY ENV CAN TRIGGER DATA DESTRUCTION = mandate violation) + literal 'YES' comparison required (drift to truthy-only relaxes operator mandate) + every hook require_root + ≥2/3 hooks have interactive confirm() prompt (defense-in-depth) + secure-wipe.sh has UNRECOVERABLE/ALL-DATA warning (operator-deterrent) + all 11 hooks source build/lib/common.sh. **Caught 3 real bugs (#26 + #27 + #28) in same commit per L3 discipline — 2 operator-mandate-grade**: bug #26 — friction-audit-spec.sh missing SOVEREIGN_OS_DRY_RUN handling (preflight executes in CI); bug #27 — **secure-wipe-context.sh missing SOVEREIGN_OS_CONFIRM_DESTROY env-var check** (CI run could wipe state-fabric without operator's explicit YES = mandate violation); bug #28 — same hook missing literal-YES comparison. All 3 fixed in same R411 commit. **Cumulative SDD-037 family: 421 → 440 assertions** (34 lints now). **Bug count: 26 → 29 bugs caught.** [from /goal re-process — R408 covered image-sign secure-boot operator-key-mandate; R411 covers the SACROSANCT decommission safety contract; the secure-wipe-context.sh bug was a real operator-mandate-grade safety violation — lint caught it before any operator data could be at risk] | ✓ shipped | R411 (tests/lint/test_lifecycle_hooks_contract.py + 19 L1 assertions + 3 bugs fixed in secure-wipe-context.sh + friction-audit-spec.sh) |
| E10.M54 | **Bootstrap verify.sh ↔ verify-grid.yaml implementation + master spec § 22 verbatim 6-check operational grid lint (6th bidirectional-consistency lint)** — extends R387-R409 + R389/R399 operational-artifact pinning to `scripts/bootstrap/verify.sh` + `config/bootstrap/verify-grid.yaml`. `tests/lint/test_bootstrap_verify_grid_implementation.py` (22 assertions): both files exist + all 6 check_NN() bash functions defined + dispatcher case statement routes each id to check_NN + **6th bidirectional-consistency lint** (verify-grid.yaml id ↔ verify.sh check_NN function — both directions); per-check verbatim invariants: check_01 avx512_vnni + avx512_bf16 (§ 22.1 + § 1.1 — Zen 5 INT8 + BF16 ISA flags) + check_02 'Width x8' + Gen 4/5 16 GT/s (§ 22.2 + § 1.2 — PCIe x8/x8 bifurcation) + check_03 reads /proc/spl/kstat/zfs/arcstats + BOOTSTRAP_VERIFY_ARC_MAX_BYTES env var comparison (R399 bidirectional ARC surface) + check_04 MIT/GPL license accept + NVIDIA*-proprietary REJECT (§ 22.4 — open kernel module verbatim; drift to accept-all silently breaks conformance) + check_05 /var/run/tetragon/tetragon.events verbatim + accepts socket OR FIFO OR file types (drift to socket-only misses FIFO mode) + check_06 MTU=9000 verbatim (§ 22.6 + § 8.1 jumbo frames) + BOOTSTRAP_VERIFY_DATA_IFACE env var (drift to hardcoded enp5s0 breaks alt profiles) + --strict mode handling (verify-grid.yaml verbatim 'promotes SKIPs to FAILs') + sovereign_os_bootstrap_check_total per-check metric (SDD-016) + CHECK_NAMES bash assoc array fallback for all 6 ids (defense-in-depth: YAML loader failure doesn't break display) + **CHECK_NAMES fallback strings MUST match verify-grid.yaml name fields exactly** (bidirectional name consistency between bash fallback + YAML metadata). **Cumulative SDD-037 family: 399 → 421 assertions** (33 lints now). **6 bidirectional-consistency lints**: R367 / R373 / R384 / R399 / R403 / R410. [from /goal re-process — R389 covered YAML schema, R399 covered ARC byte-value bidirectional consistency; R410 closes the implementation gap: every check_NN function MUST verify what verify-grid.yaml's metadata says it does + bash display-name fallback must match YAML; the NVIDIA*-proprietary REJECT lint catches drift to silent-accept-all-licenses that would break § 22.4 open-module conformance] | ✓ shipped | R410 (tests/lint/test_bootstrap_verify_grid_implementation.py + 22 L1 assertions + 6th bidirectional-consistency lint in family) |
| E10.M53 | **Image-build (step 07) + image-verify (step 09) operator-verbatim substrate-dispatch + SDD-019 reproducibility lint** — extends R387-R408 operational-artifact pinning to `scripts/build/07-image-build.sh` (substrate dispatcher) + `scripts/build/09-image-verify.sh` (QEMU smoke + reproducibility). `tests/lint/test_image_build_verify_verbatim.py` (23 assertions): both step scripts exist + step 07 dispatches on SOVEREIGN_OS_SUBSTRATE + 'mkosi build' verbatim + 'lb build' verbatim + kernel .deb staging (drift = substrate silently uses Debian-archive kernel) + mkosi.extra/var/cache/local-debs path (mkosi local-deb apt resolution) + config/packages.chroot path (live-build local-packages convention) + Stage-2+ substrates (rpm-ostree/nixos) fail with 'substrate-image-build-not-implemented' (drift = silent skip stalls migration) + unknown-substrate fails (typo doesn't exit 0) + .raw/.img/.iso/.qcow2 output extensions (operator-named bootable artifacts) + env-image.sh handoff to steps 08+09 + **step 09 SDD-019**: qemu-system-x86_64 (operator-named amd64 QEMU) + SOVEREIGN_OS_SKIP_QEMU honored + QEMU timeout default 300s ('5 minutes' verbatim) + -no-reboot flag (drift = panic loops indefinitely) + -nographic flag (headless CI) + readonly=on drive (drift = QEMU corrupts operator's image) + sha256sums.txt emission + in-toto/SLSA build-provenance manifest + provenance includes SOURCE_DATE_EPOCH + DEBIAN_SNAPSHOT (SDD-019 reproducibility-input record) + userspace-marker check (drift = halt-at-initrd images pass smoke) + in-toto Statement/v1 + SLSA provenance/v1 URLs verbatim (schema-version pinning) + exit code 124 (GNU timeout convention) in rc classification. **Cumulative SDD-037 family: 376 → 399 assertions** (32 lints now). [from /goal re-process — R405 pinned the 9-step contract, R408 pinned step 08 signing, R409 pins steps 07+09 (substrate dispatch + image verification + SDD-019 supply-chain provenance); the in-toto/SLSA URL pinning catches schema-version drift that would break operator's supply-chain verifier toolchain] | ✓ shipped | R409 (tests/lint/test_image_build_verify_verbatim.py + 23 L1 assertions pinning step 07 substrate dispatch + step 09 QEMU smoke + SDD-019 reproducibility artifacts) |
| E10.M52 | **Image-sign secure-boot operator-verbatim SDD-015 + Q-006 + operator-key-mandate lint** — extends R387-R407 operational-artifact pinning to `scripts/build/08-image-sign.sh` (the operator-named EFI signing step). `tests/lint/test_image_sign_verbatim.py` (21 assertions): file exists + **SDD-015 3-level enum verbatim (Q-006 resolution)**: none / shim / signed all handled + unknown posture fails (drift = typo deploys unsigned silently) + unknown posture calls state_step_fail "unknown-secure-boot" (operator state.yaml discoverability) + **operator-key-mandate verbatim "keys NEVER in-repo"**: SOVEREIGN_OS_PK_KEY / PK_CERT / MOK_KEY / MOK_CERT env vars referenced (no hardcoded paths) + no hardcoded *.key / *.crt / *.pem paths in script (drift = OPERATOR MANDATE VIOLATION) + shim path requires MOK keys (SDD-015 — drift = unsigned despite shim posture) + signed path falls back to MOK with operator-discoverable warning (SDD-015 verbatim fallback safety) + uses sbsign (operator-named EFI signer; drift to different signer breaks chain) + uses sbverify (post-sign verification — drift = ships broken signatures) + sbverify failure calls state_step_fail (drift = broken sig flows to step 09) + signs vmlinuz* + *.efi binaries (operator-named EFI boot chain) + SOVEREIGN_OS_DRY_RUN short-circuit (CI safety on hosts without sbsign installed) + posture= metric label (SDD-016 — per-posture Grafana aggregation) + 'disabled' legacy alias handled with operator-discoverable upgrade warning + SDD-015 + Q-006 references in script header. **Cumulative SDD-037 family: 355 → 376 assertions** (31 lints now). [from /goal re-process — R405 pinned the 9-step pipeline contract, R408 pins the deepest security surface in that pipeline: image signing; the operator-key-mandate 'keys NEVER in-repo' is sacrosanct — lint enforces zero-tolerance for hardcoded key paths; SDD-015 + Q-006 binding pinned for operator-discovery] | ✓ shipped | R408 (tests/lint/test_image_sign_verbatim.py + 21 L1 assertions pinning SDD-015 3-level enum + operator-key-mandate + sbsign/sbverify safety contract) |
| E10.M51 | **Whitelabel render engine operator-verbatim SDD-007 + SDD-006 § Legal floor lint** — extends R387-R406 operational-artifact pinning to `scripts/whitelabel/render.py` (357 lines — SDD-007 Layer 1 changeset generator + Layer 2 substrate adapter). `tests/lint/test_whitelabel_render_verbatim.py` (22 assertions): file exists + Python syntactically valid + **all 7 SDD-007 strategies handled verbatim** (template-substitution / file-overlay / package-replacement / build-time-flag / install-time-substitution / first-boot-script / must-not-touch) + '7-strategy taxonomy' docstring reference (operator-discovery) + **SDD-006 § Legal floor verbatim**: all 5 protected paths present (/etc/debian_version, /usr/share/doc/*/copyright, /usr/share/man/*, debian-logo*, debian-swirl*) — drift = whitelabel can erase Debian attribution = SDD-006 violation + violates_legal_floor() function defined + fnmatch glob matching (drift to bare-equality lets logo files through) + **legal-floor violation MUST exit non-zero** (drift to warn-only lets violations through silently) + compliance_target ↔ legal_compliance match (SDD-007 sovereignty contract between profile + whitelabel) + compliance-mismatch exits code 3 (operator-discoverable failure code) + emit_for_mkosi() defined + emit_for_live_build() defined + mkosi emit populates BOTH mkosi.skeleton (early) AND mkosi.extra (late) + live-build emit populates config/includes.chroot/ + both emit functions write whitelabel-manifest.json (install-time + first-boot read it) + yaml.safe_load (security CVE: yaml.load = RCE on untrusted YAML) + no bare yaml.load() in render code + --substrate argparse choices include mkosi + live-build + required CLI args --profile/--whitelabel/--out + substrate-not-implemented returns 5 + Changeset dataclass has all 6 strategy-bucket fields + Changeset.summary() method present (operator-discovery). **Cumulative SDD-037 family: 333 → 355 assertions** (30 lints now — MILESTONE: 30 L1 lints). [from /goal re-process — R406 pinned the substrate-config translation, R407 pins the WHITELABEL render engine ABOVE that; SDD-006 § Legal floor + SDD-007 7-strategy taxonomy are operator-named sovereignty/compliance contracts; legal-floor violation exit code is operator-discoverable failure mode — drift to warn-only silently lets whitelabel erase Debian attribution] | ✓ shipped | R407 (tests/lint/test_whitelabel_render_verbatim.py + 22 L1 assertions pinning SDD-007 7-strategy taxonomy + SDD-006 § Legal floor + sovereignty compliance contract + substrate-emit duality; 30-LINT MILESTONE) |
| E10.M50 | **Substrate adapter emission operator-verbatim lint (mkosi-emit + live-build-emit translation of profile YAML)** — extends R387-R405 operational-artifact pinning to the 2 substrate adapter emitters: `scripts/build/adapters/{mkosi-emit,live-build-emit}.sh`. `tests/lint/test_substrate_adapters_verbatim.py` (26 assertions): both adapters exist + mkosi Distribution=debian + Release=trixie verbatim (SDD-003 baseline; drift to bookworm loses kernel 6.12+ Blackwell support) + SecureBoot=yes (operator-verbatim baseline; drift to no = unsigned image silently produced) + Bootloader=systemd-boot (master spec; drift breaks ESP UEFI path) + Format=disk (drift to tar = non-bootable tarball) + SOURCE_DATE_EPOCH propagation (SDD-019 substrate-level reproducibility) + DEBIAN_SNAPSHOT propagation + snapshot.debian.org mirror URL (bit-identical apt resolution) + filters linux-image-* / linux-headers-* from package list (custom kernel ships separately; drift = double-kernel install + boot ambiguity) + zfs-tiered storage handling + ZFS partition Format=none (post-install hook creates pool; drift to ext4/zfs silently re-formats operator's data) + 512M ESP partition size + KernelCommandLine= propagation (operator-verbatim §4.3 cmdline flows through) + live-build distribution trixie (Alt-A MUST match primary mkosi) + amd64 architecture (sain-01 baseline) + iso-hybrid output (USB-installable; drift to iso9660 only blocks USB install) + linux-image-* filter on live-build path + profile.packages.deny honored (operator-verbatim sovereignty deny-list) + iso-publisher 'cyberpunk042' verbatim (operator-named ISO identity metadata) + iso-application 'Sovereign OS' verbatim + both accept 2-arg CLI <profile.yaml> <out-dir> (substrate orchestrator contract) + both source lib/common.sh + both read packages from profile (single source of truth) + both use yaml.safe_load (CVE security: yaml.load is RCE risk on untrusted YAML) + no bare yaml.load( in adapter code. **Cumulative SDD-037 family: 307 → 333 assertions** (29 lints now). [from /goal re-process — R401-R405 pinned operator-named profile content + build pipeline; R406 closes the translation-layer between profile YAML and substrate-specific config; the SecureBoot=yes / Format=none / iso-publisher / yaml.safe_load invariants are all operator-verbatim drift surfaces; security-grade catch on yaml.load CVE risk] | ✓ shipped | R406 (tests/lint/test_substrate_adapters_verbatim.py + 26 L1 assertions pinning mkosi + live-build substrate emission contract; includes security yaml.safe_load CVE catch) |
| E10.M49 | **Build pipeline operator-verbatim 9-step contract lint + 3 real bugs caught & fixed** — extends R387-R404 operational-artifact pinning to the entire build pipeline: `scripts/build/orchestrate.sh` + 9 steps `01-bootstrap-forge.sh` through `09-image-verify.sh`. `tests/lint/test_build_pipeline_verbatim.py` (20 assertions): orchestrate.sh exists + all 9 step scripts exist + orchestrate STEPS array matches operator-verbatim ordering + each step exports STEP_ID matching its filename + each step calls state_step_start / state_step_complete / state_step_fail (resume contract) + each step sources lib/common.sh + lib/observability.sh + each step honors SOVEREIGN_OS_DRY_RUN (operator-verbatim CI/preview safety) + each step emits a Layer B metric (SDD-016) + step 04 uses bindeb-pkg (E101 verbatim packaging) + step 04 propagates SOURCE_DATE_EPOCH (SDD-019 reproducibility) + step 04 sets KBUILD_BUILD_TIMESTAMP (SDD-019) + step 04 handles substrate-default skip (Q18-A) + step 08 secure boot signing references + step 09 QEMU smoke test + step 09 emits sha256sums (SDD-019 integrity) + step prefix order strictly monotonic 01..09 + orchestrate.sh documents IaC bar verbatim + STEP_ID kebab-case format pinning (no camelCase drift). **Caught 3 real bugs (#23+#24+#25) in same commit per L3 discipline**: 01-bootstrap-forge.sh missing SOVEREIGN_OS_DRY_RUN handling (apt install + tmpfs mount would execute in CI), 02-kernel-fetch.sh missing SOVEREIGN_OS_DRY_RUN handling (kernel clone would execute), 03-kernel-config.sh missing SOVEREIGN_OS_DRY_RUN handling AND no state_step_fail path for olddefconfig failures (state.yaml wouldn't record WHY config derivation failed). Fixed all 3 in the same R405 commit. **Cumulative SDD-037 family: 287 → 307 assertions** (28 lints now). **Bug count: 23 → 26 bugs caught.** [from /goal re-process — R405 closes the build-pipeline drift surface; the 9-step contract is operator-discovery surface that needs state_step_* + DRY_RUN + metric uniformity; bugs #23-25 fixed inline preserves the L3 discipline pattern continuing from R367-R399] | ✓ shipped | R405 (tests/lint/test_build_pipeline_verbatim.py + 20 L1 assertions + 3 bugs fixed in scripts/build/01,02,03-*.sh) |
| E10.M48 | **Inference backend adapters operator-verbatim §17.1 + E109 (DFlash) lint (argv-construction layer)** — extends R387-R403 operational-artifact pinning to the 3 backend adapter modules: `scripts/inference/backends/{bitnet,vllm,llama_cpp}.py`. `tests/lint/test_backend_adapters_verbatim.py` (23 assertions): all 3 adapter files exist + BitnetBackend default model 'microsoft/bitnet-b1.58-2B-4T' verbatim (SDD-005) + DEFAULT_KERNEL='TL2' (operator-named x86 kernel default for Pulse; drift loses x86 optimization on Zen 5) + default affinity '0-5' (CCD 0 cores; drift to '6-11' bleeds Pulse onto CCD 1 = §17.1 SRP) + bitnet argv uses taskset (OS-level pinning defense-in-depth) + tier='pulse' attribute verbatim + port 8081 + VllmBackend MIN_VERSION='0.20.1' (E109 verbatim — required for DFlash) + for_logic_engine uses podman=True (VFIO 3090 isolation via container wrap) + for_oracle_core uses podman=False (Blackwell host-resident, native vLLM) + Oracle Core kv_cache_dtype='fp8' default (L0 Profile 3 deep-context; drift halves effective context) + --speculative-config flag present (E109 DFlash integration) + 'method': 'dflash' verbatim (operator-named spec-decode method) + vllm.entrypoints.openai.api_server (OpenAI-API uniformity) + port 8082 / 8083 verbatim + LlamaCppBackend port 8084 (old-workstation) + 8085 (sain01 fallback) + DEFAULT_N_GPU_LAYERS=999 GPU-offload-all + all 3 inherit Backend base (SDD-011 polymorphic contract) + all 3 expose /v1/models health URL + all 3 define start_command() + all 3 use 127.0.0.1 loopback in factory classmethods (§8 ZT — backends MUST NOT bind externally). **Cumulative SDD-037 family: 264 → 287 assertions** (27 lints now). [from /goal re-process — R403 pinned router.py TIER_ENDPOINTS + classify(), R404 pins the argv-construction layer below that; the 'taskset -c' / '--kv-cache-dtype fp8' / '--speculative-config' / 'method:dflash' verbatims live in the adapters; drift here silently breaks E109 DFlash integration or §17.1 SRP at process-launch time] | ✓ shipped | R404 (tests/lint/test_backend_adapters_verbatim.py + 23 L1 assertions pinning §17.1 + E109 argv-construction across bitnet.py / vllm.py / llama_cpp.py) |
| E10.M47 | **Inference router classify + TIER_ENDPOINTS operator-verbatim §17.1 + SDD-011 lint + bidirectional consistency with start-*.sh** — extends R387-R402 operational-artifact pinning to `scripts/inference/router.py`. `tests/lint/test_router_classify_verbatim.py` (19 assertions): file exists + TIER_ENDPOINTS dict parsable via AST + 3 Trinity tier keys verbatim (pulse / logic_engine / oracle_core — operator-named §17.1) + ports 8081/8082/8083 bound (router writes to those, start-*.sh listen on them) + **bidirectional consistency**: ports MUST match between router.py TIER_ENDPOINTS AND start-pulse.sh / start-logic-engine.sh / start-oracle-core.sh defaults (drift between writer and listener breaks routing silently) + endpoints use 127.0.0.1 loopback only (§8 Zero-Trust: backends MUST NOT bind externally, only router is network-reachable; drift to 0.0.0.0 = ZT violation) + classify() function defined + SDD-011 Rule 1 (ternary/bitnet → Pulse; drift sends CPU model to GPU = CCD 0 SRP violated + 50× cost) + SDD-011 Rule 4 (JSON/structured → Logic Engine; drift sends structured output to bitnet = silent quality regression) + **SDD-011 'No black-box dispatch'**: no LLM call in classify() routing path (catches drift to LLM-as-router pattern) + sovereign_os_inference_route_total{tier=} metric (SDD-016 verbatim — Grafana Trinity dashboard aggregation) + X-Sovereign-Routed-Tier response header (operator-discovery per-request tier observability) + Trinity tier-count ≥ 3 (collapse to 2 tiers breaks §17.1 SRP architecture). **Cumulative SDD-037 family: 245 → 264 assertions** (26 lints now). New bidirectional-consistency lint #5 (router↔start-scripts port binding). [from /goal re-process — R402 pinned the 3 start scripts, R403 pins the routing dispatcher that depends on those start scripts; bidirectional port-consistency catches drift between writer and listener, completing the §17.1 runtime contract triangle: router.py / .service unit Descriptions / start-*.sh] | ✓ shipped | R403 (tests/lint/test_router_classify_verbatim.py + 19 L1 assertions pinning §17.1 + SDD-011 + bidirectional port consistency router↔start-scripts) |
| E10.M46 | **Inference Trinity start-script operator-verbatim content lint (§17.1 runtime bindings)** — extends R387-R401 operational-artifact pinning to the 3 §17.1 Trinity start scripts: `scripts/inference/start-pulse.sh`, `scripts/inference/start-logic-engine.sh`, `scripts/inference/start-oracle-core.sh`. `tests/lint/test_inference_start_scripts_verbatim.py` (22 assertions): all 3 scripts exist + Pulse:8081 / Logic Engine:8082 / Oracle Core:8083 port allocation (drift breaks router.py routing) + ports unique across Trinity (collision = silent tier shadowing) + Layer B tier= metric labels verbatim (tier="pulse" / "logic_engine" / "oracle_core" — SDD-016; drift breaks Grafana inference dashboards) + sovereign_os_inference_backend_start_total emission + Pulse references bitnet.cpp + Pulse CCD 0 affinity (operator-named §17.1 dual-CCD SRP) + Pulse uses taskset (defense-in-depth OS-level pinning vs bitnet's own which may not survive systemd restart) + Logic Engine references RTX 3090 / VFIO + Logic Engine supports vllm + llama_cpp fallback (operator-named hardware-constraint path) + Oracle Core references Blackwell / RTX PRO 6000 + Oracle Core uses vLLM + Oracle Core fp8 KV cache default (deep-context-friendly; drift to fp16/auto halves effective context) + all 3 honor SOVEREIGN_OS_DRY_RUN (CI safety) + all 3 idempotent on port-already-listening (double-start = port conflict) + all 3 use 'set -euo pipefail' bash strict mode + all 3 emit sovereign_os_inference_backend_pid metric (operator-discovery PID-to-tier mapping). **Cumulative SDD-037 family: 223 → 245 assertions** (25 lints now). [from /goal re-process — R397 pinned the .service unit Description= identity strings but the RUNTIME invariants (port assignments, taskset affinity, idempotency, DRY_RUN honor, tier labels) are encoded in the start scripts; R402 closes that drift surface; the Pulse taskset pinning is operator-named CCD 0 defense-in-depth — drift silently bleeds Pulse onto CCD 1 violating §17.1 dual-CCD SRP] | ✓ shipped | R402 (tests/lint/test_inference_start_scripts_verbatim.py + 22 L1 assertions pinning §17.1 Trinity runtime contract across start-pulse.sh / start-logic-engine.sh / start-oracle-core.sh) |
| E10.M45 | **network-vlan-config + §8.1 operator-verbatim content lint (Zero-Trust dual-NIC segregation)** — extends R387-R399 operational-artifact pinning to `scripts/hooks/post-install/network-vlan-config.sh` + `profiles/sain-01.yaml` hardware.network section. `tests/lint/test_network_vlan_verbatim.py` (13 assertions): script exists + reads hardware.network YAML path + emits /etc/systemd/network units + Kind=vlan .netdev + MTU propagation + DefaultRouteOnDevice= control (without it, Marvell silently routes WAN = §8 verbatim violation) + systemd-networkd reload + mgmt NIC §8.1 verbatim (Intel i226-v + 2.5 Gbps + VLAN 100 + default_gateway:true + 10.0.100.50/24) + data NIC §8.1 verbatim (Marvell aqc113c + 10 Gbps + VLAN 200 + MTU 9000 + 10.0.200.50/24) + **OPERATOR-CRITICAL §8 invariant**: 'Marvell MUST NOT carry the default route' — data NIC default_gateway MUST NOT be True (flip = WAN exposure of storage path = ZT topology violation) + VLAN 100↔mgmt + VLAN 200↔data binding preserved (swap silently misroutes mgmt/storage traffic onto wrong subnet) + master spec §8 reference in profile comments + no-forced-default-route drift in script. **Cumulative SDD-037 family: 210 → 223 assertions** (24 lints now). [from /goal re-process — R387 pinned KCFLAGS+SKUs in profile but did NOT pin §8.1 verbatim network spec; R401 closes that drift surface; the 'Marvell MUST NOT carry the default route' verbatim is operator-named ZT invariant — flipping data NIC default_gateway breaks §8 Zero-Trust topology silently] | ✓ shipped | R401 (tests/lint/test_network_vlan_verbatim.py + 13 L1 assertions pinning §8.1 NIC verbatim spec + §8 ZT invariant + script unit-emission contract) |
| E10.M44 | **R400 milestone — verbatim-preservation arc consolidation (46 rounds, R355-R400)** — refreshes `docs/handoff/006-verbatim-preservation-arc.md` to cover R395-R399 (4 more rounds since R395 handoff refresh) + regenerates `docs/src/verbatim-surface.md` via `scripts/intelligence/verbatim-render.py`. Final state at R400: **23 L1 lints / 210 assertions / 23 bugs caught**, 11 operational artifacts pinned (R387-R399), 4 Trinity-side systemd Descriptions pinned (R397), 4 bidirectional-consistency lints (R367 Tetragon writer↔allowlist, R373 cross-catalog, R384 handoff INDEX, R399 ZFS ARC writer↔verify-grid verifier), Trinity-side pinning COMPLETE (Pulse + Weaver + Auditor + ZFS + VFIO + Tetragon). 175+ mandate rows / ~135KB. Grade A stable across 46 rounds. State preservation: full R355-R400 arc state accessible from handoff doc 006 for next-session cold-start without scanning mandate file. [from /goal re-process — R400 is the 46-round milestone closing the verbatim-preservation arc's operational-artifact-pinning phase; the arc itself stays open per operator's perpetual mandate "we move toward my solution endlessly — DO not stop after opening or updating a PR"; next phase candidates: network-vlan-config §8.1 verbatim, inference start-script pinning (start-pulse.sh / start-logic-engine.sh / start-oracle-core.sh), or additional bidirectional-consistency lints] | ✓ shipped | R400 (handoff 006 refresh + verbatim-surface.md regen + mandate row consolidation) |
| E10.M43 | **ZFS ARC clamp operator-verbatim §4.2 content lint + bidirectional consistency with verify-grid** — extends R387-R398 operational-artifact pinning to `scripts/hooks/post-install/zfs-arc-clamp.sh`. `tests/lint/test_zfs_arc_clamp_verbatim.py` (10 assertions): script exists + zfs_arc_max parameter referenced + 128GB default clamp (§4.2 verbatim 50% of operator's 256GB target) + 1024^3 multiplier correct (catches 1000^3 SI-vs-IEC drift = 7.4% off byte value) + /etc/modprobe.d persistence (without it, ARC clamp doesn't survive reboot) + 'options zfs zfs_arc_max=' format verbatim + /sys/module/zfs runtime application (active value, not just boot config) + arc_min smaller than arc_max sanity + master spec §4.2 reference in comments + **bidirectional consistency with verify-grid.yaml**: the operator-verbatim 137438953472 (= 128 GiB) byte value MUST appear in BOTH zfs-arc-clamp.sh (writer) AND verify-grid.yaml (boot-time verifier). Side-fix: added §4.2 master spec reference + 137438953472 verbatim to the zfs-arc-clamp.sh script header comment. **Cumulative SDD-037 family: 200 → 210 assertions** (23 lints now). [from /goal re-process — §4.2 ZFS ARC clamp is operator's 'memory partitioning' contract; pinning verbatim ensures the 128GB:128GB split between ZFS-ARC and AI-workload RAM stays at operator-named ratio across both modprobe.d boot config and runtime /sys parameters] | ✓ shipped | R399 (tests/lint/test_zfs_arc_clamp_verbatim.py + 10 L1 assertions + §4.2 reference added to zfs-arc-clamp.sh comment) |
| E10.M42 | **VFIO-bind operator-verbatim §4.3 content lint** — extends R387-R397 operational-artifact pinning to `scripts/hooks/post-install/vfio-bind-3090.sh` (master spec §4.3 verbatim GRUB cmdline). `tests/lint/test_vfio_bind_verbatim.py` (10 assertions): file exists + vfio-pci.ids= parameter referenced + amd_iommu=on present (§4.3 REQUIRED for AMD platform IOMMU) + iommu=pt passthrough mode (§4.3 verbatim) + GRUB_CMDLINE_LINUX_DEFAULT target variable + vfio-pci module options/modprobe path + PCI ID format well-formed (XXXX:XXXX hex) + GRUB regeneration trigger (update-grub/grub-mkconfig — without it cmdline mutations don't take effect on boot) + master spec section reference + **no silent IOMMU disable** (catches `amd_iommu=off` or `iommu=off` which would break VFIO completely — secondary GPU bleeds into host = security perimeter violation per operator §17 dual-GPU SRP). **Cumulative SDD-037 family: 190 → 200 assertions** (22 lints now). [from /goal re-process — VFIO-bind is operator's §4.3 'isolation' contract; pinning verbatim prevents silent GPU-isolation breakdown that would corrupt the operator-named §17 dual-GPU SRP architecture] | ✓ shipped | R398 (tests/lint/test_vfio_bind_verbatim.py + 10 L1 assertions pinning §4.3 verbatim VFIO GRUB cmdline parameters + IOMMU enable + grub regeneration trigger) |
| E10.M41 | **Trinity-side systemd unit Description verbatim lint** — extends R387-R396 operational-artifact pinning to the 4 Trinity-side service units' Description= identity strings. `tests/lint/test_trinity_unit_descriptions.py` (9 assertions): all 4 units exist (Pulse / Logic Engine / Oracle Core / Router) + Pulse Description references bitnet.cpp + CCD 0 + ternary/CPU inference + Logic Engine Description references RTX 3090 + VFIO + vLLM/llama.cpp + Oracle Core Description references Blackwell + vLLM/DFlash + Router Description references inference + OpenAI/router + all Descriptions start with 'sovereign-os ' project prefix + all have em-dash or paren runtime-detail separator + all ≥40 chars (substantive identity, not stub) + operator-named §17.1 modules (Pulse / Logic Engine / Oracle Core / router) appear across the 4 Descriptions. Catches: silent Pulse → "Inference Server" rename, RTX 3090 → "GPU 0" loss of specific SKU, ternary → "CPU" loss of operator-named compute distinction. The systemctl list-units surface is operator-discovery; Description drift silently corrupts what operator sees in `systemctl list-units --type=service`. **Cumulative SDD-037 family: 181 → 190 assertions** (21 lints now). [from /goal re-process — Description strings on systemd units are operator-readable identity content; R171 covered hardening directives but not Description content; R397 closes that drift surface] | ✓ shipped | R397 (tests/lint/test_trinity_unit_descriptions.py + 9 L1 assertions pinning §17.1 module identity across 4 Trinity-side service units) |
| E10.M40 | **ZFS dataset operator-verbatim §4.1 + §3 content lint** — extends R387 (sain-01 profile pin) with deep ZFS dataset spec coverage. `tests/lint/test_zfs_datasets_verbatim.py` (14 assertions): profile has zfs datasets section + tank/models dataset present with recordsize=1M lz4 redundant_metadata=most + tank/context dataset present with recordsize=16k zstd-9 copies=2 (state-fabric durability) + tank/agents dataset present with recordsize=128k zstd-3 + no silent compression downgrade (catches lz4→none, zstd-9→zstd-1, zstd-3→zstd-1 drift across all 3 datasets) + storage throughput target documented (31.5 GB/s OR raid0 layout from §1.2 verbatim). Each per-dataset assertion catches operator-named per-access-pattern tuning drift: tank/models smaller recordsize → fragmented 100GB+ weight reads; tank/context losing copies=2 → state-fabric durability gap; tank/agents zstd-3 → zstd-1 silently weakens compression. **Cumulative SDD-037 family: 167 → 181 assertions** (20 lints now). [from /goal re-process — R387 pinned KCFLAGS+SKUs in profile but not the §4.1 ZFS Storage Tuning Matrix; R396 extends sain-01.yaml lint coverage to the per-dataset access-pattern tuning operator named per-storage-class] | ✓ shipped | R396 (tests/lint/test_zfs_datasets_verbatim.py + 14 L1 assertions covering 3 datasets × recordsize+compression+copies+redundant_metadata) |
| E10.M39 | **Handoff doc 006 extended to cover R381-R394** — `docs/handoff/006-verbatim-preservation-arc.md` updated from "R355-R380 complete" to "R355-R394 structurally mature (perpetual mandate continues)". Title + status + last-updated + final state stats refreshed. New §"Post-R381 extensions (R382-R394)" section adds 13-row table mapping each round to its surface + bugs caught (R383+R384+R391 caught 3 bugs total). 11 enforcement layers → 14 enforcement layers (R387-R394 added L0 build profile data / L0 whitelabel render data / L0 bootstrap YAML data / L0 Trinity-side scripts; R383 added L1 discoverability layer). State preservation for next session/agent continues: full 40-round arc state is now accessible from handoff doc without needing to scan mandate file. [from /goal re-process — R381 shipped handoff 006 for R355-R380; R382-R394 work was 14 more rounds + 10 new lints + 100 assertions + Trinity-side pinning trio; handoff doc needed refresh to stay operational for next-session cold-start] | ✓ shipped | R395 (docs/handoff/006-verbatim-preservation-arc.md extended with post-R381 §) |
| E10.M38 | **Pulse build-bitnet operator-verbatim §16+§9.1+§15 content lint — closes Trinity-side pinning** — completes the Trinity-side script pinning trio (R392 Auditor + R393 Weaver + R394 Pulse). `tests/lint/test_pulse_bitnet_build_verbatim.py` (11 assertions): build-bitnet.sh exists + -march=znver5 verbatim (§16+§9.1 — Pulse operator-named CPU arch) + all 6 -mavx512* extensions present (§16+§9.1 verbatim list cardinality) + no silent -march= corruption to non-znver5 + bitnet.cpp referenced (§17.1 Pulse runtime) + microsoft/bitnet-b1.58 model id (§15 BitNet b1.58 ternary verbatim) + -O3 optimization (§16+§2.2 verbatim) + master spec section refs in comments (operator-discovery context) + no silent optimization downgrade to -O0/-Os/-Og + CCD 0 / 0-5 / 0-11 / taskset / affinity context (operator-discovery only — Pulse build doesn't strictly pin CCD; start-pulse.sh handles runtime affinity) + /usr/local/bin install path (operator convention matching Tetragon allowlist). **Cumulative SDD-037 family: 156 → 167 assertions** (19 lints now). **Trinity-side pinning complete: Pulse + Weaver + Auditor all pinned at L0 operational-artifact layer.** [from /goal re-process — closes the Trinity-side script pinning that started with R392 Auditor + R393 Weaver; all 3 operator-named Trinity runtime scripts now have operator-verbatim §-section content lint-pinned] | ✓ shipped | R394 (tests/lint/test_pulse_bitnet_build_verbatim.py + 11 L1 assertions pinning §16+§9.1+§15 verbatim CFLAGS + 6-flag extension list + bitnet.cpp model id + -O3 + install path) |
| E10.M37 | **Weaver atomic-state operator-verbatim §21 content lint** — extends R387-R392 operational-artifact pinning to the Trinity Weaver side: `scripts/weaver/atomic-state.py` (master spec §21 Atomic State Transition Protocol Python primitive). `tests/lint/test_atomic_state_verbatim.py` (10 assertions): file exists + `def commit_state_atomically` operator-verbatim function name + O_DIRECT flag (§21.1 'bypass volatile OS page caches') + O_SYNC flag (synchronous write guarantees physical block commit) + O_WRONLY/O_CREAT/O_TRUNC flag combo + os.rename atomic-rename pattern (§21.1 'no reader ever views a partially written file') + 4K boundary alignment (§21.1 'NVMe physical block alignment') + tmp file pattern + /mnt/vault/context path family referenced + no silent fsync replacement (catches: agent silently swaps O_DIRECT+O_SYNC for fsync-only which is a WEAKER guarantee — page cache could still have stale data when fsync returns). **Cumulative SDD-037 family: 146 → 156 assertions** (18 lints now). [from /goal re-process — atomic-state.py is operator's §21 'lockless loopback write sequence on the ZFS layer'; pinning verbatim prevents silent guarantee-weakening across the operator's multi-agent state synchronization contract] | ✓ shipped | R393 (tests/lint/test_atomic_state_verbatim.py + 10 L1 assertions pinning §21.1 verbatim Python primitive + flag combos + atomic rename pattern) |
| E10.M36 | **Guardian Core operator-verbatim §10 content lint** — extends R387/R388/R389/R390/R391 operational-artifact pinning to `scripts/auditor/guardian-core.py` (master spec §10 Native Guardian Event Loop). `tests/lint/test_guardian_core_verbatim.py` (10 assertions): guardian-core.py exists + `def alert_and_neutralize` function name verbatim (§10.1 operator-named perimeter response handler) + `/var/run/tetragon/tetragon.events` socket path verbatim + SIGKILL action handling + JSON event stream parsing (§10.1 'raw JSON stream from the kernel eBPF filter') + podman kill on container_id + audit log append (§10.1 'Append to Atomic Sovereign Logs') + main() entry-point + no silent action drift (catches: SIGTERM replacing SIGKILL — softer signal would weaken perimeter) + kernel/eBPF/Tetragon provenance documented. **Cumulative SDD-037 family: 136 → 146 assertions** (17 lints now). The Guardian Core is operator's §10 'autonomous circuit breaker'; pinning verbatim prevents silent weakening of the kernel-perimeter response. [from /goal re-process — guardian-core.py is the Python primitive operator showed in §10.1; pinning verbatim function name + socket path + SIGKILL action closes the silent-perimeter-weakening drift surface for the Auditor side of the Trinity] | ✓ shipped | R392 (tests/lint/test_guardian_core_verbatim.py + 10 L1 assertions pinning §10.1 verbatim Python primitive structure) |
| E10.M35 | **Friction-audit operator-verbatim §5.1 content lint + ZFS check gap closed** — extends R387/R388/R389/R390 operational-artifact pinning to `scripts/hooks/post-install/friction-audit-runtime.sh` (master spec §5.1 implementation). `tests/lint/test_friction_audit_verbatim.py` (11 assertions): runtime audits PCIe x8 LnkSta width (§5.1 step 1) + ZFS pool health via zpool (§5.1 step 2 — **CAUGHT REAL BUG**: this check was missing) + M.2_2 remediation hint (operator-verbatim 'Verify if M.2_2 slot is populated, interfering with lane paths.') + exit 1 on FAIL (§5.1 'execution loops cease instantly') + 'CRITICAL ARCHITECTURAL FRICTION' severity language + spec script documents audit contract + x8 lane target + skip-warn handling + variable usage safety. **CAUGHT REAL BUG #23**: friction-audit-runtime.sh implemented only 2 of operator's 3 §5.1 checks (CPU + PCIe + Memory, missing ZFS pool integrity). R391 added the missing ZFS pool check with operator-verbatim 'CRITICAL ARCHITECTURAL FRICTION — Storage Pool Anomalies Discovered' language + graceful container skip when zpool not installed. **Cumulative SDD-037 family: 125 → 136 assertions** (16 lints now). [from /goal re-process — friction-audit is operator's §5.1 'protects its execution lifecycle via an immutable boot-time script' — operator's 3rd check was unimplemented for the entire R-arc until R391 lint caught it] | ✓ shipped | R391 (tests/lint/test_friction_audit_verbatim.py + 11 L1 assertions + ZFS pool check added to friction-audit-runtime.sh) |
| E10.M34 | **Tetragon policy operator-verbatim structure lint** — extends R387+R388+R389 operational-artifact pinning to `scripts/hooks/post-install/tetragon-policy-load.sh`. R367 already covered the 4-binary allowlist bidirectional (C-14 concept ↔ shipped policy); R390 pins the FULL TracingPolicy structure from master spec §4.1: apiVersion (cilium.io/v1alpha1) + kind (TracingPolicy — catches silent drift to TracingPolicyNamespaced or NetworkPolicy) + metadata.name (sovereign-kernel-fence) + kprobes call (sys_execve or __x64_sys_execve) + syscall: true flag + matchActions Sigkill (catches silent drift to NoOp/Log/Audit/Allow that would disable the perimeter) + NotIn operator (catches inversion: 'In' would invert the policy semantic from REJECT-by-default to ALLOWLIST-by-default — exact opposite of operator intent) + 4-binary allowlist complete + canonical /etc/tetragon/tracing-policies/sovereign-kernel-fence path consistency. **Cumulative SDD-037 family: 115 → 125 assertions** (15 lints now). Critical security pin: if a future agent silently changes `action: Sigkill` to `action: NoOp` the kernel-level container perimeter silently breaks; this lint catches at push. [from /goal re-process — operator's §4.1 TracingPolicy is load-bearing security content; pinning the full structure (not just allowlist) closes the silent-perimeter-disable drift surface] | ✓ shipped | R390 (tests/lint/test_tetragon_policy_verbatim.py + 10 L1 assertions pinning full §4.1 TracingPolicy structure including Sigkill action + NotIn semantic + apiVersion + kind + metadata.name) |
| E10.M33 | **Bootstrap YAML operator-verbatim content lint (§22 verify-grid + §12 phases)** — extends R387+R388 operational-artifact pinning pattern to bootstrap config YAMLs. `tests/lint/test_bootstrap_verbatim_content.py` (12 assertions): config/bootstrap/verify-grid.yaml has exactly 6 checks (§22 "Master Bootstrap Verification Checklist" — operator-verbatim cardinality) + IDs 01..06 + each check name matches operator-verbatim §22 keywords (Microcode/ISA, Bus Geometry, Linux Memory, Driver Fabric, Security Core, Network Line) + §22.3 ARC max value 137438953472 bytes / 128 GiB referenced + §22.6 jumbo MTU=9000 verbatim; config/bootstrap/phases.yaml has exactly 5 phases (§12 "Chronological Vision" — operator-verbatim) + IDs I/II/III/IV/V Roman numerals verbatim + Phase II references Zen 5 / znver5 + kernel (§12 'The Zen 5 Kernel Compilation Engine') + Phase V references tetragon / guardian (§12 'Multi-Agent Mission Control & Guardian Loop Activation') + all phases have preconditions/postconditions (§12 invariant 'Each phase must be completed and validated before the downstream phase is initiated'). Existing R207/R203 lints check SCHEMA; R389 pins operator-VERBATIM content. Silent drift would mean: agent renames Phase II or removes §22.3 ARC check, the bootstrap pipeline executes content not matching operator master spec. **Cumulative SDD-037 family: 103 → 115 assertions** (14 lints now). [from /goal re-process — verify-grid.yaml + phases.yaml are the operational manifestation of master spec §22 + §12; pinning verbatim content closes the master-spec-to-pipeline drift surface for the build verification + chronological phases YAMLs] | ✓ shipped | R389 (tests/lint/test_bootstrap_verbatim_content.py + 12 L1 assertions covering §22 6-check + §12 5-phase verbatim content) |
| E10.M32 | **whitelabel/default.yaml operator-verbatim motd lint** — extends R387 operational-artifact pinning pattern to `whitelabel/default.yaml`. `tests/lint/test_whitelabel_default_verbatim.py` (8 assertions): file exists + ≥30 lines + motd line 1 verbatim ('quality over quantity' + 'honesty over cheats and lies' from master spec §3.2) + motd line 2 verbatim ('hacks, quick fixes' + 'shortcuts') + os-release identity strings ('Sovereign OS' + 'sovereign') + quality-bar phrase preserved + no silent paraphrase forbidden-list (catches 'quality before quantity' / 'quick fixes and shortcuts' compression patterns) + 2-sentence quote preserved adjacent (≤200 chars apart — operator's 2-line motd not split). The render pipeline (R206 whitelabel render) consumes this YAML to materialize /etc/motd on the installed system; silent drift would ship an OS image without operator's voice. Cumulative SDD-037 family: 95 → 103 assertions (13 lints now). [from /goal re-process — operator-verbatim motd quote is identity content; pinning it ensures the OS-image-render pipeline never silently strips operator's "Quality over Quantity | Honesty over Cheats" Standard] | ✓ shipped | R388 (tests/lint/test_whitelabel_default_verbatim.py + 8 L1 assertions pinning motd + os-release identity + paraphrase forbidden-list + 2-line-adjacency) |
| E10.M31 | **sain-01 profile verbatim-content lint (build pipeline drift catch)** — `tests/lint/test_sain01_profile_verbatim.py` (9 assertions): pins operator-verbatim §2.2 KCFLAGS in `profiles/sain-01.yaml` (full 11-flag list: -march=znver5 + -O3 + -pipe + -mabm + -madx + 6 -mavx512* extensions) + KCPPFLAGS (-march=znver5) + §1.1 hardware SKUs (verbatim form OR slug form acceptable per YAML identifier convention) + Linux 6.12+ kernel target (§2.1 Blackwell/Zen5 native support) + all 6 -mavx512* extensions present (cardinality check) + no non-znver5 -march= value (catches silent corruption to znver4/generic) + dual-GPU layout (RTX PRO 6000 + RTX 3090) present + §1.2 M.2_2 slot empty constraint documented with x8/x8 / bifurcation reason. This pins the BUILD PROFILE against silent drift — if a future edit silently shortens KCFLAGS or removes M.2_2 constraint, the build pipeline would ship a kernel that doesn't match operator intent. **Cumulative SDD-037 family: 86 → 95 assertions** (12 lints now). [from /goal re-process — `profiles/sain-01.yaml` is the operational manifestation of master spec §1+§2.2 verbatim content but had no lint pinning the verbatim strings; this is the inverse of R373 catalog cross-check applied to the build pipeline] | ✓ shipped | R387 (tests/lint/test_sain01_profile_verbatim.py + 9 L1 assertions pinning KCFLAGS + hardware SKUs + dual-GPU + M.2_2 constraint) |
| E10.M30 | **Unified search verb across 3 catalog taxonomies** — `sovereign-osctl search <needle>` queries architecture-qa (4 Q-NN + 3 G-NN + 27 C-NN) + coverage-map (32 A-NN) + layers (11 layer-NN) in ONE command. Before R386, operator had to run 3 separate searches. After R386, one verb returns unified ranked results with per-catalog match counts + drill verbs routing to the right `show <id>` command. Ranking: exact ID match first (e.g., `search C-04` ranks the literal C-04 concept first); title-prefix match second; substring-only third. Each result schema: catalog (architecture-qa / coverage-map / layers) + category (question / gotcha / concept / axis / layer) + id + title + drill_verb + spec_ref/status. NEVER-raises on missing catalog module (graceful degradation: matches=0 for that catalog, others still searched). Operator typo discoverability preserved: `search experiece` (operator typo) finds the layer; the typo-preserved L3 rule means searching with corrected spelling 'experience' doesn't replace it. 12-assertion L3 covers: 3-catalog coverage + drill verb routing + ID-match ranking + operator-typo discoverability + no-match rc=1 + multi-catalog hit + NEVER-raise on missing module + osctl dispatch + usage error rc=2 + typo discoverability + JSON schema + human renderer. [from /goal re-process — operator running 3 separate searches across catalogs was friction; unified search reduces 3 verbs → 1] | ✓ shipped | R386 (scripts/intelligence/unified-search.py + sovereign-osctl search + 12 L3 assertions) |
| E10.M29 | **config example file consistency lint** — `tests/lint/test_config_example_files.py` (7 assertions): every `config/<name>.toml.example` MUST exist + be valid TOML (parseable by tomllib) + have ≥10 lines (operator-readable floor) + start with `#` header comment in first 5 lines + mention 'operator' / 'overlay' / '/etc/' (operator-discovery guidance) + have corresponding `load_with_overlay('name', ...)` caller in scripts/ (no orphan examples). Allowed-orphans list documents 9 legacy examples consumed via non-overlay paths (dashboard-auth / gpu-policy / install-layers / kernel-tuning / known-boards / notify / power / ram / shutdown-manifest). Catches: stale example with TOML syntax error after refactor; orphan example after script rename; stub example without operator-readable content. Cumulative SDD-037 family: 79 → 86 assertions (10 lints → 11 lints). [from /goal re-process — 51 overlay-using scripts vs 16 example files exposed a gap, but requiring examples for ALL 51 was too aggressive; instead, this lint enforces QUALITY of the EXISTING examples + no orphans] | ✓ shipped | R385 (tests/lint/test_config_example_files.py + 7 L1 assertions) |
| E10.M28 | **handoff INDEX consistency lint** — bidirectional consistency check for `docs/handoff/INDEX.md` ↔ `docs/handoff/*.md` files. `tests/lint/test_handoff_index_consistency.py` (6 assertions): handoff dir exists + INDEX exists + every NNN-slug.md file referenced from INDEX + every INDEX entry has a real file (no dead links) + 006-verbatim-preservation-arc.md present + sequential numbering starting from 001 with no duplicates. **CAUGHT 1 REAL BUG**: 005-master-spec-materialization-arc.md was in docs/handoff/ since R-arc 005 era but was MISSING from INDEX.md (would have been invisible to next session reading INDEX as the cold-start signpost). Fixed by adding the 005 row in INDEX. Cumulative SDD-037 family: 73 → 79 assertions (R367 12 + R368 7 + R370 9 + R371 7 + R372 8 + R373 12 + R374 6 + R380 6 + R383 6 + R384 6). Pattern mirrors test_sdd_index_consistency.py (existing SDD INDEX lint) and SDD-037's bidirectional drift catches (Tetragon allowlist C-14 ↔ shipped script). [from /goal re-process — R381 shipped handoff 006 but didn't catch the 005 INDEX-omission gap; R384 cross-validation pattern catches it + prevents future drift] | ✓ shipped | R384 (tests/lint/test_handoff_index_consistency.py + INDEX.md row added for 005 + 6 L1 assertions) |
| E10.M27 | **osctl --help R-arc verb discoverability + lint enforcement** — operator running `sovereign-osctl --help` did NOT see any of the 10 R355-R382 arc verbs (architecture-qa / ccd-pinning / state-fabric / network-topology / coverage / repl / verbatim-render / doctrine-status / quarterly-review / layers) because the help heredoc never got updated. They DISPATCHED via the case statement but were invisible to operator discovery. R383 closes that: adds a "R355-R382 /goal contract enforcement (operator-pull discovery surface)" section in `cmd_help()` listing all 10 verbs with their subverbs + spec section refs + R-number annotations + 3 EXAMPLES lines for architecture-qa concepts / coverage audit / quarterly-review snapshot. L1 lint `tests/lint/test_osctl_help_includes_r_arc_verbs.py` (6 assertions): osctl --help runs + produces ≥1000 chars; every R-arc verb appears in help output; R355-R382 anchor section header present; EXAMPLES includes ≥1 arc verb; every help-listed verb has a dispatch case (overlap with R372 from operator-discovery direction); help section well-formed (≥10 entries with `[sub]` markers + R-number annotations). Closes a discoverability gap that would have made all the verb work invisible to operator running `--help`. **Cumulative SDD-037 L1 enforcement: 67 → 73 assertions** (R367 12 + R368 7 + R370 9 + R371 7 + R372 8 + R373 12 + R374 6 + R380 6 + R383 6). [from /goal re-process — caught real discoverability gap; operator demand for "guide into experiece" is meaningless if the verbs aren't surfaced through --help] | ✓ shipped | R383 (scripts/sovereign-osctl help section + tests/lint/test_osctl_help_includes_r_arc_verbs.py + 6 L1 assertions) |
| E10.M26 | **Operator 'guide into' layer enumeration verb** — `sovereign-osctl layers list/show/search` ships the operator-verbatim 11-layer 'guide into' enumeration from the 2026-05-17 hook drop opening sentence as a discoverable operator-pull verb. The 11 layers preserved verbatim in exact operator order: experiece (operator typo for "experience" PRESERVED per SDD-037 typo-preservation rule) / field / kernel / hardware / OS / modules / features / services / configurations / personalisations / customizations. Each layer carries: layer_verbatim ("into the X" / "the X" operator phrasing); operator_note (operator-readable rationale); implementing_verbs (≥1 verb mapping to discoverable surface); guide_topic_match (cross-link to R349 guide.py topic if direct mapping exists, else None); spec_ref. Cross-links: kernel/hardware → R349 guide topics; OS → architecture-qa C-22 (Debian-as-Ark); modules → R349 selfdef topic + R351 module-state; customizations → architecture-qa C-13/C-26/C-27 (3 runtime profiles); personalisations → whitelabel + C-18 package list. L3 #2 enforces typo preservation (catches if 'experiece' gets silently corrected to 'experience'). L3 #3 enforces exact operator order. L3 #13 enforces ≥5 layers cross-link to architecture-qa C-NN concepts (catalog connectivity). Closes operator's verbatim "guide into the experiece, into the field, into the kernel..." enumeration as a first-class discoverable surface. [from /goal re-process — R349 covered some operator layers (kernel/hardware) as guide topics but the FULL 11-layer enumeration including operator's typos was not explicitly catalogued; this round closes that gap] | ✓ shipped | R382 (scripts/intelligence/layers.py + sovereign-osctl layers + 13 L3 assertions including typo-preservation enforcement) |
| E10.M25 | **Operator handoff doc for verbatim-preservation arc** — `docs/handoff/006-verbatim-preservation-arc.md` (new) — 250-line cold-start handoff doc summarizing the entire 26-round R355-R380 verbatim-preservation arc for next-session operator/agent. Documents: what the arc was (/goal directive 2026-05-18 response); what got built (7 operator-pull verbs + 2 meta-verbs + 1 static published doc + 1 codified doctrine SDD-037 + 6 fabrication-catch L1 lints + 4 supporting L1 lints); final state (32 ✓ shipped axes / 82 catalogued items / 67 L1 assertions across 8 lints / 20 bugs caught / grade A); 11 enforcement layers; 19 master spec sections covered as concepts; how to extend (SDD-037 7-step contract); explicit scope-exclusions (no Stage 2+ implementations / no L0 raw dump modification / no completion promise per perpetual mandate); 18 critical file paths; operator-pull entry points table; open questions deferred to next arc. Updates `docs/handoff/INDEX.md` with the new entry. Next session/agent reads 006 → knows exact current state without re-deriving the pattern. [from /goal re-process — preserves 26-round work for future continuity; the perpetual mandate continues per operator's "DO not stop" but a clean handoff anchor enables session-resilience] | ✓ shipped | R381 (docs/handoff/006-verbatim-preservation-arc.md + INDEX.md updated) |
| E10.M24 | **SDD reachability validator (inverse of R372 SDD ref check)** — extends SDD-037 lint family with the INVERSE-direction check from R372: R372 catches catalogs citing fabricated SDD-NNN; R380 catches SDD-NNN docs that exist but are NEVER cited from any catalog (operator who didn't write them can't find them). `tests/lint/test_sdd_reachability.py` (6 assertions): SDD directory exists + ≥30 SDDs shipped + every SDD ≥030 (post-doctrine era) is reachable from architecture-qa / coverage-map / mandate / SUMMARY.md (4 paths) + no dangling SDD references in catalogs (overlap with R372 — bidirectional defense) + recent SDDs ≥030 cited in mandate (operator-facing surface) + SDD-037 reachable via mandate explicitly. **REACHABILITY_EXEMPTIONS = {000..029}** — pre-SDD-030 SDDs were authored before R283 overlay doctrine and the catalog-wiring convention; legacy exempt. Post-doctrine SDDs (≥030) MUST be discoverable. R376 doctrine-status lint family updated 7 → 8 lints. Drift modes catalog updated 17 → 18 (new: 'Unreachable SDD docs authored without catalog wiring'). **Cumulative SDD-037 L1 enforcement: 61 → 67 assertions** (R367 12 + R368 7 + R370 9 + R371 7 + R372 8 + R373 12 + R374 6 + R380 6). [from /goal re-process — R372 caught fabricated SDD refs; R380 catches the opposite direction (SDDs that exist but aren't reachable). Symmetric defense against doctrine-doc orphaning] | ✓ shipped | R380 (tests/lint/test_sdd_reachability.py + 6 L1 + doctrine-status.py R380 entry + R376 L3 updated for 8-lint family) |
| E10.M23 | **Architecture-qa concepts C-26 + C-27 (§18 Load Balancing Profiles 1 + 3 verbatim)** — concept catalog extended 25 → 27 with the 2 remaining §18 runtime profiles operator-verbatim. C-26 Profile 1 Ultra-Sovereign Efficiency Mode (CPU Focused) — verbatim `taskset -c 0-7 bitnet-cli -m ./models/bitnet_b1_58_3b/ggml-model-i2.gguf -p "Evaluate state transition from CLAUDE.md" --threads 8 --memory-f32` with nvidia-smi -pm 1 GPU sleep + Persistence Mode + AVX-512 pipeline + CCD-0 pinning rationale verbatim. C-27 Profile 3 Deep Context Synthesis Mode (Unified Memory Span) — verbatim `podman run --device nvidia.com/gpu=all -v /mnt/vault/models:/models:ro vllm/vllm-openai:latest --model /models/DeepSeek-V3-Quant --tensor-parallel-size 2 --pipeline-parallel-size 1 --gpu-memory-utilization 0.95 --kv-cache-dtype fp8` with layer 0-30 ↔ GPU 0 + layer 31-80 ↔ GPU 1 + KV cache 4-bit width allocation scheme. Previously C-13 only covered §18 Profile 2 (Asymmetric_Burst); §18 has 3 profiles → catalog now covers all 3. Coverage 32/32 ✓ shipped + grade A unchanged. [from /goal re-process — closes the missing 2/3 of §18 operator-verbatim runtime profiles] | ✓ shipped | R379 (architecture-qa.py concepts 25→27 + verbatim-surface.md regenerated) |
| E10.M22 | **Coverage-map A-31 + A-32 (operator mindset/workflow contract axes)** — closes 2 operator-stated axes from the hook drop not yet first-classed: A-31 "Senior Architect DevOps Software Engineer Fullstack Expert & Mindset. Always a strong workflow and non-blocking but always toward the goal(s). Apply what I said at scale and you have for a very long time of work. Take your time, do this right." (operator MINDSET + WORKFLOW contract) and A-32 "I trust you to break down planify and continue with the SDD and TDD" (operator delegation contract). Both mechanized via SDD-033 (perpetual-intake doctrine) + SDD-037 (verbatim-preservation) + SDD-028 (phases.yaml break-down) + the fabrication-catch quintet. A-31 specifically grounds the operator-stated quality bar phrase as a verbatim coverage entry, making it queryable via `sovereign-osctl coverage show A-31`. R377 grade A audit still passes after adding 2 axes (coverage now 32/32 ✓ shipped). [from /goal re-process — operator's verbatim quality-bar/mindset phrasing was implicit doctrine but not catalogued as discoverable verb-mapped axes; this closes that gap] | ✓ shipped | R378 (coverage-map A-31 + A-32 + docs/src/verbatim-surface.md regenerated; quarterly-review snapshot updates to 32/32 ✓ shipped automatically) |
| E10.M21 | **Quarterly-review meta-audit** — `sovereign-osctl quarterly-review snapshot/grade/recent` composes coverage-map audit + doctrine-status + verbatim-render summary + mandate stats + git-history rounds into one consolidated operator-readable verdict suitable for periodic review snapshots. snapshot returns full report with letter grade (A/B/C/D/F) + headline issues. grade returns letter only. recent --since R<N> returns commits since round N (default R350 = verbatim-preservation arc start). Grade thresholds: A = 0 TODO + 0 critical issues; B = ≤2 partial; C = TODO present OR >2 partial; D = lint family below floor OR verbatim catalog below 70; F = mandate file missing. **Current state grades A**: 30/30 ✓ shipped coverage axes, 7 lints × 61 assertions × 20 bugs caught, 78 verbatim items, ~506 phrases, 162 mandate rows / 112648 bytes, 27 commits shipped since R350. rc=0 on A/B; rc=1 on C/D/F (operator-actionable signal). 12 L3 assertions cover schema + current-A-grade + each composed source rolled up + recent verb + rc semantics + NEVER-raise on missing composed source (graceful degradation: error field instead of crash). [from /goal re-process — R285 quarterly review SDD mentioned this verb but it didn't exist; closing the meta-discovery loop with a single operator-pull command answering "is sovereign-os healthy + has the catalog grown + has anything regressed?"] | ✓ shipped | R377 (scripts/intelligence/quarterly-review.py + sovereign-osctl quarterly-review + 12 L3 assertions) |
| E10.M20 | **Doctrine-status meta-verb** — `sovereign-osctl doctrine-status status/tally/run` exposes the SDD-037 lint family health at a glance for operator audit. status (lightweight, no pytest): 7-lint catalog × declared assertion counts × cumulative-bugs-caught × per-lint purpose strings (≥50 chars operator-readable). tally: 17 drift modes catalog with named fabrication-catch surfaces (Fabricated §N / Fabricated mandate row / Fabricated sovereign-osctl verb / Cross-catalog phrase drift / Fabricated R<N>). run: executes pytest on all 7 SDD-037 lints + reports pass/fail per-lint + rc=1 on any failure. Without this verb, operator had to run pytest directly (developer-facing); now operator-pull "is my doctrine clean?" answers in one command. 12-assertion L3 covers: 7-lint family schema + R367..R374 round set + ≥17 drift modes + ≥20 cumulative bugs + run executes pytest cleanly + rc semantics + 3 human renderers + osctl dispatch + unknown-subverb rc=2 + purpose strings ≥50 chars + 5 fabrication surfaces named in tally + status human markers. [from /goal re-process — after R367-R374 built the lint family, operator-pull "doctrine status at a glance" was missing] | ✓ shipped | R376 (scripts/intelligence/doctrine-status.py + sovereign-osctl doctrine-status + 12 L3 assertions) |
| E10.M19 | **Architecture-qa concepts C-24 + C-25 (§2 Sovereign Forge + §6 Implementation Ledger verbatim)** — extends concept catalog 23 → 25 with the 2 remaining un-catalogued master spec sections. C-24 §2 Sovereign Forge Bootstrap Forge (Stage 1 kernel compilation): operator-verbatim 64GB tmpfs setup + GCC 14 -march=znver5 -O3 flags + 6-mavx512* extension list + explicit CONFIG_* kernel options (CONFIG_MNATIVE_AMD / CONFIG_AMD_IOMMU / CONFIG_VFIO / CONFIG_VFIO_PCI / CONFIG_ZFS / CONFIG_PREEMPT / CONFIG_HZ 1000 / CONFIG_AQC111 / CONFIG_IGC / CONFIG_MT7925) + make -j24 deb-pkg + Linux 6.12+ + nvidia-open-kernel-dkms + atlantic 10GbE driver + mt7925 WiFi 7. C-25 §6 Implementation Ledger 4-item next-steps verbatim (Forge Initialization → ISO Synthesis → Physical Audit → Security Deployment) with explicit cross-link mapping to sovereign-os bootstrap phases II/III/V + §22 friction-audit verify. Concepts catalog now spans 19 distinct master spec sections (full L0 coverage of operator-verbatim explanatory blocks). Subsidiary R374 lint refinement: zero-padding check scoped to Rounds COLUMN only (so documentation examples like 'R5 not R005' in row text don't false-flag). [from /goal re-process — closes the last 2 un-catalogued operator-verbatim sections; concept catalog now covers full master spec breadth from §1 to §23 + Block 6 + dump-tail + macro-arc plan] | ✓ shipped | R375 (architecture-qa.py ARCHITECTURE_CONCEPTS extended 23→25 + R374 lint scoping refinement + verbatim-surface.md regenerated) |
| E10.M18 | **Round-reference validator** — extends R368+R371+R372+R373 fabrication-catch quartet with R374: every `R<N>` round reference in mandate Rounds column must be in active range (R1..R400) AND recent rounds (R350+) must have backing commit messages in git history AND each E10 mandate row's PRIMARY round (first R<N> in Rounds column) must not collide with another E10 row + no zero-padded round numbers (R5 not R005). `tests/lint/test_round_refs.py` (6 assertions): parses ≥100 mandate row Rounds columns; active range check (1..400); zero-padding catch; recent-rounds-in-git-history (R350+ MUST have backing commit); no primary R<N> collision in E10 epic; ≥150 total mandate rows (sanity backstop). Specifically catches: agent invents R999 in mandate without commit; typo R354 → R345; mandate claims R<N> but no commit of that round. Cumulative SDD-037 L1 enforcement: 55 → 61 assertions (R367 12 + R368 7 + R370 9 + R371 7 + R372 8 + R373 12 + R374 6). The **fabrication-catch quintet** (R368+R371+R372+R373+R374) now covers 5 distinct citation surfaces: §N sections / E.M rows / sovereign-osctl verbs / cross-catalog phrases / R<N> round numbers. [from /goal re-process — each round in the fabrication-catch series adds one more cross-reference axis; R374 completes the surfaces that mandate citations span] | ✓ shipped | R374 (tests/lint/test_round_refs.py + 6 L1 assertions covering active-range + zero-padding + git-history-backing + primary-collision + total-row-sanity + Rounds-column-parsing) |
| E10.M17 | **Cross-catalog phrase consistency validator** — extends R367 Tetragon-allowlist bidirectional check pattern to a broader set of operator-verbatim phrases that appear in multiple catalogs / shipped artifacts. `tests/lint/test_cross_catalog_phrase_consistency.py` (12 assertions): 'M.2_2 slot must remain empty' must appear in concept catalog AND friction-audit script; 'sync=always' must appear in concept catalog AND state-fabric ZFS properties; '31.5 GB/s' throughput target must appear in concepts; 'Marvell AQC113C' must appear in concepts AND network-topology; 'Intel I226-V' must appear in concepts AND network-topology; 'BindsTo=tetragon.service' must appear in concept catalog AND gotchas (C-07 + G-03 cross-link); 'CMK128GX5M2B6400C42' RAM SKU must appear in concept catalog AND coverage-map AND inventory-catalog; 'SMT2200C' UPS SKU must appear in coverage AND inventory; '990 EVO Plus' NVMe must appear in coverage AND inventory; 'Magician' with apostrophes preserved in concepts; Ryzen 9 9900X full SKU cross-catalog. Final assertion: no silent paraphrase forbidden-list (catches if 'blocking writes' appears without operator-verbatim 'synchronous writes' present — proves operator's exact phrasing wasn't silently swapped). **CAUGHT 2 REAL COVERAGE GAPS**: C-16 hardware concept was missing Intel I226-V (operator's §8 management NIC) AND CMK128GX5M2B6400C42 RAM SKU (operator's §1b hardware-spec drop); fixed by extending C-16 with explicit §1b hardware additional SKUs (RAM + NVMe + UPS + PSU) verbatim. Cumulative SDD-037 L1 enforcement: 43 → 55 assertions (R367 12 + R368 7 + R370 9 + R371 7 + R372 8 + R373 12). [from /goal re-process — R372 caught fabricated VERB refs; this round catches operator-verbatim PHRASE drift across catalogs] | ✓ shipped | R373 (tests/lint/test_cross_catalog_phrase_consistency.py + 12 L1 + C-16 hardware concept extended with §1b additional SKUs + verbatim-surface.md regenerated) |
| E10.M16 | **Verb-dispatch + SDD reference validator (extends SDD-037)** — extends R367+R368+R370+R371 SDD-037 lint family with `tests/lint/test_verb_dispatch_refs.py` (8 assertions): every `sovereign-osctl <subverb>` referenced in coverage-map `implementing_verbs` + architecture-qa gotcha `related_verbs` + repl mode `reference_commands` MUST have a corresponding `case` dispatch in `scripts/sovereign-osctl`. R368 checked the prefix; R372 closes the gap by checking the subverb actually dispatches. Plus: every `sdd_refs: ["NNN"]` cited in coverage-map MUST correspond to a real `docs/sdd/NNN-*.md` file. Plus: no duplicate dispatch cases in osctl. Plus: SDD directory canonical files exist. **CAUGHT 16 REAL FABRICATED VERB REFERENCES**: A-07 net-state/ingress-advisor (→ network/network-stack), A-08 install-mode-advisor (→ install-mode), A-10 raid-status/raid-operate/raid-config (→ raid), A-11 logs/log-rotate (→ insights/fs), A-13 charter (→ architecture-qa show C-22), A-17 net-state/perimeter-check (→ network/perimeter), A-19 pcie-lanes/vfio-bind (→ pcie-policy/pcie-lane-detect), A-26 pcie-lanes (→ pcie-policy); G-01 pcie-lanes (→ pcie-policy); G-03 tetragon-status/net-state (→ perimeter/network). All 16 were stale assumptions about verb names from agent paraphrasing; the live osctl uses different actual names. Lint message identifies the offending axis + verb + remediation hint. **Cumulative SDD-037 L1 enforcement: 35 → 43 assertions** (R367 12 + R368 7 + R370 9 + R371 7 + R372 8). [from /goal re-process — R371 caught fabricated mandate-row refs via the same cross-validation pattern; R372 extends to verb-dispatch references catching even more fabrication mode than the mandate-row variant did] | ✓ shipped | R372 (tests/lint/test_verb_dispatch_refs.py + 8 L1 + 16 verb-ref corrections in coverage-map A-07/A-08/A-10/A-11/A-13/A-17/A-19/A-26 + architecture-qa G-01/G-03 + verbatim-surface.md regenerated) |
| E10.M15 | **Mandate-row reference validator (extends SDD-037 enforcement)** — extends R367+R368+R370 SDD-037 lint family with `tests/lint/test_mandate_row_refs.py` (7 assertions): every `mandate_rows: ["E<N>.M<N>"]` reference in any coverage-map A-NN axis MUST point to a row that actually exists in `docs/standing-directives/2026-05-17-operator-mandate.md`. Catches: fabricated row IDs (E1.M999), typos (E1.M21 vs E1.M22), and stale references after row rename/delete. Pattern mirrors R368's `master spec §N` validity check. **CAUGHT 2 REAL BUGS during shipping**: (1) `E2.M21` was duplicated in the mandate file — R325 overlay-drift detector AND R366 multi-level REPL both claimed it; fixed by renumbering R366 row to `E2.M35`. (2) coverage-map A-06 (notification axis) cited fabricated `E2.M17` (no such row); fixed to point at the real `E2.M14` doctor/autohealth row which contains notify-dispatch synthesis. Both bugs invisible to humans scanning the file; only the cross-validator caught them. Additional assertions: ≥100 mandate rows total (sanity check against file truncation), well-formed E<N>.M<N> format, ≥5 distinct epics cited across coverage axes (breadth check), no duplicate row IDs in mandate file itself. **Cumulative SDD-037 L1 enforcement: 28 → 35 assertions** (R367 12 + R368 7 + R370 9 + R371 7). [from /goal re-process — R368 caught fabricated §N section refs; this round caught fabricated E.M mandate row refs using the same cross-validation pattern, surfacing 2 real bugs that humans missed] | ✓ shipped | R371 (tests/lint/test_mandate_row_refs.py + 7 L1 + mandate row renumbering E2.M21→E2.M35 + coverage-map A-06 mandate_rows fix to E2.M14 + verbatim-surface.md regenerated) |
| E10.M14 | **verbatim-surface.md operator-readable doc + drift lint** — publishes the verbatim-render output as a static `docs/src/verbatim-surface.md` artifact (770 lines, every Q-NN/G-NN/C-NN/A-NN + CCD layers + state-fabric files + ZFS props + network topology + REPL modes rendered as operator-readable markdown). Added to mdbook SUMMARY.md so it's published in the operator-facing doc tree. Drift detection L1 lint (9 assertions in `tests/lint/test_verbatim_surface_doc_drift.py`): doc exists + correct H1 + all 10 required section headers present + every concept C-NN has its `### C-NN` header in doc + every axis A-NN appears + every Q-NN + G-NN appears + generator footer intact + SUMMARY.md links to doc + tally section shows ≥70 items. If an agent adds a new C-NN or A-NN to the catalog without regenerating, the lint fails at push with the exact regeneration command in the assertion message. NEGATIVE TEST verified: simulating a C-NN drift (replaced one ### C-NN line with sentinel) causes the lint to fail with the exact missing-ID list — confirms drift detection actually fires, not just passes vacuously. Operator can now: (1) browse the entire verbatim surface in mdbook (operator-friendly UI), (2) trust the static artifact stays in sync with the Python source-of-truth (drift lint), (3) attach the rendered doc to the R285 quarterly review unchanged. [from /goal re-process — R369 shipped the meta-verb but operator had to run it manually; this round publishes the output as a static + drift-protected doc] | ✓ shipped | R370 (docs/src/verbatim-surface.md regenerated from R369 + SUMMARY.md updated + tests/lint/test_verbatim_surface_doc_drift.py 9 L1 assertions with negative-test verification) |
| E10.M13 | **Verbatim-render meta-verb** — `sovereign-osctl verbatim-render render/summary/manifest` consolidates the entire SDD-037 verbatim surface (76 items across 9 catalogs: 4 architecture-qa questions + 3 gotchas + 23 concepts + 30 coverage-map axes + 3 CCD layers + 4 state-fabric files + 3 ZFS props + 2 network interfaces + 4 REPL modes) into one operator-readable artifact. Without this verb, operator had to run ≥7 separate `show` commands to audit the full /goal verbatim-preservation contract. With this verb, one render gives the entire surface as a navigable markdown doc with operator-VERBATIM preservation (sync=always / 0xfff / 'Magician' apostrophes / M.2_2 slot must remain empty / CMK128GX5M2B6400C42 / SMT2200C / 990 EVO Plus / OPNsense ASCII diagram / Immutable System Persona / Atomic Append-Only / etc). summary verb returns per-catalog tally + estimated phrase count (current: 465+). manifest verb returns operator-runnable verb catalog (one `sovereign-osctl architecture-qa show C-NN` or `coverage show A-NN` per entry). NEVER-raises on missing catalog module — gracefully degrades (other catalogs still render). L3 covers: 9-catalog summary tally + SDD-037 floor compliance + 60+ manifest entries with valid verbs + 9 catalog section headers in render output + 10 operator-verbatim phrases cross-catalog preservation + §8 ASCII diagram 3 key lines + §7.1 4 files with verbatim role text + NEVER-raise-on-missing-module graceful degradation + osctl-dispatch × 3 + phrase count ≥200 + manifest verb-shape per category + render ≥500 lines + R367+R368 SDD-037 L1 no-regression. [from /goal re-process — operator's verbatim-preservation surface had grown to 76 items across 6 separate verbs; needed consolidated render for operator audit at a glance + quarterly review artifact] | ✓ shipped | R369 (scripts/intelligence/verbatim-render.py + sovereign-osctl verbatim-render + 13 L3) |
| E10.M12 | **spec_ref format lint (extends SDD-037)** — extends R367's SDD-037 L1 lint with 7 more assertions on `tests/lint/test_verbatim_spec_ref_format.py`: every Q-NN/G-NN/C-NN `spec_ref` MUST match one of the recognized operator-citation patterns (`master spec §N` / `master spec §N.M` / `master spec §N + §M` / `master spec Block N` / `master spec dump-tail` / `macro-arc plan dump <date>` / `operator overlay <date>` / `/goal directive <date>`); every `master spec §N` citation MUST point to a real section number (§1..§23 + §N.M); concept catalog cites ≥10 distinct master spec sections (coverage breadth); fabricated section refs like §99 fail; every coverage-map `source` matches a known origin pattern (hook drop / mandate row / macro-arc plan / raw dump / test); every implementing_verb starts with `sovereign-osctl ` / `systemctl ` / `# `. Catches: silent fabrication ("master spec §99 verbatim" — never existed), unknown citation forms (agent invents new pattern without updating lint), placeholder verbs that wouldn't dispatch, and coverage breadth regression (silent narrowing to fewer master spec sections). Extends SDD-037 L1 lint enforcement from 12 → 19 total assertions. [from /goal re-process — R367 SDD-037 lint checked spec_ref non-empty + ≥10 chars but didn't verify the cited section actually exists; an agent could write `spec_ref = "master spec §99 verbatim"` without lint complaint] | ✓ shipped | R368 (tests/lint/test_verbatim_spec_ref_format.py + 7 L1 covering pattern-match + valid-§N-section + coverage-breadth-≥10-sections + no-fabricated-section + spec_ref-string-type + coverage-source-pattern + implementing_verb-real-prefix; SDD-037 §"L1 lint enforcement" extended to document both R367 + R368 assertion sets) |
| E10.M11 | **SDD-037 verbatim-preservation doctrine + L1 lint** — formalizes the R355-R366 pattern (12 rounds covering 20+ master spec sections + dump-tail + macro-arc plan post-Plan refinements + 30 A-NN axes) as a reusable doctrine future agents apply uniformly. SDD-037 codifies: 7-step contract (identify verbatim content → surface as discoverable verb → preserve operator-exact text including typos / punctuation / exact numbers / list order + cardinality → L3 phrase assertions ≥5 specific operator-EXACT phrases per entry → bidirectional consistency for code-bearing entries → implementation deviation documentation → coverage-map back-link); typo-preservation rule (Proto-Programing / planifest — operator typos ARE the operator-stated text); 3 named failure modes (silent paraphrase / silent compression / silent conflation); ~378+ operator-exact phrases mechanized at push-time. L1 lint `tests/lint/test_verbatim_preservation_doctrine.py` (12 assertions): SDD-037 exists + 7 required sections + typo-preservation cited + 9-round cross-link (R355/R357/R358/R359/R362/R363/R364/R365/R366) + architecture-qa catalog floor (≥4 Q-NN + ≥3 G-NN + ≥10 C-NN) + every architecture-qa item has spec_ref ≥10 chars + coverage-map floor (≥30 A-NN) + every axis has ≥1 implementing verb (no orphan operator demand) + status values ∈ {✓ shipped / partial / TODO} + Tetragon 4-binary allowlist bidirectional consistency (C-14 concept text AND tetragon-policy-load.sh script) + C-NN / A-NN ID monotonic + no duplicates. Future operator §1b drops follow the same template — SDD-037 documents it; L1 lint catches drift in the doctrine itself. [from /goal re-process — after 12 rounds of verbatim-preservation work, the pattern needed doctrinal codification so future sessions don't reinvent + L1 lint catches catalog regression (e.g., concept ID collision, spec_ref nullification, Tetragon allowlist drift)] | ✓ shipped | R367 (docs/sdd/037-verbatim-preservation-doctrine.md + tests/lint/test_verbatim_preservation_doctrine.py + SDD-037 indexed + 12 L1 assertions covering 7-section pin + typo-preservation citation + 9-round cross-link + architecture-qa-floor + spec_ref-non-empty + coverage-floor + every-axis-has-verb + valid-status + Tetragon-bidirectional + ID-monotonic × 2) |
| E2.M35 | **Multi-level REPL — Python / System / GPU / LLM (closes A-14)** — operator-pull `sovereign-osctl repl modes/show/exec/shell` ships the operator-named 4-level REPL from the hook drop verbatim ("Everything via dashboard/UInterface or terminal tools OR AI. Python, System and GPU and LLM and multiple level and REPL"). 4 modes: (a) python — Python REPL with SDD-032 helper trio (operator_overlay + apply_audit + safe_apply) + R348 inventory_consult pre-imported; PYTHONPATH=scripts/lib injected. (b) system — shell passthrough with operator-pull pre-arms (lspci / nvidia-smi / zpool status -v / ip -j addr show / journalctl -u tetragon / systemctl list-units --type=service --state=failed / cpuinfo avx512 grep). (c) gpu — GPU-focused: nvidia-smi (full / dmon / power / temperature) + sovereign-osctl gpu-card-advisor + gpu-wattage + gpu-mode + xmp-oc-room + thermal-oc-budget. (d) llm — inference router (pulse / oracle / router) + models adapt/build/eval + trinity profile show cross-referencing the 3 operator-named runtime profiles (ultra-sovereign-efficiency / high-concurrency-burst / deep-context-synthesis). exec verb = non-interactive one-shot with 30s timeout, NEVER-raises (bad cmd → rc=1, timeout → rc=124, exception → rc=1). shell verb = operator-runnable interactive (execvpe replaces process); for python mode writes preamble to tempfile + spawns python3 -i; for shell modes prints preamble to stderr + execs interactive shell. Operator-overlay extends modes catalog per host. Coverage-map A-14 flipped partial → ✓ shipped. Cumulative coverage state: 30/30 ✓ shipped, 0 partial, 0 TODO. [from /goal re-process — R365 coverage audit flagged A-14 as the only partial axis; this round closes it with the operator's exact 4-level REPL] | ✓ shipped | R366 (scripts/intelligence/repl.py + sovereign-osctl repl + 4 operator-named modes + 13 L3 covering 4-mode-catalog + python-SDD-032-preimports + system-pre-arms + gpu-verbs + llm-trinity-cross-link + exec-one-shot + python-env-pythonpath-injection + NEVER-raise-bad-cmd + show-unknown-known_modes + overlay-extends + osctl-dispatch + A-14-flip-to-shipped + audit-0-TODO-0-partial-30-shipped) |
| E10.M10 | **Operator-axis coverage map** — `sovereign-osctl coverage axes/show/audit/search` provides operator-pull catalog of every named axis from hook drops + mandate rows + raw dumps mapped to implementing verbs + SDD refs + mandate rows. 30 A-NN axes catalogued covering operator's verbatim hook-drop content (guide-into-experience / AI tools download-fine-tune-eval / selfdef-modules / GPU watts RTX-3090 RTX-Pro-6000 / autohealth-doctor / notification / DNS-Cloudflared-tailscale-Traefik / docker-vs-system / dashboard / RAID / logs-log-rotate / MCP-deps / Debian-13-Base / REPL-Python-GPU / Proto-Programing-CoT-custom-CoT / kernel-optimization / bios-X870E-CREATOR / pci-lane-splits / PSU-Dark-Power-Pro-13-1600W / XMP-OC-room-budget / APC-schedule-planifest-graceful / fan-cooling-advisor / SMT2200C-CMK128GX5M2B6400C42-990-EVO-Plus hardware SKUs / NO-MINIMIZING-RE-READ-RAW-DUMP /goal contract / perpetual-mandate / no-minimization-research-loop). Each axis carries: axis_verbatim (operator-EXACT text, NO PARAPHRASING — even operator typos like "planifest" preserved), source (hook drop date / mandate row / raw dump section), implementing_verbs (≥1), sdd_refs, mandate_rows, status (✓ shipped / partial / TODO), notes. Current state: 29 ✓ shipped, 1 partial (multi-level REPL in-flight), 0 TODO — every operator-stated demand has ≥1 implementing verb. audit verb rc=1 if any TODO axis exists (enforcement gate). L3 #4 enforces 20 operator-VERBATIM hook-drop phrases preserved unchanged (Cloudflared / tailscale / Traefik with operator's exact capitalization; "be Quiet! Dark Power Pro 13 1600W" exact PSU name; "Proto-Programing"/"Proto-Proto-Programming" operator typos preserved; "continue till you meet ALL MY REQUIREMENTS" /goal contract; "DO not stop"; "We do not minimize anything"). L3 #7 specifically asserts "schedule/planifest/graceful" with operator's "planifest" typo preserved (per /goal NO PARAPHRASING contract). Operator-overlay extends axes catalog per session — when operator drops a new axis between sessions, it gets added via overlay TOML and audit verb tracks it. [from /goal re-process — operator's hook drop enumerates ~30 axes without bounded checklist; this meta-verb provides operator-pull audit of every axis without forcing operator to scan the entire 130+ row mandate file] | ✓ shipped | R365 (scripts/intelligence/coverage-map.py + sovereign-osctl coverage axes/show/audit/search + 30 A-NN catalog × every-axis-has-verb invariant + 13 L3 covering ≥30-axes-schema + 20-phrase-verbatim-preservation + status-counts ≥25/≤5/0 + audit-rc-on-TODO + show-A-04-GPU-trio + show-A-22-planifest-typo-preserved + show-unknown-known_axes + search × 3 + status-filter + osctl-dispatch + overlay-extends-with-TODO-audit-enforcement) |
| E10.M9 | **Architecture-qa concepts C-20..C-23 (macro-arc plan post-Plan refinements verbatim)** — extends concepts catalog 19 → 23 with operator-verbatim content from the SECOND raw dump (`2026-05-16-sovereign-os-macro-arc-plan.md`, 404 lines, post-Plan refinements section). C-20 SFIF discipline (Scaffold → Foundation → Infrastructure → Features arc: PRs 1-3 = Scaffold; PRs 4-8 = Foundation; PRs 9-10 begin Infrastructure; Stage 2 onwards delivers Infrastructure + Features). C-21 IaC quality bar (operator's verbatim 7-property requirement: high-quality scripts + libs + configuration + easily tweakable + customisable + env-var-driven + restart-from-state; resumable + observable not one-shot; cross-refs SDD-030 overlay + SDD-028 phases + R327 audit + R322 snapshot). C-22 'Debian as Ark' framing (Debian 13 = starting boat, not destination; 'boat' metaphor; working hypothesis stay on Debian + customize the boat; NOT building a Debian derivative — building Sovereign OS that happens to sail on Debian-13 hull). C-23 Q-016 distro-base reconsideration (added to macro-arc seed list; stays open through PR 4 substrate survey; resolved at Stage Gate 2 alongside Q-001; candidates evaluated honestly: NixOS / Fedora Silverblue + ostree / Arch Linux / Buildroot+Yocto). L3 #2-5 are verbatim-preservation: 42 NEW operator-EXACT phrases (7 SFIF + 12 IaC + 8 Debian-as-Ark + 15 Q-016). Search verb matches `sfif` → C-20, `debian as ark` → C-22, `q-016` → C-23, `restart-from-state` → C-21. Cross-link verified: search `distro` → C-22 AND C-23 mutually reinforcing (Debian-as-Ark framing motivates Q-016 honesty gate). All 6 sibling architecture-qa L3 tests still green. Both raw dump files now fully surfaced as discoverable verbs with verbatim-preservation L3. [from /goal re-process — macro-arc plan dump post-Plan operator refinements (SFIF / IaC / Debian-as-Ark / Q-016) were previously inaccessible as discoverable verbs despite being foundational doctrine] | ✓ shipped | R364 (architecture-qa.py ARCHITECTURE_CONCEPTS extended 19→23 + 13 L3 covering ≥23-concepts + 4 verbatim-preservation × 42 phrases + 4-concept uniform spec_ref + cross-category search × 4 + Debian-as-Ark ↔ Q-016 cross-link + 6-sibling-no-regression + overlay-still-extends) |
| E10.M8 | **Architecture-qa concepts C-16/C-17/C-18/C-19 (§1 Hardware + §23 Summary + §3.2 Package List + dump-tail DFlash/Models verbatim)** — extends concepts catalog 15 → 19 with 4 final operator-verbatim master spec content blocks. C-16 §1 + §1.1 + §1.2 Hardware Infrastructure (operator-verbatim 6-row hardware spec table: Ryzen 9 9900X / ASUS ProArt X870E-Creator / RTX PRO 6000 96GB / RTX 3090 24GB / 256GB DDR5 / Marvell AQC113C 10GbE — each with operator-verbatim rationale; PCIe x8/x8 symmetry constraint; M.2_2 MUST REMAIN EMPTY rule; 'Magician' symmetry phrasing preserved with apostrophes; 2x PCIe 5.0 NVMe ZFS RAID 0 31.5 GB/s target). C-17 §23 Summary of System Cohesion (operator's 3-point closing synthesis: 1. Pulse CCD 0 / AVX-512 / ternary; 2. Weaver CCD 1 / lockless ZFS transactions; 3. Auditor eBPF Tetragon / silent kernel executor; closing 'complete, unified, and engineered to standard'). C-18 §3.2 Sovereign Forge live-build package list (12 operator-verbatim packages: nvidia-open-kernel-dkms / nvidia-driver / nvidia-smi / nvidia-container-toolkit / zfsutils-linux / zfs-dkms / podman / git / curl / tmux / python3-minimal / python3-pip; plus os-release verbatim values NAME="Sovereign OS" / ID=sovereign / ID_LIKE=debian / VERSION_ID="1.0"). C-19 Dump-tail operator additions (verbatim DFlash quote about 3x faster on code tasks + arxiv 2602.06036 + z-lab/dflash + 2 model candidate HF URLs: inclusionAI/Ling-2.6-flash 107494M bailing_hybrid MIT + nvidia/Nemotron-3-Nano-Omni-30B-A3B-Reasoning-BF16 33015M NemotronH_Nano_Omni_Reasoning_V3 multimodal any-to-any). L3 #2-5 are verbatim-preservation: 62 NEW operator-EXACT phrases (21 §1 hardware + 13 §23 summary + 12 §3.2 packages + 4 os-release + 16 dump-tail including exact param counts 107494M + 33015M). Search verb matches `magician` → C-16, `dflash` → C-19, `nemotron` → C-19, `lockless` → C-17 + C-08 cross-match. All 5 sibling architecture-qa L3 tests still green (R355/R357/R360/R361/R362). [from /goal re-process — §1 hardware verbatim + §23 summary + §3.2 package list + dump-tail operator additions all previously inaccessible as discoverable verbs; this round completes coverage of every operator-verbatim content block in the 1139-line dump] | ✓ shipped | R363 (architecture-qa.py ARCHITECTURE_CONCEPTS extended 15→19 + 13 L3 covering ≥19-concepts + 4 verbatim-preservation × 62 phrases + 4-section spec_ref + cross-category search × 4 + 5-sibling-no-regression + shipped-package-cross-check informational + overlay-still-extends) |
| E10.M7 | **Architecture-qa concepts C-14 + C-15 (§4 Tetragon TracingPolicy + §3 Storage Architecture verbatim)** — extends concepts catalog 13 → 15 with operator-verbatim §4 Security Perimeter Tetragon TracingPolicy YAML (apiVersion: cilium.io/v1alpha1 / kind: TracingPolicy / metadata.name: sovereign-kernel-fence / kprobes call sys_execve + matchArgs NotIn allowlist [/usr/bin/python3 / /usr/bin/nvidia-smi / /usr/local/bin/vllm / /usr/bin/podman] + matchActions Sigkill) AND §3 Storage Architecture per-dataset table (tank/models recordsize=1M lz4 Redundant Metadata for 100GB+ weight files / tank/context recordsize=16k zstd-9 copies=2 stores [SOUL.md] [IDENTITY.md] / tank/agents recordsize=128k zstd-3 Stateful local agent storage; pool created via zpool create -o ashift=12 -O compression=lz4 -m none tank /dev/nvme0n1 /dev/nvme1n1; 2x PCIe 5.0 NVMe ZFS RAID 0 with 31.5 GB/s target). C-14 explicitly DOCUMENTS the implementation deviation (shipped policy uses __x64_sys_execve + matchBinaries while preserving operator's exact 4-binary allowlist) — operator can audit the refinement explicitly rather than discovering it via reading shipped YAML. L3 #2-5 are verbatim-preservation: 15 §4 phrases (apiVersion + TracingPolicy + 4-binary allowlist + Sigkill) + 23 §3+§4.1 phrases (3 datasets × recordsize+compression + 31.5 GB/s throughput target + ashift=12). L3 #4 enforces the operator's exact 4-binary allowlist preserved in BOTH the concept text AND the shipped tetragon-policy-load.sh script (bidirectional consistency check — neither can drift silently). L3 #13 directly asserts the shipped policy script contains the 4 allowlist binaries (catches drift in the implementation file). [from /goal re-process — §3 + §4 operator-verbatim YAML + storage tables not yet first-classed; security-critical content needed verbatim+deviation-documented surface] | ✓ shipped | R362 (architecture-qa.py ARCHITECTURE_CONCEPTS extended 13→15 + 13 L3 covering ≥15-concepts + 2 verbatim-preservation × 38 phrases + deviation-documented + 4-binary-allowlist-bidirectional + 2 spec_refs + search × 3 + R357/R360/R361 no-regression × 3 + shipped-script-allowlist-consistency) |
| E10.M6 | **Architecture-qa concepts × 3 more verbatim (C-11..C-13)** — extends R360 concepts catalog 10 → 13 with operator-verbatim explanations from master spec sections §5 + §9 + §18 not yet first-classed: C-11 §5 Operational Logic / Vibe Manager (120GB total VRAM tiered execution fabric: 96GB Blackwell Direct Host primary reasoning + 24GB 3090 VFIO sandbox speculative decoding + 9900X CPU state persistence via tank/context ZFS atomic writes); C-12 §9 Container Build AVX-512 Vectorization (verbatim Dockerfile snippet: ENV CFLAGS="-march=znver5 -mavx512f -mavx512dq -mavx512bw -mavx512vl -mavx512bf16 -mavx512fp16" + ENV CXXFLAGS same + ENV GGML_AVX512=1 + ENV GGML_AVX512_VBMI=1 + ENV GGML_AVX512_VNNI=1 to force GGML/vLLM 512-bit vector paths under Podman, avoid fallback emulation); C-13 §18 Load Balancing Asymmetric_Burst JSON (verbatim 3-allocation profile: conductor_01 on cpu core_mask 0-11 running BitNet-b1.58-13B via bitnet.cpp; translator_01 on cuda:0 vram_limit_bytes=22548578304 running Qwen-32B-Ternary-Quant via vllm-vulkan; deep_reasoner_01 on cuda:1 vram_limit_bytes=94489280512 running DeepSeek-R1-Distill-Llama-70B-FP16 via llama.cpp). L3 #2-4 are verbatim-preservation assertions: 48 NEW operator-EXACT phrases preserved unchanged (12 §5 + 18 §9 + 18 §18) including all 6 -mavx512* compiler flags, 3 GGML environment variables verbatim with =1 values, and the EXACT integer byte limits 22548578304 + 94489280512 (= 21 GiB + 88 GiB) which prevents silent rounding-to-GiB conversion drift. Cross-category search verb now matches `asymmetric_burst` → C-13, `GGML_AVX512_VNNI` → C-12, `vibe` → C-11. R355 (12 L3) + R357 (13 L3) + R360 (13 L3) sibling tests still green. Cumulative architecture-qa concepts catalog: 13 entries across 10 master spec sections (§5, §9, §10, §11, §15-16, §17.1, §18, §19, §20, §21, Block 6). [from /goal re-process — §5/§9/§18 operator-verbatim explanatory blocks not yet first-classed; particularly the exact VRAM byte limits and -mavx512* flag list which prevent compiler-drift silently disabling instructions] | ✓ shipped | R361 (architecture-qa.py ARCHITECTURE_CONCEPTS extended 10→13 + 13 L3 covering ≥13-concepts + 3 verbatim-preservation × 48 phrases + 3-section spec_ref + cross-category search × 3 + R357/R360/R355 no-regression × 3 + exact-byte-limits-verbatim + tag-filter-on-new-concept) |
| E10.M5 | **Architecture-qa concepts × 5 more verbatim** — extends R357 concepts catalog 5 → 10 with operator-verbatim explanations from master spec sections previously not first-classed: C-06 §17.1 Layered Responsibility Mapping (3 agents: The Conductor Agent CPU Bound / The Logic Engine GPU 0 RTX 3090 / The Oracle Core GPU 1 Blackwell PRO 6000 with per-agent runtime + justification verbatim); C-07 §10 Native Guardian Event Loop (SecureToast.ps1 replacement / Tetragon eBPF UNIX socket listener / BindsTo=tetragon.service / circuit breaker pattern); C-08 §21 Atomic State Transition Protocol (Weaver lockless loopback write / O_WRONLY OR-pattern O_CREAT OR-pattern O_TRUNC OR-pattern O_DIRECT OR-pattern O_SYNC / 4K NVMe alignment / atomic rename); C-09 §11 Consolidated Execution Strategy (5-phase deployment order: Phase I Iron Validation / Phase II Engine / Phase III OS Image / Phase IV File System / Phase V Perimeter); C-10 §20 Wasm-to-AVX-512 AOT Pipeline (Cranelift/LLVM AOT compilation / WASMTIME_COMPARE_OPTIONS znver5 + relaxed-simd / taskset CCD 0 enforcement). L3 #2-6 are verbatim-preservation assertions: 46 new operator-EXACT phrases preserved across C-06..C-10 (3 agent titles + Q4_K_M/IQ4_NL quantization + 96GB Blackwell + SecureToast.ps1 + BindsTo + circuit breaker + lockless loopback + O_DIRECT|O_SYNC + 4K alignment + 5 Phase titles + 'No hacks, no shortcuts, no compromises' closing + Cranelift + WASMTIME flags + taskset CCD 0). Cross-category search verb now matches `cranelift` → C-10, `phase iii` → C-09, `o_direct` → C-08. R355 (12 L3) + R357 (13 L3) sibling tests still green. Cumulative architecture-qa concepts catalog spans 7 master spec sections (§10, §11, §15-16, §17.1, §19, §20, §21, Block 6). [from /goal re-process — §10/§11/§17.1/§20/§21 had operator-verbatim explanatory blocks not yet first-classed] | ✓ shipped | R360 (architecture-qa.py ARCHITECTURE_CONCEPTS extended 5→10 + 13 L3 covering ≥10-concepts + 5 verbatim-preservation × 46 phrases + 5-section spec_ref-cross-link + cross-category search × 3 + R357-no-regression + R355-no-regression + tag-filter-on-new-concept) |
| E3.M8 | **Network topology verifier** — operator-pull `sovereign-osctl network-topology show/verify/scaffold` surfacing master spec §8 Network Infrastructure & Perimeter Segregation ASCII diagram + §8.1 verbatim interface configs. show emits operator's verbatim §8 ASCII diagram (OPNsense Core Router → VLAN 100/200 → SAIN-01 NODE with Intel I226-V 2.5GbE management + Marvell AQC113C 10GbE compute) AND §8.1 per-NIC schema (interface + vendor + chipset + speed + VLAN + address_cidr + gateway + intended_mtu + responsibilities_verbatim + wan_access + spec_ref). verify probes /sys/class/net/<iface>/mtu + `ip -j addr show dev <iface>` against operator's §8.1 intent (enp6s0=10.0.100.50/24+1500+VLAN100+WAN; enp5s0=10.0.200.50/24+9000+VLAN200+NO-WAN); drift → rc=1 + ip-link/ip-addr remediation commands per drifted NIC. scaffold emits §8.1-shaped /etc/network/interfaces stanzas WITHOUT executing (note: 'does NOT execute' + SOVEREIGN_OS_CONFIRM_DESTROY guard). NEVER-raises on container. L3 #2-5 enforce operator-VERBATIM phrases unchanged: 10 ASCII diagram lines (OPNsense, VLAN 100/200, Intel I226-V 2.5GbE, Marvell AQC113C 10GbE, Host SSH, Tetragon Log Streams, System Updates, Isolated Container Bridge, Model Weight Pulls (NAS), No Outbound WAN Access) + 6 §8.1 numeric values (addresses/MTU/VLAN tags) + 6 per-NIC responsibility bullets + 2 chipset SKUs (I226-V + AQC113C). Zero-trust posture preserved: Marvell 10GbE wan_access=False (operator's §8 "No Outbound WAN Access" verbatim) + Intel 2.5GbE wan_access=True. Operator-overlay replaces interfaces list per-host. [from /goal re-process — render-asymmetric.sh existed at script level but no discoverable operator-pull verb surfaced/verified the §8 topology] | ✓ shipped | R359 (`sovereign-osctl network-topology show/verify/scaffold` + 2-NIC × verbatim §8.1 matrix + ASCII diagram preservation + 12 L3 covering ASCII diagram + 10-phrase-verbatim + §8.1-addresses-MTU-VLAN-verbatim + 6-responsibility-bullets-verbatim + I226-V/AQC113C-chipset-SKUs-verbatim + NEVER-raise-container + §8.1-scaffold-shape + does-NOT-execute-note + overlay-replaces-interfaces + osctl-dispatch + spec_ref-cites-§8/§8.1 + zero-trust-wan_access-posture) |
| E1.M42 | **State-fabric verifier** — operator-pull `sovereign-osctl state-fabric layout/verify/scaffold` surfacing master spec §7.1 File-State Matrix + §7.2 ZFS Transactional Optimizations as discoverable + verifiable contract. layout renders the 4-file table (IDENTITY.md Immutable / SOUL.md RW-via-Manager / AGENTS.md RO-to-Sub-Agents / CLAUDE.md Atomic-Append-Only) with operator-verbatim role text + intended mode (0o400 / 0o644) + writer + readers + intent_axis. verify probes ZFS dataset (`zfs get -H -o value sync|primarycache|logbias tank/context`) against operator's exact §7.2 values (always / all / latency) AND each file's presence + mode against intended; drift → rc=1 + remediation commands (chmod / zfs set). scaffold emits operator-runnable bootstrap commands (zfs create + zfs set ×3 + touch ×4 + chmod ×4) WITHOUT executing — note explicitly states "does NOT execute" + "SOVEREIGN_OS_CONFIRM_DESTROY" triple-gate guard. NEVER-raises on container (dataset_exists=false; files missing). L3 #2-4 are verbatim-preservation assertions: 10 §7.1 role phrases (Immutable System Persona, Read-Only to Agents, Core Behavioral Logic, Dynamic Long-Term Memory, Read-Write via Manager, Routing Table, Hardware Pinning Map, Read-Only to Sub-Agents, Active Session Context, Atomic Append-Only) + 3 §7.2 properties (sync=always, primarycache=all, logbias=latency) verbatim + 3 §7.2 sync rationale phrases ("synchronous writes", "physically committed to the NVMe", "agent's state change"). Cross-references R154 Weaver atomic-state.py for writer mapping. Operator-overlay replaces dataset/mountpoint/files per-host. [from /goal re-process — §7 Vibe-Manager state-fabric is foundational to multi-agent architecture but had no dedicated discoverable verifier verb] | ✓ shipped | R358 (`sovereign-osctl state-fabric layout/verify/scaffold` + 4-file × verbatim role matrix + 3-property × verbatim ZFS table + 12 L3 covering 4-file-§7.1 + 10-phrase-verbatim + 3-property-verbatim + 3-phrase-rationale-verbatim + NEVER-raise-container + drift-detection-with-remediation + scaffold-≥12-cmds + does-NOT-execute-note + overlay-replaces-3-fields + osctl-dispatch + spec_ref-cites-§7.1/§7.2 + atomic-state-cross-ref) |
| E10.M4 | **Architecture-qa concepts extension** — extends R355 architecture-qa with a `concepts` category for master spec §15-16 1-Bit Paradigm + Hardware Fusion + §19 dual-CCD doctrine + Block 6 Trinity Genesis content (verbatim operator explanations that justify the entire Pulse/CPU-bound architecture). `sovereign-osctl architecture-qa concepts/show C-NN/search`. 5 C-NN entries: C-01 ternary {-1,0,+1} elimination of multiplication (§15.1) / C-02 64x INT8 per ZMM register (§16+§16.1) / C-03 VNNI/VPDPBUSD fused multiply-accumulate + 5–12 tokens/sec + PCIe bypass (§16.1 closing) / C-04 dual-CCD Infinity Fabric cross-die penalty (§19+§19.1) / C-05 Trinity Genesis decoupled software trinity — Pulse/Weaver/Auditor — MASM/Wasm/eBPF (Block 6 Modules 1/2/3). L3 #2-6 are verbatim-preservation assertions enforcing 42 operator-exact phrases unchanged across the 5 concepts ('{-1, 0, +1}', 'log_2(3)', '1.585', 'discrete ternary set', 'No-Op', '5–12 tokens/sec', 'engineering masterpiece', 'AMD Infinity Fabric', '32MB of L3 cache', 'cross-die latency penalty', 'decoupled software trinity', 'MASM (Microsoft Macro Assembler)', 'bit-plane transposition', 'automated, immediate circuit breaker', …). Search verb cross-matches all 3 categories (questions + gotchas + concepts). Operator-overlay extends concepts per host. Closes a §15-16 + §19 + Block 6 surface gap — these explanations lived only in `docs/src/sain-01-master-spec.md` until now; no operator-pull verb queried them. [from /goal re-process of raw dump §15-16 + §19 + Block 6 — operator's architectural justification content now first-class] | ✓ shipped | R357 (architecture-qa.py extended with ARCHITECTURE_CONCEPTS catalog [5 C-NN entries] + concepts subverb + render_concept_show + cross-category search + 13 L3 covering schema + 5 verbatim-preservation assertions + tag-filter + cross-category-search + known_concepts-error + overlay-extends + osctl-dispatches-concepts + R355-no-regression) |
| E1.M41 | **CCD-pinning verifier** — operator-pull "is my Ryzen 9 9900X CCD pinning correct?" surfacing master spec §19.2 verbatim Core Isolation Strategy table (3 layers: Pulse Core CCD 0 cores 0-5 mask 0xfff / Weaver & Auditor CCD 1 cores 6-9 mask 0xff000 / System Host / OS Base CCD 1 cores 10-11 mask 0xf00000). `sovereign-osctl ccd-pinning show/verify/recommend`. Verify probes `/proc/<MainPID>/status` `Cpus_allowed:` for known service units (sovereign-pulse, sovereign-guardian-core) and compares actual hex mask vs operator's §19.2 intended mask. Drift → rc=1 + recommendation emits `systemctl set-property <unit> AllowedCPUs=<thread-range>` remediation command per-PID. NEVER-raises on container (no systemd → all probed=false; degrades to information-only). L3 #2 enforces operator-VERBATIM masks 0xfff/0xff000/0xf00000 unchanged; L3 #3 enforces verbatim phrases ("AVX-512", "bitnet.cpp", "state engine", "Tetragon", "Marvell 10GbE") preserved in responsibility text. Operator-overlay replaces layers list per-host (different Ryzen SKU). §19.1 dual-CCD boundary doctrine (Infinity Fabric latency elimination) preserved structurally — CCD0=Pulse only, CCD1=Weaver+Auditor+Host. [from /goal re-read of master spec §19 dump + gap analysis — masks lived in console output but no verb verified live PIDs against them] | ✓ shipped | R356 (`sovereign-osctl ccd-pinning show/verify/recommend` + 3-layer × verbatim-mask catalog + 10 L3 covering 3-layers + verbatim-masks-preserved + verbatim-responsibility-phrases + NEVER-raise-on-container + recommend-empty-on-no-drift + systemctl-set-property-remediation-shape + overlay-replaces-layers + CCD0/CCD1-boundary-structural + osctl-dispatch + Trinity-naming-preserved) |
| E10.M3 | **Architecture-Q&A verbatim catalog** — operator-pull "why these choices?" + "what edge cases should I know?" surface for master spec §13 (Architectural Q&A Matrix, 4 verbatim Q&A items: Debian-13 vs Arch/RHEL; ZFS sync=always; -march=znver5; bindeb-pkg DKMS bypass) + §14 (Critical Edge Cases & Operational Gotchas, 3 verbatim items: dual-GPU lane asymmetry / Secure Boot MOK blockades / OPNsense+Tetragon dropouts). `sovereign-osctl architecture-qa questions/gotchas/show/search`. Each item carries operator-verbatim text (NO REPHRASING) + tags + spec_ref pointing back to master spec section. L3 #3-5 are verbatim-preservation assertions that enforce the operator's `/goal` "NO MINIMIZING / NO REPHRASING / NO COMPRESSING / NO CONFLATING" mandate at push-time by checking specific phrases ("sync=always", "x8 / x8 execution topology", "6.12-znver5 kernel", "Machine Owner Key (MOK)", "physically flushes the dirty cache pages to NVMe silicon" etc.) appear unchanged. Each gotcha lists related_verbs (e.g. G-01 → `pcie-lanes` + `gpu-card-advisor` + `model-build plan --recipe quantize-awq-int4`). Search verb cross-matches questions + gotchas. Operator-overlay (R283/SDD-030) extends both catalogs per host. Closes a §13+§14 surface gap that lived only in `docs/src/sain-01-master-spec.md` until now. [from /goal directive "continue till you meet ALL MY REQUIREMENTS without MINIMIZING or rephrasing or compressing or conflating" + raw dump re-read 2026-05-15-sain-01-master-spec-other-conversation-transposition.md] | ✓ shipped | R355 (`sovereign-osctl architecture-qa questions/gotchas/show/search` + 4 verbatim Q-NN + 3 verbatim G-NN + 12 L3 covering schema × 2 + verbatim-preservation × 3 + unknown-rc + tag-filter + search × 2 + overlay-extends + osctl-dispatch + related_verbs-real) |
| E4.M10 | **Intelligence-tier cards on dashboard** — wire R349 guide / R350 model-adapt / R351 module-state / R352 morning-brief / R353 model-build into the dashboard CARDS registry so the verbs that were CLI-only become operator-clickable. morning_brief is placed at CARDS[0] — operator's daily entry-point. Each new card derives `data.summary` + `data.needs_attention` so it participates correctly in the R248 terminal grid view + the R225 web cards page + the per-card /api/<id> endpoint. Card-count assertion in test_dashboard_grid loosened to >=20 (forward-compatible; was pinned to ==20). New `_run_intel_script` helper added to serve.py with NEVER-raise semantics matching `_run_json` + `_run_models_script`. Closes the §1b "Everything via dashboard/UInterface or terminal tools OR AI" gap — terminal verbs existed since R349-R353 but were not surfaced in the dashboard. [from §1b verbatim — "dashboard or API and modules and tools vision. Everything via dashboard/UInterface or terminal tools OR AI"] | ✓ shipped | R354 (serve.py +5 cards [morning_brief/module_state/guide/model_adapt/model_build] + _run_intel_script helper + CARDS extended 20→25 + grid-test forward-compat + 10 L3 covering CARDS-registration + full-schema + summary-derivation × 5 cards + CARDS[0] = morning_brief + grid-cross-regression + NEVER-raise helper) |

### Epic E5 — AI / LLM / Training-station

> "AI and the tools but also download, fine-tune, parameters, build,
> run, use and train and adapt and use and eval and etc ... like LM
> Studio and LM Link maybe ? Unsloth ?"

| ID | Module | Status | Rounds |
|----|--------|--------|--------|
| E5.M1 | Model catalog R71 taxonomy + 17 entries × 12 classes | ✓ shipped (prior) | R212 |
| E5.M2 | Model browse + detail + eval + suggester | ✓ shipped (prior) | R213, R214, R231, R232 |
| E5.M3 | LoRA state file + atomic attach/detach/set-status | ✓ shipped (selfdef) | SD-R81, SD-R89 |
| E5.M4 | Toolchain catalog (llama.cpp / bitnet.cpp / vllm / ollama / lm-studio / lm-link / unsloth / transformers / trl / lm-eval / mteb / dflash) | ✓ shipped | R242 |
| E5.M5 | Fine-tune workflow skeleton (R244 surface; SD-R89 selfdef state) | ✓ shipped | R244 |
| E5.M6 | End-to-end fine-tune lifecycle (operator triggers training → eval → register) | ✓ shipped | R290 (`sovereign-osctl lifecycle list/plan/next-step` — composes R244 + R232 + R182) |
| E5.M7 | Model variants + quantizations + advanced features parametrization | ✓ shipped | R231 (variants/quantization detail) + R311 (`sovereign-osctl model-params list/show/recommend` — hardware-aware parameter advisor) |
| E5.M8 | Speculative-decoding (DFlash) integration | ✓ shipped (prior) | R157 |
| E5.M9 | Operator-mutable flexible profile (download / fine-tune / parameters / build / run / use / train / adapt / eval workflow) | ✓ shipped | R291 (`sovereign-osctl workflow list/plan/next-step` — full 9-stage operator-named sequence) |
| E5.M10 | **Operator "assistant feel" UX layer** — clear paths, options, modules-combo-features, super-features. Every dashboard card + CLI verb surfaces "next-best-step" hints; module combinations get curated names ("inference-burst pack", "headless-server pack") that flip multiple knobs at once. [from §1a] | ✓ shipped | R282 |
| E5.M11 | **Endless flexibility + fine-tuning + adapting** — operator-pull config layer that lets EVERY shipped script accept TOML overlays for thresholds, paths, knobs, advisory copy. [from §1a] | ✓ shipped | R283 (library + SDD-030 doctrine + L1 lint) |
| E5.M18 | **Model-build planner** — fills the "build" verb in the §1b operator-named 9-verb AI tools pipeline ("download / fine-tune / parameters / **BUILD** / run / use / train / adapt / eval"). `sovereign-osctl model-build recipes/plan/show/history`. 4 build-recipe types covering all 4 artifact kinds: `merge-lora-into-base` (peft merge_and_unload; needs --adapter), `quantize-gguf-q4-k-m` (llama.cpp convert+quantize; CPU-only, ~6 GiB output for 7B), `quantize-awq-int4` (autoawq, needs 24 GiB VRAM, vLLM-loadable), `export-safetensors` (transformers save_pretrained, no quantize, reference baseline for eval). Each recipe carries operator-runnable command_template + min_vram_gib + cost_estimate. Plan resolves GPU fit (highest-VRAM card by default; --target-gpu N enforces explicit choice) + computes output_path under operator-configurable build_dir. Downstream_verbs hand off to R232 eval + router-wire. History reads JSONL state file (NEVER-raises on missing). Operator-overlay (R283/SDD-030) replaces recipes/gpus/build_dir/history_path. Closes the 9th + final verb in operator's §1b pipeline — pipeline now end-to-end: R350 adapt → R244 fine-tune → R353 build → R232 eval → R263 use. [from §1b verbatim — "build" was the unfilled 4th verb in the 9-verb pipeline] | ✓ shipped | R353 (`sovereign-osctl model-build` + 4-recipe × 4-artifact-kind matrix + 13 L3 covering recipes-schema + 4-kinds-present + adapter-required + adapter-success + gguf-cpu-only + awq-24gib-fit + --target-gpu-enforces + unknown-rc + show-schema + downstream-handoff + history-never-raises + overlay-replaces-build-dir + osctl-dispatch) |
| E5.M17 | **Model-adapt task→(recipe, GPU) recommender** — fills the explicit "adapt" word from §1b verbatim ("download, fine-tune, parameters, build, run, use and train and adapt and use and eval and etc."). `sovereign-osctl model-adapt tasks/recipes/recommend/show`. 6-recipe catalog covers chat/code/agent/tool-use/reasoning/math/long-context/edge/fast-iteration/alignment via {qlora-7b-codegen, lora-13b-chat, qlora-32b-reasoning, lora-70b-instruct, sft-3b-edge, dpo-preference}. Recommender is HARDWARE-AWARE: cross-references declared GPUs (default mirrors SAIN-01 catalog: RTX 3090 24 GiB + RTX PRO 6000 98 GiB) — `recommend reasoning` picks the 32B/qlora recipe that needs 48 GiB FT VRAM (fits ONLY on PRO 6000); `recommend code --target-gpu 0` picks the 7B/qlora recipe that fits the 3090. Downstream verbs hand off to R244 fine-tune plan + R232 eval plan (composes the full operator-named "adapt → fine-tune → eval" pipeline). Third consumer of R348 inventory_consult helper — `gpu_caveats` surfaces R317 catalog PCIe-bifurcation caveats in `recipes` JSON. Operator-overlay (R283/SDD-030) replaces recipes/gpus per host (e.g. test rig with different VRAM). [from §1b verbatim — "adapt" was the unfilled gap between fine-tune (R244) and eval (R232)] | ✓ shipped | R350 (`sovereign-osctl model-adapt` + 6-recipe × 13-task matrix + 11 L3 covering tasks-list + recipes-schema + reasoning→PRO6000 + code→3090 + unknown-task-rc + show-recipe + downstream-handoff + overlay-replaces-gpus + osctl-dispatch + helper-integration) |

### Epic E6 — Health / Doctor / Autonomy

> "autohealth and doctor, notification and messaging ... scans ...
> analysis and event."

| ID | Module | Status | Rounds |
|----|--------|--------|--------|
| E6.M1 | Composite health-scan over all Z-vectors | ✓ shipped (prior) | R226 |
| E6.M2 | Notification fan-out (file / webhook / ntfy) with dedup | ✓ shipped (prior) | R228 |
| E6.M3 | Autonomous timer-driven scan → notify loop | ✓ shipped (prior) | R229 |
| E6.M4 | Event aggregator + insights synthesizer | ✓ shipped (prior) | R234, R246 |
| E6.M5 | Doctor verb (analysis + recommendations across all axes) | ✓ shipped as `diagnose` | R266 |
| E6.M6 | Severity escalation policy (attention → critical after dwell-time) | ✓ shipped | R273 |

### Epic E7 — Interop / MCP / Tools / Deps

> "Interoperability, MCP, tools, deps ... interoperate with an MCP via
> tools calls and/or MCP. (e.g. I might install node, claude and
> whatever deps and use it on it.)"

| ID | Module | Status | Rounds |
|----|--------|--------|--------|
| E7.M1 | MCP tool manifest (read-only verbs) | ✓ shipped (selfdef) | SD-R84 |
| E7.M2 | MCP stdio JSON-RPC server | ✓ shipped (selfdef) | SD-R91, SD-R92 |
| E7.M3 | MCP TCP transport | ✓ shipped (selfdef) | SD-R94 |
| E7.M4 | MCP write-tool authorization gate (SELFDEF_MCP_ALLOW_WRITES=YES) | ✓ shipped | SD-R96 |
| E7.M5 | Cross-repo MCP-tool aggregator (sovereign-os surfaces selfdef tools too) | ✓ shipped | R286 (SDD-031) |
| E7.M6 | Operator-supplied dep install hooks (node + claude + arbitrary apt/pip + curl-shell) | ✓ shipped | R284 |

### Epic E8 — Python REPL / Programming tiers / Integrated intelligence

> "Python, System and GPU and LLM and multiple level and REPL.
> Programming, Proto-Programing, Proto-Proto-Programming and CoT and
> custom CoT, integrated intelligence modules, features and options
> and etc."

| ID | Module | Status | Rounds |
|----|--------|--------|--------|
| E8.M1 | Tier 1 Proto-Programming bootstrap (Python REPL with subprocess wrappers) | ✓ shipped (selfdef) | SD-R85 |
| E8.M2 | Tier 2 Proto-Proto-Programming examples (operator-pull macros) | ✓ shipped (selfdef) | SD-R90 |
| E8.M3 | REPL invocation audit trail (SELFDEF_REPL_HISTORY JSONL) | ✓ shipped (selfdef) | SD-R95 |
| E8.M4 | Integrated-intelligence modules — operator-pull CoT routines registered with @selfdef_macro | ✓ shipped (selfdef) | SD-R98 |
| E8.M5 | Tier 3 native pyo3 bindings (zero-subprocess Tier 1) | ✓ shipped (selfdef, via MCP-TCP bridge — pyo3 deferred for `#![forbid(unsafe_code)]` rule) | SD-R101 |
| E8.M6 | Token-saving aliases + wasted-path tracker | ✓ shipped (selfdef) | SD-R97 |

### Epic E9 — Operator-mandate process discipline

> "register what I said so that you can break it down and have
> identifiable and workable pieces ... do not minimize anything nor
> the situation."

| ID | Module | Status | Rounds |
|----|--------|--------|--------|
| E9.M1 | Standing-directive durable record (THIS FILE) | ✓ shipped | R264 |
| E9.M2 | Per-round Epic/Module citation in commit messages | ✓ in-practice from R265 | R265+ |
| E9.M3 | Quarterly mandate review + new-axis intake process | ✓ shipped | R285 (mandate-review-2026-Q2.md) |
| E9.M4 | Cross-link Epic/Module IDs into SDD-029 + future SDDs | ✓ shipped | R264 (SDD-029 doctrine) + R320 (`tests/lint/test_sdd_cross_links.py` L1 audit pins doctrine for all SDDs ≥029) |
| E9.M5 | `/goal` autopilot re-arming — root cause + compact-pointer script + L1 lint guard | ✓ shipped | R267 |
| E9.M6 | **Multi-`/goal`-paste compounding doctrine** — every `/goal` text adds to the mandate; never replaces. §1.0 anchors the rule. [from §1.0 + this turn] | ✓ shipped | R278 (this round) |
| E9.M7 | **PR not-draft-by-default** — selfdef cycle-N PRs lifted from draft immediately so reviewers see active work; CI gates remain authoritative. [from §1a + this turn] | ✓ shipped | R278 (PR #198 lifted) |
| E9.M8 | **Raw-dump re-read protocol** — when operator says "RETURN REREAD ALL THE RAW DUMP", agent reads `devops-solutions-information-hub/raw/` before asking; only ask when ambiguity remains. [from §1.0] | ✓ in-practice | R278 onward |
| E9.M9 | **Operator-pull rounds catalog** — meta-verb that parses the mandate file + emits every shipped round with its Epic/Module mapping for operator navigation across the 300+ round codebase; supports list / show <round> / by-epic <E> / recent <N>. [from §1.0 — meta-navigation for the perpetual E9.M3 intake loop] | ✓ shipped | R321 (`sovereign-osctl rounds list/show/by-epic/recent`) |
| E9.M10 | **SDD-033 perpetual-intake-doctrine** — formalize the E9.M3 intake pattern (§1b verbatim → TODO row → operator-runnable verb → L3-tested → ✓) as a permanent SDD with L1 lint pinning required sections. Currently implicit in mandate-review-2026-Q2.md; lifts to first-class doctrine doc. (Originally R326 numbered SDD-031, collided with pre-existing SDD-031 MCP aggregator → R331 renamed to SDD-033 + updated INDEX). [from §1.0 — doctrine formalization for the perpetual loop] | ✓ shipped | R326 (file) + R331 (rename to 033) — `docs/sdd/033-perpetual-intake-doctrine.md` + `tests/lint/test_perpetual_intake_doctrine.py` |
| E9.M11 | **Apply-audit log** — central JSONL (`/var/lib/sovereign-os/apply-audit.jsonl`) where every mutating verb invocation appends a row {time, verb, gates_satisfied, what_was_written, op_user, host}. Python helper module `scripts/lib/apply_audit.py` that any apply-verb imports. R318 first adopter. Query CLI for operator audit. [from §1.0 — observability over the triple-gate doctrine documented in SDD-031] | ✓ shipped | R327 (`sovereign-osctl apply-audit list/tail/by-verb/audit` + `scripts/lib/apply_audit.py` helper + R318 first adopter) |
| E9.M12 | **safe_apply helper library** — codifies triple-gate + R323 maintenance-window check + R327 audit-write into ONE `run_apply_safe()` function future apply verbs import. Eliminates copy-paste of the apply ceremony. Composes R318 pattern + R323 + R327. [from §1.0 — apply-side helper library to match the read-side R283 overlay helper] | ✓ shipped | R328 (`scripts/lib/safe_apply.py` + `tests/unit/test_safe_apply.py` — 13 unit tests) |
| E9.M13 | **SDD-032 helper-library doctrine** — formalize the three helper modules (R283 `load_with_overlay` + R327 `apply_audit` + R328 `safe_apply`) as a permanent contract; L1 lint pins required public API. Lifts the implicit "every consumer script imports from scripts/lib/" convention to first-class doctrine. [from §1.0 — codify the helper-library contract for future authors] | ✓ shipped | R330 (`docs/sdd/032-helper-library-doctrine.md` + `tests/lint/test_helper_library_doctrine.py` — 11 L1 assertions) |
| E9.M14 | **Self-test verb** — operator-pull "is sovereign-os itself working correctly on this host?" Runs all L1 lint suites + curated L3 nspawn sample + emits operator-readable health summary. Distinct from R322 state-snapshot (which is about the OPERATOR'S host) — this is about the TOOLING running on the host. R331 also fixed the SDD-031 number collision discovered while developing self-test (R326's perpetual-intake-doctrine renamed 031→033; INDEX updated). [from §1.0 — meta-health-check for the perpetual loop's tooling] | ✓ shipped | R331 (`sovereign-osctl self-test run/list` + SDD-031→033 rename fix) |
| E9.M15 | **SDD-034 snapshot doctrine** — formalize the 6-script snapshot family (R322 state-snapshot + R324 fleet-aggregator + R332 config-snapshot + R333 config-restore + R334 state-snapshot-diff + R335 config-snapshot-diff) as a permanent contract. L1 lint pins round IDs, JSON schema-version field, round-mismatch detection. Lifts the implicit "snapshots carry round=R<N> + schema_version" convention to first-class doctrine. [from §1.0 — codify the snapshot family contract] | ✓ shipped | R336 (`docs/sdd/034-snapshot-doctrine.md` + `tests/lint/test_snapshot_doctrine.py` — 7 L1 assertions + INDEX update) |
| E9.M16 | **SDD-035 workload-mode adoption contract** — formalize the cross-advisor pattern proven across R339-R342: every R338 workload-mode adopter declares `_read_canonical_mode` + `_apply_mode_modulation` (or equivalent derive) helpers, `follow_workload_mode_coordinator` + `workload_mode_overlay_path` overlay knobs, a `WORKLOAD_MODE_TO_<SHAPE>` map, and emits `workload_mode_canonical` + `workload_mode_source` fields in JSON output. L1 lint pins the contract across all 4 current adopters + auto-picks-up future adopters. [from §1.0 — formalize the cross-advisor mode coordination doctrine] | ✓ shipped | R343 (`docs/sdd/035-workload-mode-adoption-doctrine.md` + `tests/lint/test_workload_mode_adoption_doctrine.py` — 12 L1 assertions + R337 standardized to canonical-pair field names) |

### Epic E10 — AI as guide (operator-pull narrative entry-points)

> "[it's] only a guide into the experience, into the field, into the
> kernel, into the hardware, into the OS, into the modules, into the
> features, the services, the configurations, the personalisations,
> the customizations" (§1b hook drop).

R309 cot-registry answers "what's the decision-flow for <routine>?".
R329 next-action-advisor answers "what should I do NOW?". R349 guide
answers "what IS this system + how do I look INTO it?" — the third
operator-pull intelligence vector.

| ID | Module | Status | Rounds |
|----|--------|--------|--------|
| E10.M1 | **Topic guide catalog** — `sovereign-osctl guide topics/list/show/walkthrough <topic>`. 9 default topics covering operator's verbatim §1b axes (kernel / hardware / gpu / psu / ups / memory / workload-mode / inference / network). Each topic carries: mission narrative + ordered layers (look-into steps) + operator_verbs (paired 1:1 with layers for `walkthrough` zip) + thresholds (operator-meaningful numbers) + bios_or_hw_caveats (per-board: ASUS X870E-CREATOR specifics, RTX 3090 shunt-mod, Dark Power Pro 13 physical OC switch, etc.) + cross_refs (SDDs / advisors). Operator-overlay (R283/SDD-030) lets operator extend or replace topics. L3 test pins layer/verb count parity across all topics (catches walkthrough zip drift at push-time) AND verifies operator-verbatim §1b SKUs (CMK128GX5M2B6400C42, SMT2200C, RTX 3090, RTX PRO 6000, Ryzen 9 9900X, be Quiet! Dark Power Pro, ASUS X870E-CREATOR) surface in topic content AND cross_refs cite SDDs that exist on disk. [from §1b verbatim hook drop quoted above — "AI as guide" is the operator's central animating vision; partial coverage via R309 CoT + R329 next-action; R349 fills the explicit "guide INTO" vector] | ✓ shipped | R349 (`sovereign-osctl guide topics/list/show/walkthrough` + 9→11 topics × 5 axes + 10 L3 covering topics-enumeration / axes / show-schema / unknown-rc / walkthrough-mode / layer-verb-parity / verbatim-SKU-preservation / axis-filter / osctl-dispatch / cross_ref-SDD-existence; R349 follow-up added autohealth + selfdef topics) |
| E10.M2 | **Morning-brief rollup** — `sovereign-osctl morning-brief rollup` composes 4 operator-pull intelligence sources (R329 next-action top-N + R351 module-state attention items + R308 autohealth severity + R349 guide-topic suggestion) into ONE operator-readable "what should I look at first this morning?" report. Each sub-probe runs best-effort (NEVER-raise on subprocess failure; section reports `available: false` + error blurb instead of crashing the rollup). Critical-signal aggregation escalates rc=1 when next-action has priority=critical OR autohealth severity=critical OR a module is `running-without-overlay` (stock defaults in production). Topic-suggestion heuristic scores R349 topics by keyword hits across recommendations + module gaps, picks the highest-signal topic (e.g. memory-pressure recommendation → suggests `guide walkthrough memory`). Operator-overlay tunes section limits + can disable autohealth/guide-suggestion sections. R352 also extends R331 self-test curated L3 sample with R349/R350/R351/R352 tests so self-test stays current with shipped intelligence verbs. [from §1.0 — operator-pull "single screen, what matters right now?" gap; composes 4 existing verbs without coupling] | ✓ shipped | R352 (`sovereign-osctl morning-brief rollup` + 4-source composition + R331 self-test L3 sample extended + 12 L3 covering schema + 3-section coverage + module-attention surfacing + best-effort-na + autohealth-never-raises + suggested-topic-real + verb-shape + --limit + 4-section-human-render + osctl-dispatch + unknown-rc=2 + overlay-tunes-sections) |

### Epic E11 — §1g "Ultimate Sovereign OS" expansion (docs + master-dashboard + global history + auth tiers + network topology + edge-firewall + Nemotron 3)

> Sources §1g operator paste (2026-05-18) reproduced verbatim in §1g above.
>
> This Epic captures the §1g expansion that doesn't fit cleanly into
> E1–E10 (those were operator-named axes of system functioning; E11 is
> a NEW dimension on top — documentation/UX/dashboards/auth/network-
> topology as cross-cutting concerns + Nemotron 3 model family).

| ID | Module | Status | Rounds |
|----|--------|--------|--------|
| E11.M1 | **Documentation through-and-through (selfdef + sovereign-os, all modules, all features)** — README.md + extension files for both repos + all modules. High-standards UX-design stage upstream of every surface. 300+ modules/options/features at scale. NOT just `docs/sdd/*.md` (already covered E2.M11) — operator wants the README + per-module READMEs + per-feature READMEs + the human-discoverable on-ramp from "I just cloned this" → "I can do everything". [§1g verbatim: "very clear and well defined documentation through and through which follow the high standards"] | TODO | — |
| E11.M2 | **Master-dashboard / reverse-proxy aggregator** — optional nginx (or alternative) consolidating per-port dashboards under a single super-dashboard port. Operator can choose: (a) per-port-direct, (b) reverse-proxied aggregate, (c) alternative aggregator. [§1g verbatim: "Maybe there can even be an option to add a reverse proxy nginx or such to do a master dashboard which regroup all those of different port under a single port and super-dashboard"] | TODO | — |
| E11.M3 | **Multi-surface delivery contract** — every operator-facing capability MUST be delivered across at least: core, CLI, TUI, API, MCP, **Dashboard, Web App, Service**. Lint at push-time that every operator-facing module has at least N of these surfaces present (or explicit waiver). [§1g verbatim: "Everything is not just core, not just cli, not just TUI, not just API, not just tool and MCP but also Dashboards and Web Apps and Services"] | TODO | — |
| E11.M4 | **Nvidia Nemotron 3 / Nano Omni integration** — research the model family, slot into model-catalog as a Trinity-tier candidate (Oracle Core or Logic Engine — operator decides), evaluate quantization paths (fp8 / bf16 / Q4_K_M etc.), update sain-01 profile + start-script defaults if operator picks it. [§1g verbatim: "Nvidia Nemotron 3, Nano Omni..."] | partial | R446 (online research completed; 2 new catalog entries added: Nemotron-3-Nano-Omni-30B-Reasoning-FP8 + Nemotron-3-Nano-Omni-30B-Reasoning-NVFP4 alongside the pre-existing BF16; schema updated to add `audio` purpose + `nvfp4` quantization enum values; existing BF16 entry's operator_note enriched with verified facts — MoE-A3B + Mamba-Transformer hybrid + 9× throughput + multi-modal benchmark wins; remaining work: pick concrete one for sain-01 default + wire start-oracle-core.sh to it + verify model fits VRAM budget on PRO 6000 + 3090) |
| E11.M5 | **Global history (delta/differentials)** — more reliable than `.bash_history` aggregation. Tracks: apt operations, every CLI/tool call, sovereign-osctl verbs, module install/uninstall, profile switches, hotswap events. Separate management-layer surface (`sovereign-osctl history` family). Operator-discoverable: "what happened to my system in the last N hours/days?". [§1g verbatim: "Some kind of global history too. tracking things happening, delta, differentials"] | ✓ shipped | R448 (scripts/operator/global-history.py NEW 480 lines + 4 subcommands recent/summary/sources/delta + 6-source taxonomy {apt, dpkg, shell, osctl, events, modules} + per-source reader functions + apt history.log parser (Start-Date/Commandline/Install/Remove/Upgrade blocks) + dpkg.log line parser + .bash_history HISTTIMEFORMAT-aware reader + JSONL aggregator for osctl/events/modules + --since accepts ISO 8601 OR relative (24h/7d/2w/1m) + --source comma-filter + --json/--human + 6 env-overridable path constants + SOVEREIGN_OS_DRY_RUN safety + read-only (≤1 write_text for metric only) + sovereign_os_operator_global_history_query_total{verb,source,result} metric (SDD-016) + sovereign-osctl global-history dispatcher + help text with E11.M5 binding + 27 L1 lint assertions in test_global_history_contract.py + smoke-test via subprocess; sister surface distinct from `history` (build-run) + `events` (JSONL state-only) + `journal` (systemd)) |
| E11.M6 | **bashrc opt-in configuration** — operator-opt-in `sovereign-osctl bashrc install` adds: autocompletes for sovereign-osctl + selfdefctl, aliases, inline `man`/`--help` shortcuts, menus. Idempotent + reversible (`bashrc uninstall`). [§1g verbatim: "the bashrc we can offer to configure it too"] | ✓ shipped | R447 (scripts/operator/bashrc-install.sh + cmd_bashrc dispatcher + 4 subcommands install/uninstall/status/dump + sentinel-bounded block + 10 operator-discoverable aliases (sosctl/soshelp/sosstatus/sosmodels/soshealth/sosdoctor/sosthermal/soswatt/soshist/sosmorning) + soshelp-menu quick-help function + bash completion covering 25+ top-level subcommands + per-subcommand 2nd-level completion (profiles/whitelabel/perimeter/models/morning-brief/bashrc) + DRY_RUN safety + zsh-compatible via SOVEREIGN_OS_BASHRC_PATH env override + sovereign_os_operator_bashrc_install_total metric (SDD-016) + 26 L1 lint assertions in test_bashrc_install_contract.py) |
| E11.M7 | **Auth tier ladder** — no-auth (default) → basic auth → advanced auth (token-bound) → social auth → enterprise auth → network-level access. Per-dashboard auth tier configurable. Operator-discoverable upgrade path; clear UX for "I want to expose this dashboard, what should I enable?". [§1g verbatim: "a mode of access from no auth at all by default to basic auth to advanced auth to social auth to enterprise auth and network level access and etc."] | TODO | — |
| E11.M8 | **Network-topology detection — multi-NAT / VPN-bridge across two LANs/WANs / Opnsense integration** — sovereign-os detects: behind 1× NAT (ISP), behind 2× NAT (ISP + Opnsense), VPN-bridge active to second LAN. Opnsense state-awareness (reachable, configured, user+API-key present). Integration features unlock when operator creates user + API key in Opnsense AND sovereign-os has connectivity. [§1g verbatim: "Like normal my AI will also be behind a firewall which will do a VPN bridge to my other network..."] | TODO | — |
| E11.M9 | **Edge-firewall alternative — install equivalent on workstation** — when no edge firewall present (or even when present), offer to install IPS-class firewall **on the AI workstation itself**. Explicit performance-cost disclosure. Compatible with whatever edge firewall is present, including limited fanless variants. Operator-named hardware target for edge: `Sharevdi Fanless Firewall Mini PC` (Intel J3710/N3710 + 4× Intel 2.5GbE i226-V + 8G DDR3 + 128G SSD + AES-NI, running pfSense/OPNsense). [§1g verbatim] | TODO | — |
| E11.M10 | **UX Design stage upstream of every module** — every E11.M* (and ALL operator-facing surfaces) goes through a documented UX-design pass before SDD-codification. Standard: operator can reach the goal of the surface in N or fewer actions, with clear discovery, recoverable mistakes, and discoverable next steps. [§1g verbatim: "everything will also need to go through a thorough UX Design stage in order to be of quality"] | TODO | — |
| E11.M11 | **Anti-minimization continued audit (cross-Epic)** — periodic re-audit of every Epic asking "have we covered all angles and levels and layers? if so, can we improve it?". Output: per-Epic improvement backlog. [§1g verbatim: "If you think something is really already done, ask yourself if you covered all angles and levels and layers and even if then improve it. Do not minimize or settle for less."] | TODO | — |
| E11.M12 | **selfdef branch + never-ending PR setup** — establish `claude/general-session-Wk97z` branch in `cyberpunk042/selfdef`, open a DRAFT PR (not blocked at draft — accumulating + merge when massive group is ready), push selfdef-side rounds against it. sovereign-os stays direct-push to `main`. [operator's perpetual mandate text: "you can keep working in selfdef branch and have a work PR (not blocked at draft) that keep cummulating and that when a massive group of changes and features are ready we merge it"] | TODO | — |

## 4. How future rounds use this file

1. **Pick the next TODO Module** from any Epic above. Prefer Modules
   whose Epic has the most TODO siblings (broaden coverage) or the
   Module that unblocks downstream work.
2. **Decompose into rounds.** A Module may need 1-10 rounds. Each
   round = one round-ID (R<N> or SD-R<N>) + commit + push.
3. **Cite Epic / Module ID in commit message** (e.g. "Round 265 —
   heat-budget integration (E1.M11)"). This is the structural trace
   that addresses the operator's "make sure we record this and make
   sure we do some things about it" mandate.
4. **Update THIS FILE** when a Module flips to ✓. New TODO Modules
   added under existing Epics MUST quote the operator's verbatim
   text they derive from.

## 5. What this file does NOT do

This file is the **decomposition** of the mandate, not the
**implementation status**. SDD-029 + each round's L3 test are the
implementation truth. INDEX.md remains the chronological SDD ledger.
Handoff docs remain the trajectory tracker.

## 6. Anti-corruption invariants

- Do **NOT** rewrite the operator's verbatim text. Edits to Section 1
  require a new file with a new date and operator confirmation.
- Do **NOT** delete TODO Modules without operator confirmation.
- New Modules added during work go under an Epic with a one-line
  source-quote citation.
