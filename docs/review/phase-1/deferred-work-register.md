# Deferred-work register (Phase-1 audit — F-2026-037)

> The docs already **promise** the work below across decisions, SDDs, and context.md.
> This register consolidates those promises in one place so they get owners + ordering
> instead of being rediscovered each pass. It is a **pointer index**, not a re-spec —
> each item's authoritative definition stays in its cited source.
>
> **Owner column is `operator-to-assign`** — sequencing/ownership is an operator call
> (a decision-package), not an agent decision. The **Proposed order** is a suggestion.
>
> `tests/lint/test_deferred_work_register.py` verifies every source cited here resolves
> (the SDD / doc files exist), so the register can't rot into dangling references.
> It does **not** assert an item is still open — status reconciliation is per-item
> operator/authoring-session work against the cited source.

| # | Deferred item | Sources | Scope (one line) | Proposed order | Owner |
|---|---|---|---|---|---|
| 1 | Telemetry-sink choice + Grafana JSON dashboards | `docs/decisions.md` | **Substantially closed (2026-07-24):** the sink choice is settled — `prometheus-local` (SDD-016 Layer B; local-default textfile collector, no phone-home), and **61 Grafana dashboard JSONs** ship under `scripts`-adjacent `docs/observability/dashboards/*.json` (overview, inference, router, gatewayd, thermals, telemetry, auditor, four-watchdog, the selfdef fleet, …). Panels are held in lockstep with the emitted metrics by `tests/lint/test_dashboard_metrics_lockstep.py` + `test_metric_inventory_lockstep.py` + the per-dashboard `*_contract.py` lints. **Still open:** none for the shipped software artifact — operator-side Grafana *installation* is opt-in per SDD-016 Layer C (not a build deliverable). | ~~P1 (foundation)~~ closed | operator-to-assign |
| 2 | SDD-016 Layer-B Prometheus emission | SDD-016 | **Substantially closed (2026-07-24):** the emission is wired, not just contracted. `scripts/build/lib/observability.sh` provides `emit_metric` / `emit_metric_set` (atomic tempfile+rename `.prom` writes under `${SOVEREIGN_OS_METRICS_DIR}`), called from **19 recurrent hooks** (zfs-scrub, log-rotate, memory-*, wattage-*, security-update-check, model-catalog-sync, session-reap, backup-snapshot, notify-dispatch, alerts-check, ms003-verify, root-modules-verify, tetragon-policy-verify, selfdef-sync, power-shutdown-guard, sovereign-telemetry-textfile) plus `scripts/auditor/guardian-core.py` + `scripts/science/warp-runner.py` (21 emit sites total); `thermal-watch.sh` emits the thermal series via `scripts/hardware/thermal-watch.py`'s own atomic Python write under a documented `LAYER-B-WAIVER`. Pinned by `tests/lint/test_hook_layer_b_coverage.py` + `test_metric_inventory_lockstep.py` + `test_metric_observability_coverage.py` + `test_prom_read_write_binding.py` + `tests/nspawn/test_observability_lib.sh` (all green). **Still open:** none — the SDD-016 Layer-B contract is fully emitted; its deferred Layer-C Grafana half is item 1 (also closed). | ~~P1 (foundation)~~ closed | operator-to-assign |
| 3 | Layer-4 QEMU + Layer-5 hardware conformance suites | `docs/decisions.md`, SDD-020 | **Partially closed (2026-07-24):** `tests/qemu/feature-conformance.sh` adds a REAL Layer-4 in-guest conformance harness — it builds `sovereign-feature-selftest`, and when a KVM runner + built image are present boots the image and asserts the in-guest `--self-check` reports `all_ok`; else it SKIPs (exit 0). The feature-test **payload** is now covered host-side by `cargo test -p sovereign-feature-selftest` (`every_feature_self_test_passes`), so the shipped features are verified even where the QEMU transport can't run; `tests/lint/test_qemu_layer4_feature_conformance_contract.py` pins the harness. **Still open:** the boot transport is environment-gated (needs a KVM-capable self-hosted runner), and the Layer-5 hardware tier (`tests/chroot`/hardware) remains scaffold. | P2 (test rigor) | operator-to-assign |
| 4 | SDD-019 apt-snapshot enforcement + `SOURCE_DATE_EPOCH` + in-toto provenance | SDD-019 | **Substantially closed (2026-07-24):** all three legs are wired. **`SOURCE_DATE_EPOCH`** is honored + propagated in step-04 `scripts/build/04-kernel-compile.sh` (into `KBUILD_BUILD_TIMESTAMP` + the step `inputs_hash`) and across `05-substrate-prepare.sh`, `09-image-verify.sh`, `adapters/mkosi-emit.sh`, `scripts/release/build-release.sh`. **apt-snapshot** pinning is wired in `adapters/mkosi-emit.sh` (`DEBIAN_SNAPSHOT` → `Mirror=http://snapshot.debian.org/archive/debian/<snap>`). **in-toto** provenance is emitted by `scripts/release/generate-release-metadata.py` (in-toto `Statement/v1` + SLSA `provenance/v1`), verified in `09-image-verify.sh`, surfaced via `sovereign-osctl`. Pinned by `tests/lint/test_release_contract.py` + `tests/unit/test_provenance_manifest_shape.py` + `tests/lint/test_image_build_verify_verbatim.py`. **Still open:** the end-to-end same-inputs→same-bytes rebuild proof is environment-gated (needs a build host — same class of gate as item 3's KVM runner); cross-operator bit-identicality of signed artifacts is explicitly out of scope per SDD-019. | ~~P2 (reproducibility)~~ substantially closed | operator-to-assign |
| 5 | TPM2 disk-encryption PCR binding | SDD-015, SDD-022 | Bind LUKS to TPM2 PCRs (secure-boot + disk-encryption). | P2 (security) | operator-to-assign |
| 6 | SDD-029 roadmap R257–R262 | SDD-029 | XMP detection, wattage sampler, PSU OC toggle, KNOWN_BOARDS TOML, Z-14/Z-19 cards. | P3 (hardware roadmap) | operator-to-assign |
| 7 | MS043 selfdef mirror-crate implementations | `context.md` (MS043 row); selfdef `context.md` (M060 arc) | **Reconciled closed (2026-07-25) — verify-first per operator directive.** The 9 D-12..D-18 mirror crates are **shipped in the selfdef tree**, not impl-pending: selfdef's own `context.md` marks the arc *"M060 cross-repo mirror producers — COMPLETE"* (2026-05-28), and an **empirical `cargo test`** (this session, selfdef tree in scope) confirms each builds + passes — `selfdef-rules-mirror` 7 · `-grants-mirror` 8 · `-capability-mirror` 11 · `-sandbox-mirror` 8 · `-audit-mirror` 11 · `-quarantine-mirror` 11 · `-trust-score-mirror` 12 · `-profile-mirror` (D-02) 11, all 0 failed, no stub markers. sovereign-os `context.md:239` (the stale "impl pending") is corrected to shipped in the same pass (line 305 already struck it through as done). Nothing to build. | ~~P3 (cross-repo)~~ reconciled closed | operator-to-assign |
| 8 | selfdef CLI/TUI mirror surface integration + SG7/SG8 stage-gates | `context.md` (MS043 row); selfdef `context.md` (M060 arc) | **Reconciled closed (2026-07-25).** The concrete deliverable — the `selfdef-cli-mirror` + `selfdef-tui-mirror` surfaces (8-of-9 + 9-of-9 of the MS043 set) — is **shipped + tested** in the selfdef tree (empirical `cargo test`: `selfdef-cli-mirror` 13, `selfdef-tui-mirror` 14, 0 failed), per selfdef `context.md` (*"cli-mirror … 50+ subcommand schema introspection"* / *"tui-mirror … 4-panel layout schema introspection"*). The trailing **"SG7/SG8 stage-gates"** clause has **no concrete spec in either repo** (`grep SG7\|SG8` → the string appears only in this register row, nowhere in selfdef or sovereign-os) — it is an undefined/aspirational fragment, not a tracked deliverable; treated as **not-a-gap** (superseded by the shipped mirror surfaces). Nothing to build. | ~~P3 (cross-repo)~~ reconciled closed | operator-to-assign |
| 9 | Open question series — SDD-046 Q-046-001..004; root Q-A..Q-D; Q4..Q25 | SDD-046, SDD-003, SDD-025 | Resolve the open-question backlog (mdbook deploy cadence/provider; the Q4..Q25 series across SDD-003..025). | P4 (questions) | operator-to-assign |
| 10 | Q-067-A..F app-shell questions (incl. Q-067-F live-LLM assistant) | `docs/sdd/067-*.md` Q-table; SDD-200/201/202; SDD-062/204/205 | **Reconciled closed (2026-07-25):** 5 of 6 are answered by shipped work, and the 6th is de-tensioned + forward-tracked. **A** (sidemenu taxonomy) → app-shell sidemenu is catalog-generated from `config/dashboard-catalog.yaml` (SDD-067). **B** (default state + key) → `body.so-sm-collapsed` default + `sovereign-os.personalization` key + ≤1024px breakpoint, exactly the proposed fold-into-existing-key. **C** (header approvals roll-up) → the `#so-inbox-badge` attention/down roll-up shipped in the app-shell chrome. **D** (assistant content authoring) → superseded by SDD-200→201→**202** (the full-height context-cascade gold-standard rebuild; 866 selectors resolving across all 51 panels). **E** (which panels; meta/audit in-or-out) → `scripts/webapp/sync-app-shell.py:ADOPTED_PANELS` includes the audit panels (`anti-minimization-audit`, `doc-coverage`, `ux-design-audit`) + `brain` + `code-console` + `science` = all 51, so "in". **F** (live-LLM Assistant) → the original blocker (a network path in tension with sovereignty-clean) is **dissolved**: SDD-062 (loopback D-22 chat), SDD-205 (Anthropic Messages API on loopback), and SDD-204 (Code Console) shipped a **sovereignty-clean local LLM** (loopback-only, no external network); only the *product decision* to wire "discuss-what's-hovered" into the Assistant survives, already forward-tracked as **SDD-202 Q-C** (inherits Q-200-C / Q-067-F). Net: nothing here needs a fresh operator decision except that one already-tracked Q-C. | ~~P4 (questions — reconcile)~~ reconciled closed | operator-to-assign |

## Notes

- **Items 1, 2, 4 reconciled closed (2026-07-24).** A read-only audit found the whole **P1 "software-only, agent-buildable" class already built** by the July arc — the register descriptions were stale, not the code. Evidence is inlined per row + verified by the cited passing contracts (`test_hook_layer_b_coverage`, `test_metric_inventory_lockstep`, `test_dashboard_metrics_lockstep`, `test_release_contract`, `test_provenance_manifest_shape`, `test_observability_lib.sh`). Net effect: **no P1 software gap remains**; the agent-buildable frontier is now the cross-repo (items 7–8) and question (9–10) work, everything else being hardware-gated or an operator decision.
- **Items 7–8 reconciled closed (2026-07-25)** — with the selfdef tree in scope this session, the verify-first pass confirmed both are already shipped (the M060 mirror-producer arc, selfdef 2026-05-28): the 9 D-12..D-18 mirror crates + the cli/tui-mirror surfaces all build + pass `cargo test` empirically (per-crate counts inlined in rows 7–8). The "impl-pending" framing was stale; sovereign-os `context.md:239` is corrected in the same pass. The "SG7/SG8" fragment in item 8 has no spec in either repo and is treated as not-a-gap.
- **Item 10 reconciled closed (2026-07-25).** The reconcile ran: Q-067-A/B/C are answered by the shipped app-shell chrome (catalog-driven sidemenu · `so-sm-collapsed`+personalization-key · `#so-inbox-badge` roll-up), Q-067-D/E by the Assistant gold-standard arc (SDD-200→202, 51-panel adoption incl. the audit panels), and Q-067-F is de-tensioned by the loopback-LLM arc (SDD-062/204/205) with only its product decision surviving as the already-tracked SDD-202 Q-C. No fresh operator decision remains from this item beyond that one forward-tracked Q-C. Evidence inlined in the row.
- **Item 3** is the same surface as **F-2026-052** (the 3-tier test harness is effectively 1-tier) — track them together.
- The **Proposed order** groups by kind (foundation → reproducibility/security → hardware/cross-repo → questions); the operator sets the real sequence.

## Cross-references

- `docs/review/phase-1/99-findings-ledger.md` — F-2026-037 (source), F-2026-052 (Layer-4/5 sibling)
- `tests/lint/test_deferred_work_register.py` — the source-resolution contract
- `docs/sdd/INDEX.md` — the SDDs cited above
