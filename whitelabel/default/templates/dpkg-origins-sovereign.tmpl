Vendor: ${os_name}
Vendor-URL: ${home_url}
Bugs: ${bug_report_url}
Parent: Debian

# Rendered by sovereign-os whitelabel render.py at /etc/dpkg/origins/sovereign.
# Allows `dpkg-vendor --vendor sovereign --query Origin` to identify the
# downstream distribution while preserving the upstream Debian provenance
# at /etc/dpkg/origins/default. Per SDD-006 § Package-manager surfaces.
