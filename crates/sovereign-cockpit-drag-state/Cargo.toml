[package]
name = "sovereign-cockpit-drag-state"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Drag operation state machine: start, hover, drop, cancel."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
