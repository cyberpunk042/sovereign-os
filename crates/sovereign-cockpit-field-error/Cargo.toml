[package]
name = "sovereign-cockpit-field-error"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit form-field error registry. Each (field_id, severity, message) is recorded; visible_in_severity(>=) and worst_for_field(id) drive how the renderer paints the field's helper text + ring color. Distinct from form-validity (form-level valid/invalid) and error-banner (page-level error)."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
