[package]
name = "sovereign-cockpit-focus-ring"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit keyboard-focus-ring visibility tracker. Modal :focus-visible behavior: emit a ring after a keyboard event, hide it after a pointer event, keep it on across focus transitions if the last input source was the keyboard. Pure state-machine crate; the renderer subscribes."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
