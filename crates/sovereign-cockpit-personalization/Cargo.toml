[package]
name = "sovereign-cockpit-personalization"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Per-operator UX personalization layer for the cockpit per M060 R10137 + R10140 + R10141 + R10173-R10175. Theme (dark/light/auto) + accent color + typography scale + per-profile preference layering. Operator standing direction verbatim: 'endless configurations and options and personalization's'."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
sovereign-dashboard-coverage = { path = "../sovereign-dashboard-coverage" }

[features]
default = []
