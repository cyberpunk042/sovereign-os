[package]
name = "sovereign-cockpit-kbd-hint"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Keyboard-shortcut inline hint surface (rendered as <kbd> chunks)."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
