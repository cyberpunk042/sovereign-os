[package]
name = "sovereign-cockpit-sound-preferences"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit sound preferences — per BannerSeverity, operator picks a notification sound (None / Soft / Standard / Sharp / Alarm) + master volume 0..100. Pure UX with no IPS authority dimension."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
sovereign-cockpit-banner-state = { path = "../sovereign-cockpit-banner-state" }

[features]
default = []
