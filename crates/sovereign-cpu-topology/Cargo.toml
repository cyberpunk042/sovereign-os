[package]
name = "sovereign-cpu-topology"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "M070 — dual-CCD cache topology + core pinning. The 2 CCDs (cores/threads/L3) and the 3 SRP-Trinity core allocations (Pulse / Weaver+Auditor / System-Host) with their thread masks, plus partition validation and cgroup-v2 cpuset emission for CCD-aware scheduling."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
