[package]
name = "sovereign-cockpit-command-palette"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit command palette catalog — operator-callable commands surfaced via Ctrl-K style overlay. Each command declares (id, label, group, mode_gate, action_id). Composes sovereign-execution-mode-registry for gating + sovereign-cockpit-keystroke-map for binding lookup."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
sovereign-execution-mode-registry = { path = "../sovereign-execution-mode-registry" }

[features]
default = []
