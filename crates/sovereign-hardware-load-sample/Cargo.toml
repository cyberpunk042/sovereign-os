[package]
name = "sovereign-hardware-load-sample"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Runtime hardware load sample — per-target VRAM-used / compute-util / thermal snapshot the SRP scheduler consumes and the cockpit dashboard renders. Keyed to the 5 HardwareTargets in sovereign-hardware-registry."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
sovereign-hardware-registry = { path = "../sovereign-hardware-registry" }

[features]
default = []
