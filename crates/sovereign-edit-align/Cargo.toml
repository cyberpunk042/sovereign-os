[package]
name = "sovereign-edit-align"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Edit-distance alignment: the dynamic program that returns the actual minimal edit script — match / substitute / insert / delete operations with their positions — between two sequences, not just the distance. For diff highlighting, spelling-correction display, and word-error-rate breakdowns."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
