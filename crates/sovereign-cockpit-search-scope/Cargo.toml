[package]
name = "sovereign-cockpit-search-scope"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit search-scope selector. Tracks the set of available Scope chips (id, label, enabled) and one currently-active scope. activate(id) selects; available() returns enabled scopes for the bar; default() returns the canonical fallback when active is not enabled. Pure descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
