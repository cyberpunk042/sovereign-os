[package]
name = "sovereign-cockpit-pane-layout"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit multi-pane window layout — operator-selectable split modes (Single / SplitVertical / SplitHorizontal / QuadGrid). Each pane holds a Tab reference (id). Validates that the number of populated panes matches the chosen split."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
