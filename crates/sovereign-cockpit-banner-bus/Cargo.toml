[package]
name = "sovereign-cockpit-banner-bus"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit single-slot banner bus. Holds at most one banner; higher-priority post replaces lower-priority. dismiss(id) clears only if the dismissed id is currently showing; lower-prio queued banners can be inspected via queued(). Pure descriptor; distinct from banner-state (in-place banner) and toast-tray (multi-toast)."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
