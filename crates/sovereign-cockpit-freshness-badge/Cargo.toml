[package]
name = "sovereign-cockpit-freshness-badge"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Freshness badge (Fresh/Stale/Revalidating/Failed) for cached data displays."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
