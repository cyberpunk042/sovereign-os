[package]
name = "sovereign-cockpit-export-bundle"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit operator export bundle — packages selected items (conversation thread, dashboard snapshot, pin board, replay session) into a single named export the operator can download / share. Pure UX wrapper; selfdef enforces access on content."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
