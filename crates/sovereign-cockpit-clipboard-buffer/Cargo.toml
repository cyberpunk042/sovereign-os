[package]
name = "sovereign-cockpit-clipboard-buffer"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit in-engine clipboard ring buffer. Capped FIFO of recent copies (text, image-ref) with size limit and per-entry timestamp. paste(index) returns the n-th most recent. Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
