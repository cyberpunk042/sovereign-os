[package]
name = "sovereign-cockpit-favorites"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit operator-favorites registry. Per kind (dashboard/query/widget/contact/...), ordered Vec<Favorite{id, label, kind, pinned_at}>. star/unstar; reorder(kind, from, to). list_kind preserves order. Pure descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
