[package]
name = "sovereign-cockpit-shortcut-cheatsheet"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit shortcut cheatsheet generator — given a KeystrokeMap, produces a per-scope grouped, operator-readable cheatsheet (Markdown / plain-text) the cockpit's `?` overlay renders. Pure visual UX layer over sovereign-cockpit-keystroke-map."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
sovereign-cockpit-keystroke-map = { path = "../sovereign-cockpit-keystroke-map" }

[features]
default = []
