[package]
name = "sovereign-cockpit-action-bar"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit bottom action-bar state. Holds primary, secondary, and tertiary action slots, each with label + enabled flag + danger flag. Ordering: primary right, secondary left, tertiary inline; clear() / set() mutators. Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
