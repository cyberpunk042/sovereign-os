[package]
name = "sovereign-logprobs"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Utilities over token log-probabilities: sequence log-prob, length-normalized scoring (GNMT penalty), perplexity, predictive entropy, top-token confidence margin, and the weakest-token locator for hallucination flagging. The scoring-and-uncertainty layer that reads a model's per-token logprobs."

[lints]
workspace = true

[dependencies]
