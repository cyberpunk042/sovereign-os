[package]
name = "sovereign-cockpit-tag-cloud"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit weighted tag-cloud projection. Given (label, weight) pairs, computes per-tag font_size_pct in [min..max] linearly from observed weight range. Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
