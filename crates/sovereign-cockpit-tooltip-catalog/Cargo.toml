[package]
name = "sovereign-cockpit-tooltip-catalog"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit tooltip catalog — registry of tooltips keyed by element_id with text + placement (top/right/bottom/left) + delay_ms. Operator can override or disable any tooltip. Pure UX surface."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
