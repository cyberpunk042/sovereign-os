[package]
name = "sovereign-world-model"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "M030 World Model plane — a learned tabular state/action/transition model: observe transitions, predict the most-likely next state, roll out trajectories, and measure prediction accuracy. Distinct from the symbolic planner (fixed effects); this learns dynamics from data (Dreamer-style)."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
