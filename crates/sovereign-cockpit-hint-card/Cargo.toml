[package]
name = "sovereign-cockpit-hint-card"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Hint card with dismiss + cooldown before re-show."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
