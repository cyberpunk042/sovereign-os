[package]
name = "sovereign-cockpit-kpi-tile-grid"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Dashboard KPI tile grid with formatting and threshold-based status."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
