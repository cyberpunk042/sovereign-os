[package]
name = "sovereign-rope-scaling"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "RoPE context-length scaling: linear position interpolation, NTK-aware base scaling, and dynamic-NTK for running a rotary-embedding model past its trained context window. Computes the effective position and per-dimension inverse frequencies under each method, the position-side counterpart to the base rope rotation."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
