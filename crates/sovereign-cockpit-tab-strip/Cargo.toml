[package]
name = "sovereign-cockpit-tab-strip"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit tab strip — operator-managed top-of-window tab list. Each Tab declares (id, kind, label, payload_id, pinned). 5 tab kinds (Conversation/Dashboard/Replay/Search/Settings). Max 20 tabs; pinned tabs cannot be closed."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
