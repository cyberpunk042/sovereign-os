[package]
name = "sovereign-cockpit-undo-stack"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit undo stack — bounded LIFO of reversible operator actions (toggle flip, mode switch, bookmark add/remove, prompt-template add). Each action declares (kind, reverse_payload, performed_at, performed_by). Capacity-capped at 100; oldest dropped on overflow."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
