[package]
name = "sovereign-cockpit-friction-audit-panel"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Cockpit panel for the selfdef friction-audit boot-time gate — consumes the verdict ring buffer written by selfdef IPS at the file boundary; produces RenderRow values for the M060 main dashboard."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
