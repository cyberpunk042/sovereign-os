[package]
name = "sovereign-cockpit-toast-tray"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit ephemeral toast tray — per-severity FIFO queue of operator-visible notifications with TTL-based eviction + manual dismissal + count caps. Distinct from the persistent banner: toasts surface ephemeral events (a build finished, a download succeeded) that auto-dismiss."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
sovereign-cockpit-banner-state = { path = "../sovereign-cockpit-banner-state" }

[features]
default = []
