[package]
name = "sovereign-cockpit-titlebar-config"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Cockpit titlebar configuration with segmented title and status pinning."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
