[package]
name = "sovereign-gateway"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "M048 Module 4 Anthropic-first gateway per E0462 + M00806 + dump 14584-14610. 6 surfaces (Anthropic Messages API / OpenAI shim / MCP bridge / Claude Code / OpenCode-Cline / cost+route ledger) + 7-responsibility provider-inversion ownership. Doctrine: 'Instead of tools owning provider keys: client → Sovereign Gateway → local/cloud/model router.'"

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
