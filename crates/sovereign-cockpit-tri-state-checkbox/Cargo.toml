[package]
name = "sovereign-cockpit-tri-state-checkbox"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Tri-state checkbox — Checked / Unchecked / Indeterminate with parent rollup."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
