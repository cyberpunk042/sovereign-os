[package]
name = "sovereign-cockpit-presence-mode"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit presence mode (Focus / Standard / Glance / Off / DoNotDisturb). Each mode carries a notification-policy (suppress, summarize, all), a refresh-cadence (high/medium/low/paused), and an animations flag. set(mode) updates the state; classify_event(severity) returns Suppress/Summarize/Show by current mode. Pure descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
