[package]
name = "sovereign-cockpit-spinner-pool"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit spinner-pool aggregator. Tracks N concurrent named spinners with started_at + cancellable flag. show_status() returns the operator-facing label ('Loading…' / '3 background tasks…' / specific spinner label), suppressing flicker for very short-lived spinners (<200ms). Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
