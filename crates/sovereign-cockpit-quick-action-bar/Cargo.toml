[package]
name = "sovereign-cockpit-quick-action-bar"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit quick-action bar — operator-curated horizontal bar of frequently-used commands (max 12). Each slot holds (command_id, label, icon, shortcut_chord). Operator drags-to-reorder. Pure UX."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
