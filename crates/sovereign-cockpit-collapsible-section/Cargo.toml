[package]
name = "sovereign-cockpit-collapsible-section"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit collapsible section state — per-section_id collapsed/expanded boolean persistence. The cockpit restores the operator's choices across reloads. Pure UX."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
