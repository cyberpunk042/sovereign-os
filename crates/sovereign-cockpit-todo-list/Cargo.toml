[package]
name = "sovereign-cockpit-todo-list"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Operator todo list with status, ordering, and completion stats."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
