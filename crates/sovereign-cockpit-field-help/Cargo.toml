[package]
name = "sovereign-cockpit-field-help"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit per-field help-text registry. Each field has (id, help_text, error_text, dismissed). resolve(id) returns the rendered HelpRender (Error wins over Help; both hidden if dismissed). Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
