[package]
name = "sovereign-cockpit-toast-stack"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit stacked-toast queue. Each Toast{id, body, severity, posted_at, ttl_ms, dismissable}. post/dismiss/visible(now) (auto-dismisses past-TTL). max_visible cap; overflow ages out oldest first. Distinct from toast-tray (single-tray) — this is the multi-toast stack lane."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
