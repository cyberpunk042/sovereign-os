[package]
name = "sovereign-cockpit-audit-trail"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Bounded audit trail — (actor, action, target, ts_ms) records."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
