[package]
name = "sovereign-spec-decode"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Speculative decoding verification (DFlash family) — greedy draft-then-verify accept rule: accept the longest draft prefix matching the target's greedy tokens, then emit one corrected/bonus token. Pure-Rust reference; the acceleration primitive the AVX++ dump flags as DFlash (3x faster code)."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
