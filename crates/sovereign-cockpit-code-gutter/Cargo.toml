[package]
name = "sovereign-cockpit-code-gutter"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit code editor gutter. Tracks per-line annotations (breakpoint, warning, error, info, diff added/removed/modified) and the gutter width required to show the line number plus a single annotation glyph. Pure descriptor; the renderer draws."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
