[package]
name = "sovereign-rolling-hash"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Polynomial rolling hash with O(1) window updates: Rabin-Karp substring search that finds all occurrences in O(n+m), and content-defined chunking that places chunk boundaries where the rolling hash hits a mask — so an edit only re-chunks locally instead of shifting every boundary. For fast scanning and shift-resistant chunking of ingested text."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
