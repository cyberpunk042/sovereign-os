[package]
name = "sovereign-cockpit-filter-chip-bar"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit filter-chip bar state. Each chip is (id, label, value, removable). add/remove/clear_all with per-chip removable flag. Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
