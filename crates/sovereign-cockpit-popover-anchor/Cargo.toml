[package]
name = "sovereign-cockpit-popover-anchor"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit popover anchor positioning. Given anchor rect + popover size + viewport size + preferred placement, returns the resolved (x, y) plus the effective placement, flipping to the opposite side when the preferred side overflows. Pure layout math; no DOM."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
