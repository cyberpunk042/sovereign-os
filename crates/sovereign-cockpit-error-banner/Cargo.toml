[package]
name = "sovereign-cockpit-error-banner"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit error banner stack — persistent top-of-page error/warning banners (separate from ephemeral toasts). Each banner has (severity, title, body, dismissible, primary_action). Operator dismisses or acts. Pure UX surface."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
sovereign-cockpit-banner-state = { path = "../sovereign-cockpit-banner-state" }

[features]
default = []
