[package]
name = "sovereign-eval-result-summary"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Runtime eval-suite result envelope — single-run summary (suite_id, started_at, finished_at, total cases, passing, failing, per-dimension averages). The cockpit displays this on the eval tile + the dispatcher gates next-action on `pass_rate`."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
sovereign-eval-suite-catalog = { path = "../sovereign-eval-suite-catalog" }
sovereign-eval-plane = { path = "../sovereign-eval-plane" }

[features]
default = []
