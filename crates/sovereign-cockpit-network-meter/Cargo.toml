[package]
name = "sovereign-cockpit-network-meter"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit network throughput meter. Tracks (last_rx_bytes, last_tx_bytes, last_sample_unix_ms) + new sample to compute rx_bps + tx_bps. render_display formats 'A KB/s ↓ / B KB/s ↑' with auto unit. Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
