[package]
name = "sovereign-vptree"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Vantage-point tree for exact nearest-neighbor search in a metric space: recursively split points by their distance to a chosen vantage point, then answer k-NN and radius queries while pruning whole subtrees via the triangle inequality. Exact (not approximate) over continuous vectors — a metric-space complement to the discrete-edit-distance BK-tree."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
