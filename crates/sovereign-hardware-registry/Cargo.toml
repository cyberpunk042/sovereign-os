[package]
name = "sovereign-hardware-registry"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Runtime hardware registry — 5-tier catalog (CPU/RDNA-3090/Blackwell/Cloud/None) carrying vram + role + latency-tier so the SRP scheduler + router can dispatch deterministically. Mirrors selfdef HardwareTarget discriminator surface."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
