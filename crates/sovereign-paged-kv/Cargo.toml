[package]
name = "sovereign-paged-kv"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Paged KV-cache memory management (PagedAttention-style): a fixed-size block pool with per-sequence page tables, on-demand block allocation as tokens append, reference-counted copy-on-write so forked sequences share their common prefix blocks, and out-of-memory reporting. The allocator behind non-fragmenting KV-cache serving."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
