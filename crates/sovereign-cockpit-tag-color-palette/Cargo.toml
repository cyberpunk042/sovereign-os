[package]
name = "sovereign-cockpit-tag-color-palette"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Tag color palette: assign discrete colors to tags with deterministic fallback."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
