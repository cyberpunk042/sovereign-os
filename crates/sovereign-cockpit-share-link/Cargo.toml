[package]
name = "sovereign-cockpit-share-link"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit share-link state — operator-generated time-bounded share URLs for conversation excerpts / dashboard snapshots / replay sessions. Carries (target_id, kind, expires_at, recipient_tag). Pure UX wrapper; actual access control lives in selfdef."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
