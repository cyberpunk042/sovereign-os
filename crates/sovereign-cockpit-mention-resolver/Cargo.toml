[package]
name = "sovereign-cockpit-mention-resolver"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Segments text into plain/mention tokens, resolving @handles to known users."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
