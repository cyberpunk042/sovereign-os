[package]
name = "sovereign-pressure-reactions"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "E0431 / M00760 — the 5 adaptive-intelligence reactions that turn live OS pressure (PSI 6-axis) + hardware idleness into scheduler actions. Observability-as-control-input (M013): pressure SENSED → action prescribed. Consumes sovereign-pressure-sensors + sovereign-hardware-load-sample."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
sovereign-pressure-sensors = { path = "../sovereign-pressure-sensors" }
sovereign-hardware-load-sample = { path = "../sovereign-hardware-load-sample" }
sovereign-hardware-registry = { path = "../sovereign-hardware-registry" }

[dev-dependencies]
serde_json = { workspace = true }
