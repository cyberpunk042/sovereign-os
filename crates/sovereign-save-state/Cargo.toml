[package]
name = "sovereign-save-state"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "E0451 — ZFS + CRIU together: the 5-layer agent save-state (ZFS snapshot + CRIU checkpoint + replay log + memory record + profile state). A true save-state requires all five; this fixes the layers + the completeness gate."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
