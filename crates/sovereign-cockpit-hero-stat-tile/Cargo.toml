[package]
name = "sovereign-cockpit-hero-stat-tile"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Hero stat tile — big number + label + delta indicator."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
