[package]
name = "sovereign-cockpit-frame-budget"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Per-frame work budget tracker — yield when budget exhausted."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
