[package]
name = "sovereign-cockpit-mini-map"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit minimap viewport-rect state. Given content (w, h) and viewport (x, y, w, h), computes the scaled minimap dimensions + the viewport-rect within the minimap. Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
