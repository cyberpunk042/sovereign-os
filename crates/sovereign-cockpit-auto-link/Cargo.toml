[package]
name = "sovereign-cockpit-auto-link"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Detects http/https URL substrings in plain text and segments them as links."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
