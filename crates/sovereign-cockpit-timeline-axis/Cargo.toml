[package]
name = "sovereign-cockpit-timeline-axis"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit chart timeline axis. ticks(from_ms, to_ms, target_count) picks a tick interval from a curated set (1s, 5s, 15s, 1m, 5m, 15m, 1h, 6h, 1d, 7d) closest to (range / target_count), returns evenly-spaced ts within [from, to]. Pure math; chart renderer paints."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
