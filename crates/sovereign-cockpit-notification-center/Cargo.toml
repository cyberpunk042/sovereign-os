[package]
name = "sovereign-cockpit-notification-center"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit notification center — persistent operator inbox (distinct from ephemeral toasts). Each notification carries (id, severity, title, body, source, read_state, archived). Operator marks read/unread, archives, filters by source. Pure UX surface."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
sovereign-cockpit-banner-state = { path = "../sovereign-cockpit-banner-state" }

[features]
default = []
