[package]
name = "sovereign-cockpit-data-grid-sort"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit data-grid sort state. Ordered multi-column sort spec (column_id × direction). click_column tri-state cycles None → Asc → Desc → None when the same column is clicked, or replaces single-column sort when a new column is clicked. Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
