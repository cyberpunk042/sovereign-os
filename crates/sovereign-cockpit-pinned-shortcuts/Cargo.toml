[package]
name = "sovereign-cockpit-pinned-shortcuts"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit pinned-shortcut bar — operator-curated quick-launch buttons surfaced on the top-right. Each pin declares (id, label, icon, command_id, color). Max 8 pins; pins reorderable via swap; duplicates rejected."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
