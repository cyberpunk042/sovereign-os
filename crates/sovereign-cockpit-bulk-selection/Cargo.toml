[package]
name = "sovereign-cockpit-bulk-selection"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Multi-item selection with anchor + shift-range support."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
