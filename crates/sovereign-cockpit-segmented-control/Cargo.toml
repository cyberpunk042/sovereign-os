[package]
name = "sovereign-cockpit-segmented-control"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit segmented-control widget. Mutually-exclusive selection from 2-6 segments (id, label, enabled). select() / next() / prev() navigate through enabled segments; arrow keys wrap. Pure UX descriptor (similar to RadioGroup but rendered as a horizontal pill bar)."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
