[package]
name = "sovereign-cockpit-lazy-loader"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Lazy resource loader — Idle/Loading/Loaded/Failed per resource id."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
