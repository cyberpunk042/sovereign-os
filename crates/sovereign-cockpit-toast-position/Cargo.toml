[package]
name = "sovereign-cockpit-toast-position"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit toast positioning + stacking. Operator-configurable corner anchor; stacking direction derived from corner (top-* stacks down, bottom-* stacks up). layout(toasts, container_h) computes per-toast (x, y) px positions. Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
