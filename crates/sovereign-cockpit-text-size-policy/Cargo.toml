[package]
name = "sovereign-cockpit-text-size-policy"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Accessibility text-size policy with scale presets and per-element override."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
