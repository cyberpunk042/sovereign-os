[package]
name = "sovereign-cockpit-bottom-sheet"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit bottom-sheet state with three snap heights (Collapsed / Half / Full). set_snap; drag_to(px) clamps + snaps to the nearest of {collapsed_px, half_px, full_px} per snap_threshold_px; current_height() reports the resolved px. Pure layout math."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
