[package]
name = "sovereign-cockpit-popover-stack"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit popover stack with z-order, dismiss-on-outside-click signal, and parent-popover lineage. Only the topmost popover responds to escape; closing a popover closes all child popovers. Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
