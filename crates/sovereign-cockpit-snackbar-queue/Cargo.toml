[package]
name = "sovereign-cockpit-snackbar-queue"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit snackbar/toast queue state. FIFO of transient notifications with severity, auto-dismiss TTL, max-concurrent visible, and a separate dismissed log so the cockpit can build a 'recent notifications' panel. Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
