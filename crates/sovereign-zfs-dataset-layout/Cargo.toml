[package]
name = "sovereign-zfs-dataset-layout"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "M068 — canonical ZFS dataset layout for the tank pool (context / containers / models / logs / snapshots / vault) with per-dataset recordsize, compression and sync properties, plus a drift validator that flags an observed `zfs get` layout against the catalogued canon (E0660-E0664, F05702-F05730)."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
