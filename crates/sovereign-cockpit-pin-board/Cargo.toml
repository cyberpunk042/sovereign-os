[package]
name = "sovereign-cockpit-pin-board"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit pin board — corkboard-like wall of operator-pinned snippets / links / notes. Each PinCard declares (id, title, kind, body, color, position). Max 50 cards. Pure visual UX."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
