[package]
name = "sovereign-cockpit-side-panel-state"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit side-panel (drawer) state. 4 panel modes (Closed/Peek/Open/Pinned), an active tab id, and a remembered width per mode. open/close/pin toggles + last_used MRU tab list. Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
