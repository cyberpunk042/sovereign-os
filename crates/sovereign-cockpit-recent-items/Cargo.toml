[package]
name = "sovereign-cockpit-recent-items"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit recent-items panel — operator's recently-viewed conversations / dashboards / replays / pin boards. LRU eviction. Each entry (kind, subject_id, label, visited_at). Pure UX."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
