[package]
name = "sovereign-cockpit-stream-pause"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit live-stream pause/resume state. pause()/resume(); observe(arrived) increments queued count only while paused. queued_count() reports the staged buffer size. drop_queued() empties the buffer (operator chose to discard while away). Pure descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
