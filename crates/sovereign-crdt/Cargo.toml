[package]
name = "sovereign-crdt"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "State-based conflict-free replicated data types (CvRDTs): a grow-only counter, a PN-counter, a last-writer-wins register, and an observed-remove set, each with a merge that is commutative, associative, and idempotent so independent replicas converge to the same state with no coordination or conflict resolution. For distributed state that syncs across nodes by exchanging and merging snapshots."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
