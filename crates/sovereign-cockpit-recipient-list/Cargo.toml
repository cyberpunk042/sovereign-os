[package]
name = "sovereign-cockpit-recipient-list"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "To/cc/bcc recipient lists with dedup + cross-list dedup."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
