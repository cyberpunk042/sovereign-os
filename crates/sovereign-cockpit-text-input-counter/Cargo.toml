[package]
name = "sovereign-cockpit-text-input-counter"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit text-input with character counter. Tracks buffer + min/max length + counter display (used/max). Mode: Soft (counter only) or Hard (rejects keystrokes that would exceed max). render_counter() emits operator-facing remaining string. Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
