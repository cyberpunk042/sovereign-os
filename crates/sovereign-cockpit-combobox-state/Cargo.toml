[package]
name = "sovereign-cockpit-combobox-state"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Combobox UI state: open/closed, highlighted item, search filter, accepted value."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
