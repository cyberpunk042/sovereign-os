[package]
name = "sovereign-cockpit-confirmation-modal"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit confirmation modal — operator-facing yes/no dialog state with optional 'type to confirm' phrase + countdown. Pure UX gate; actual destructive operations are still gated by IPS-side authority crates."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
