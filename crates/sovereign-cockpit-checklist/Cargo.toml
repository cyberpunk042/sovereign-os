[package]
name = "sovereign-cockpit-checklist"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit ordered checklist of operator-facing items (e.g. onboarding setup, end-of-task review). Each Item{id, label, completed_at}. complete(id, ts)/uncomplete(id); progress() returns (done, total) and percent. Pure descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
