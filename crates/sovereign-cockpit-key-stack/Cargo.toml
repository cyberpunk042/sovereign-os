[package]
name = "sovereign-cockpit-key-stack"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit key-stack chord recorder. Records the last N keystrokes within a sliding time window for multi-keystroke chord recognition (Vim-style 'gg', emacs-style 'C-x C-f'). matches(prefix) returns true when the last K keys equal a registered chord prefix. Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
