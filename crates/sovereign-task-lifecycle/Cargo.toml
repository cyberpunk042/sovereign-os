[package]
name = "sovereign-task-lifecycle"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "E0548 / E0556 — the Task Lifecycle: the 12 lifecycle steps (Intake … Resume/Archive) and the 9 task states (active/paused/waiting_user/waiting_tool/hibernated/completed/failed/rolled_back/archived) with a validated state machine + resume requirements."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
