[package]
name = "sovereign-cockpit-turn-annotation"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Runtime cockpit per-turn annotations — operator-applied notes / highlights / stars on individual conversation turns. Purely UX/display data with no IPS authority dimension; selfdef sees them only as opaque payload in trace spans."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
sovereign-conversation-thread = { path = "../sovereign-conversation-thread" }

[features]
default = []
