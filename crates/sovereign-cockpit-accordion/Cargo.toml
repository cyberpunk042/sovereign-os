[package]
name = "sovereign-cockpit-accordion"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit accordion (collapsible-panel group) state. Ordered sections with title, expanded flag, and an optional single-open invariant so only one section may be open at a time. Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
