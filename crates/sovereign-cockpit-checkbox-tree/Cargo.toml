[package]
name = "sovereign-cockpit-checkbox-tree"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit tri-state checkbox-tree state. Hierarchical nodes with three checkbox states: Checked / Unchecked / Indeterminate. Toggling a parent propagates down (Checked/Unchecked); a parent's state is derived from its children's aggregate (all-checked / all-unchecked / mixed = Indeterminate). Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
