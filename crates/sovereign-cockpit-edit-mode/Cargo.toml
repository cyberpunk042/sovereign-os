[package]
name = "sovereign-cockpit-edit-mode"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit per-document edit-mode tracker. Each doc_id has a mode (Read/Edit/ReviewPending). request_edit, save_draft, submit_for_review, approve, reject transitions. is_dirty(doc_id) tracks unsaved changes. Pure descriptor; downstream callers fire mutation API calls."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
