[package]
name = "sovereign-cockpit-sync-status"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Sync status indicator — Saved / Saving / Failed / Stale."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
