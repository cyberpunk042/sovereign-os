[package]
name = "sovereign-keywords"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "RAKE-style keyword extraction: pulls the most salient keyphrases from a document by splitting on stopwords into candidate phrases and scoring each by its words' degree-over-frequency. The tagging/indexing primitive for routing and surfacing documents — deterministic and dependency-free."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
