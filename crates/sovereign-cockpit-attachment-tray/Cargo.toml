[package]
name = "sovereign-cockpit-attachment-tray"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Per-draft attachment tray with size + count limits."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
