[package]
name = "sovereign-cockpit-memory-meter"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit memory-usage gauge. Tracks (used_bytes, total_bytes) + warn/critical % thresholds. used_pct, zone (Normal/Warn/Critical), render_display ('1.4 GB / 8 GB (17%)' with GB/MB/KB unit selection). Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
