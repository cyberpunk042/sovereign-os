[package]
name = "sovereign-cockpit-workspace-state"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Workspace layout snapshot (panes + active + scroll offsets) with save/load."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
