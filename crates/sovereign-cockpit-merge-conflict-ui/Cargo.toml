[package]
name = "sovereign-cockpit-merge-conflict-ui"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Three-way merge conflict UI state with per-hunk resolution."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
