[package]
name = "sovereign-cockpit-task-stack"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit operator-task stack. push/pop tasks; current() returns the active task (top of stack); peek_below() returns the next-up task (for breadcrumb-style display). Pure descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
