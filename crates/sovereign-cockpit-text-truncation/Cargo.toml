[package]
name = "sovereign-cockpit-text-truncation"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Char-aware text truncation for cockpit rows — ellipsize mid/end/middle so narrow columns stay readable."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
