[package]
name = "sovereign-cockpit-search-filter"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit composite-search filter state. Tracks (query_text, facet_set, sort_key, sort_direction) bundled into one immutable snapshot so the data layer can request results deterministically. apply_facet / clear_facets / set_query / set_sort mutators. Pure UX descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
