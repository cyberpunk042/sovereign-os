[package]
name = "sovereign-cockpit-perimeter-panel"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Cockpit panel for the selfdef IPS real-time security perimeter (Tetragon sovereign-kernel-fence) — consumes the selfdef-emitted OCSF jsonl + verdict ring buffer at the file boundary; produces RenderRow values for the M061 main dashboard."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
