[package]
name = "sovereign-cockpit-number-format"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit number formatter. Renders an i64 with a configurable thousands separator and (for cents) a decimal separator + minor-unit width. Includes a compact mode that abbreviates with k/M/B/T to a chosen significant-digit count. Pure presentation crate."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
