[package]
name = "sovereign-cockpit-split-button"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Split button — primary action + dropdown menu of alternatives."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
