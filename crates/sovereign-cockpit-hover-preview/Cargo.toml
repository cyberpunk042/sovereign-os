[package]
name = "sovereign-cockpit-hover-preview"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit hover-preview state. Pointer-driven peek window: enter(id, now)/leave(now)/anchor_hidden(now). visible(now) flips after dwell_ms; pinned tracks whether the operator clicked to pin the preview (then leave() does not close). Distinct from tooltip-delay (single-element tooltip) — this is the rich peek-window lane."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
