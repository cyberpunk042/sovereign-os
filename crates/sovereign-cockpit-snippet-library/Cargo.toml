[package]
name = "sovereign-cockpit-snippet-library"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit operator-curated snippet library. Each Snippet{id, name, body, trigger, tags}. add/remove; search(query, tag_filter) ranks by exact name → trigger → name-prefix → body-contains. Pure descriptor."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
