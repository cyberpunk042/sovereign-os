[package]
name = "sovereign-cockpit-color-swatch"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit color-swatch grid. Maintains ordered named (name, hex) swatches plus a selected_index. add/insert/remove/select keep the grid in sync. Pure descriptor; distinct from sovereign-cockpit-theme-palette (cockpit theme tokens) and sovereign-cockpit-color-picker (continuous picker)."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
