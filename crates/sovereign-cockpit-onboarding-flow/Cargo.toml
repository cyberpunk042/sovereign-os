[package]
name = "sovereign-cockpit-onboarding-flow"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit first-run onboarding flow — 8-step guided tour (welcome / theme / locale / profile / first-conversation / dashboard-tour / replay-tour / done). Operator advances forward/back/skip. Pure UX state."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
