[package]
name = "sovereign-cockpit-context-panel"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit sidebar context panel — composite that gathers (active conversation, active bundle, active mode, active workspace folder, current branch_id) into one signed envelope the sidebar renders. Refreshes on every cockpit state change."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
sovereign-execution-mode-registry = { path = "../sovereign-execution-mode-registry" }
sovereign-profile-bundles = { path = "../sovereign-profile-bundles" }

[features]
default = []
