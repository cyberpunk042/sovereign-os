[package]
name = "sovereign-cockpit-keystroke-map"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit keyboard shortcut registry — operator-customizable shortcut bindings (Ctrl/Shift/Alt + key -> action-id) with conflict detection. Each binding declares the (modifiers, key, action_id, scope, description). The cockpit reads this to render the shortcut overlay + dispatch keypresses."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
