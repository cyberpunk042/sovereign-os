[package]
name = "sovereign-cockpit-toggle-event"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit toggle-event log — operator-signed append-only record of every dashboard toggle / feature flip / mode boolean change. Used by replay engine to reconstruct UI state and by audit to track operator-driven configuration shifts."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
