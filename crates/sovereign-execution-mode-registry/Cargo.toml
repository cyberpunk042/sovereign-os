[package]
name = "sovereign-execution-mode-registry"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Runtime execution-mode registry — Plan / DryRun / Shadow / Sandbox / Execute / Replay / Debug. Each mode declares (writes-allowed, network-allowed, snapshot-required, replay-source-required) so the cockpit can render the active mode badge + the dispatcher can refuse incompatible operations."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
