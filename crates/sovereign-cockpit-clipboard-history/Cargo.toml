[package]
name = "sovereign-cockpit-clipboard-history"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit clipboard history — bounded FIFO of recent operator-copied snippets (text / link / trace_id / command-output). Capacity 100. Each entry carries a kind tag + copied-at timestamp. Pure UX surface."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
