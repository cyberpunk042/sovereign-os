[package]
name = "sovereign-cockpit-marquee-loop"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit marquee loop. When a label's text_px exceeds its container_px, returns an x_offset that loops [0..gap+text_px] over time. Below the threshold the label is static. Pure presentation crate; reduced-motion mode forces static."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
