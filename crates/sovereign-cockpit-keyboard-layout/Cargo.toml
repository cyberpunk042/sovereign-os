[package]
name = "sovereign-cockpit-keyboard-layout"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Cockpit keyboard-layout selector. Layout{Qwerty/QwertyUk/Dvorak/Colemak/Azerty} + per-layout description label. set(layout); current() returns the chosen layout. description(layout) returns the operator-facing label. Pure descriptor for the settings UI; keystroke physical-key remap lives in the OS, not the cockpit."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
