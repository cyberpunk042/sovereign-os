[package]
name = "sovereign-mode-transition-log"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Runtime mode-transition audit log — append-only record of every ExecutionMode switch (from, to, reason, operator signature, timestamp, trace_id). Enables the cockpit to render the day's mode timeline and the daemon to assert no illegal transitions occurred."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
sovereign-execution-mode-registry = { path = "../sovereign-execution-mode-registry" }

[features]
default = []
