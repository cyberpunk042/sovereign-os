[package]
name = "sovereign-cockpit-selection-summary"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Selected-items summary — count, sum, unique categories."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
