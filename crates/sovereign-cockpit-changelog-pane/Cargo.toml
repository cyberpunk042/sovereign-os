[package]
name = "sovereign-cockpit-changelog-pane"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Versioned changelog entries with per-entry read state."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
