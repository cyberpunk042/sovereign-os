# Python
__pycache__/
*.py[cod]
*.egg-info/

# Build artifacts (OS image pipeline)
/build/
/dist/
*.iso
*.iso.sig
*.img
*.img.sig
*.qcow2
*.raw
*.sbom.json

# Live-build / debootstrap working dirs
/cache/
/chroot/
.live-build/
binary/
config/auto/build

# Sovereign-os build state (resume-from-step)
.sovereign-os-build/
.build-state/
*.build-state.json
*.build-state.yaml

# tmpfs / kernel-forge scratch
/tmpfs-state/
/mnt/kernel_forge/

# Models cache (never committed; weights live on tank/models)
# — but DO commit the declared catalog manifest authored at R156.
/models/*
!/models/catalog.yaml
*.gguf
*.safetensors
*.bin
*.pt

# Generated profile renderings
/rendered/

# Documentation build
/docs/book/
/book/

# Editors
.idea/
*.swp
*~
.DS_Store
Thumbs.db

# Secrets / env / local overrides
.env
.env.*
secrets/
*.local.yaml
*.local.toml
local.yaml
local.toml

# Per-developer scratch
/scratch/
TODO.local.md

# Test outputs
/test-output/
/test-vm-state/
*.qemu-state
*.nspawn-state

# Snapshots (insta-style)
**/snapshots/*.new
.sovereign-os/

# Rust build artifacts
/target/
**/*.rs.bk

# webapp DEMO capture output (scripts/webapp/demo-capture.mjs default --out)
.demo-captures/

# Built on demand by cockpit-wasm/build.sh (make cockpit-wasm) — never commit the multi-MB full bridge.
webapp/_shared/cockpit-wasm/cockpit_wasm_full.js
webapp/_shared/cockpit-wasm/cockpit_wasm_full_bg.wasm
